/** G1g: virtualize AI chat turns when count exceeds this threshold (design: avoid full mount at scale). */
export const AI_CHAT_TURN_VIRTUAL_THRESHOLD = 20;

/** Initial row height guess before `measureElement` runs (px). */
export const AI_CHAT_TURN_ESTIMATE_PX = 136;

export const AI_CHAT_TURN_VIRTUAL_OVERSCAN = 5;

export const AI_CHAT_STICK_TO_BOTTOM_THRESHOLD_PX = 120;
