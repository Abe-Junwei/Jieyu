// @vitest-environment jsdom
import { describe, it } from 'vitest';

/**
 * useSpeakerManagement 遗留场景占位测试 | Placeholder suite for legacy useSpeakerManagement scenarios
 *
 * 说明：原有用例依赖旧版 hook API，已整体 skip；
 * 后续在 API 稳定后按新签名重建完整交互测试。
 *
 * Note: The previous suite targeted a deprecated hook API and was fully skipped.
 * Rebuild full interaction tests after the new API contract is finalized.
 */
describe.skip('useSpeakerManagement legacy scenarios (pending rewrite)', () => {
  it('keeps a skipped placeholder to track rewrite work', () => {
    // 占位断言 | Placeholder assertion
  });
});
