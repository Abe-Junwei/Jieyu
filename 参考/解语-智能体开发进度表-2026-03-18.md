# 解语智能体开发进度表（按功能编号）

> 日期：2026-03-19（Phase 1 完成后更新）
> 范围：F8 / F9 / F10 / F31 / F32 / Voice Agent
> 判定标准：以当前仓库代码与测试为准（文档为辅助）

---

## 1) 总览状态

| 功能 | 状态 | 完成度判断 | 说明 |
|---|---|---:|---|
| F8 AI任务调度与状态管理 | 进行中（高级能力待补） | 92% | 已有统一 TaskRunner + cancel/retry + 全局共享，且 timeout 与僵尸任务恢复已落地，缺优先级/分级并发 |
| F9 Provider抽象层 | 已完成（超额） | 90% | 多Provider+流式+错误归一化已成型 |
| F10 强制对齐（MFA/WhisperX/WebMAUS） | 未开始（工程化层面） | 10% | 方案明确，代码侧未见完整接入 |
| F31 LLM对话与上下文感知 | 已完成（核心） | 85% | 对话、上下文、persona 自动映射、工具调用、观察器均已落地 |
| F32 项目知识库RAG | 进行中（引用高亮闭环完成） | 98% | 已接入多源检索（utterance+note+pdf）+ hybrid 重排 + 关键词直召融合 + MiniSearch 全文融合 + 结构化 citations 可点击跳转；PDF ingestion 已支持 details 字段与原始 Blob/URL 的 pdf.js 抽取回填，PDF 预览已升级为嵌入式 PDF.js viewer（替代 iframe），支持页码导航、片段搜索与黄色高亮定位、新窗打开验证；仅缺精细的段落锚点与样式优化 |

---

## 2) 逐项证据与缺口

## F8 AI任务调度与状态管理

### 已完成
- 任务数据结构与状态字段在 [db.ts](../db.ts#L236) 与 [db.ts](../db.ts#L1906)
- 全局统一调度器已落地：`TaskRunner`（queue + retry + cancel）在 [src/ai/tasks/TaskRunner.ts](../src/ai/tasks/TaskRunner.ts#L1)
- 共享 runner 单例在 [src/ai/tasks/taskRunnerSingleton.ts](../src/ai/tasks/taskRunnerSingleton.ts#L1)
- Embedding / AutoGloss 已接入统一 runner，在 [src/ai/embeddings/EmbeddingService.ts](../src/ai/embeddings/EmbeddingService.ts#L1) 与 [src/ai/AutoGlossService.ts](../src/ai/AutoGlossService.ts#L1)
- 面板已支持任务 cancel/retry，在 [src/pages/TranscriptionPage.tsx](../src/pages/TranscriptionPage.tsx#L436) 与 [src/components/AiAnalysisPanel.tsx](../src/components/AiAnalysisPanel.tsx#L892)

### 缺口
- 缺按任务类型分级并发（当前全局并发固定）
- 缺统一优先级策略（如用户交互任务优先于批量后台任务）

### 建议下一步
1. 增加 taskType 级并发配置（embed/gloss/alignment）
2. 增加统一优先级策略（交互任务优先）
3. 增加任务超时与重试策略的配置化（按任务类型可覆写）

---

## F9 Provider抽象层

### 已完成
- 抽象接口在 [src/ai/providers/LLMProvider.ts](../src/ai/providers/LLMProvider.ts)
- Provider目录与工厂在 [src/ai/providers/providerCatalog.ts](../src/ai/providers/providerCatalog.ts)
- 编排层在 [src/ai/ChatOrchestrator.ts](../src/ai/ChatOrchestrator.ts#L1)
- 关键Provider实现位于 [src/ai/providers](../src/ai/providers)

### 缺口
- 缺自动 failover 与健康检查

### 建议下一步
1. 增加 provider 健康探测（启动探针 + 周期探针）
2. 增加失败时的自动降级路由（可配置）

---

## F10 强制对齐（MFA/WhisperX/WebMAUS）

### 已完成
- 方案文档清晰，含三通道策略（MFA主导）

### 缺口
- 当前 src 目录未发现 MFA/WhisperX/WebMAUS 的工程接入模块
- 未形成“输入音频+文本 -> 对齐结果回填”的端到端链路

### 建议下一步
1. 先落地 WebMAUS 远程最小链路（低工程门槛）
2. 再落地 Tauri sidecar + MFA（高精度本地方案）
3. 最后补 WhisperX 快速对齐通道

---

## F31 LLM对话与上下文感知

### 已完成
- 对话主链路（流式 + 持久化）在 [src/hooks/useAiChat.ts](../src/hooks/useAiChat.ts#L1)
- system prompt + persona 在 [src/hooks/useAiChat.ts](../src/hooks/useAiChat.ts#L141)
- ProjectObserver 在 [src/ai/ProjectObserver.ts](../src/ai/ProjectObserver.ts#L1)
- task->persona 自动映射在 [src/hooks/useAiPanelLogic.ts](../src/hooks/useAiPanelLogic.ts#L7)
- Prompt Lab 在 [src/components/AiAnalysisPanel.tsx](../src/components/AiAnalysisPanel.tsx#L467)

### 缺口
- 多步 agent loop 未形成（当前更偏单步工具调用）
- 对话分支/回溯能力较弱

### 建议下一步
1. 引入最多2-3步的受控工具循环（防止失控）
2. 增加“重试本轮但保留上下文”能力

---

## F32 项目知识库RAG

### 已完成
- 最小RAG已接入聊天上下文，在 [src/hooks/useAiChat.ts](../src/hooks/useAiChat.ts#L1015)
- 向量检索服务支持多源检索，在 [src/ai/embeddings/EmbeddingSearchService.ts](../src/ai/embeddings/EmbeddingSearchService.ts#L138)
- embeddings 表 sourceType 预留到 note/pdf/schema，在 [db.ts](../db.ts#L259)
- note 源向量化入口已接入，在 [src/ai/embeddings/EmbeddingService.ts](../src/ai/embeddings/EmbeddingService.ts#L100)
- 对话上下文已注入来源标记（`[note:id]` / `[utt:id]`），在 [src/hooks/useAiChat.ts](../src/hooks/useAiChat.ts#L1044)
- ai_messages 已写入结构化 citations，并支持点击跳转：`utterance` / `note` / `schema` 已落地，`pdf` 支持 URL/media 引用在应用内预览面板打开并保留 note 回退，在 [src/hooks/useAiChat.ts](../src/hooks/useAiChat.ts#L1), [src/components/AiAnalysisPanel.tsx](../src/components/AiAnalysisPanel.tsx#L626), [src/pages/TranscriptionPage.tsx](../src/pages/TranscriptionPage.tsx#L941)
- PDF 引用已支持页码解析与预览增强（页码标签、拖拽移动、面板缩放、上一页/下一页、新窗打开），在 [src/utils/citationJumpUtils.ts](../src/utils/citationJumpUtils.ts#L36), [src/utils/citationJumpUtils.test.ts](../src/utils/citationJumpUtils.test.ts#L45), [src/pages/TranscriptionPage.tsx](../src/pages/TranscriptionPage.tsx#L181), [src/styles.css](../src/styles.css#L2612)
- 面板新增“向量化笔记”入口，在 [src/components/AiAnalysisPanel.tsx](../src/components/AiAnalysisPanel.tsx#L808)
- 检索已接入“向量 + 关键词”融合重排（hybrid），在 [src/ai/embeddings/EmbeddingSearchService.ts](../src/ai/embeddings/EmbeddingSearchService.ts#L230) 与 [src/hooks/useAiChat.ts](../src/hooks/useAiChat.ts#L1030)
- 检索已支持关键词直召回（即使向量候选缺失时也可召回），并与向量结果融合排序，在 [src/ai/embeddings/EmbeddingSearchService.ts](../src/ai/embeddings/EmbeddingSearchService.ts#L246), [src/ai/embeddings/EmbeddingSearchService.test.ts](../src/ai/embeddings/EmbeddingSearchService.test.ts#L433)
- 已新增最小 PDF ingestion 管线（从 media_items.details 抽取文本 -> 分块 -> 写入 `sourceType=pdf` embeddings），并接入面板触发按钮与 RAG 检索，在 [src/ai/embeddings/EmbeddingService.ts](../src/ai/embeddings/EmbeddingService.ts#L117), [src/ai/embeddings/pdfTextUtils.ts](../src/ai/embeddings/pdfTextUtils.ts#L1), [src/pages/TranscriptionPage.tsx](../src/pages/TranscriptionPage.tsx#L597), [src/components/AiAnalysisPanel.tsx](../src/components/AiAnalysisPanel.tsx#L840), [src/hooks/useAiChat.ts](../src/hooks/useAiChat.ts#L1020)
- 已接入 MiniSearch 全文召回并与向量/关键词进行可配置融合（`keywordWeight` + `fullTextWeight`），在 [src/ai/embeddings/EmbeddingSearchService.ts](../src/ai/embeddings/EmbeddingSearchService.ts#L22), [src/ai/embeddings/EmbeddingSearchService.test.ts](../src/ai/embeddings/EmbeddingSearchService.test.ts#L433)
- 已支持从原始 PDF Blob/URL 走 pdf.js 抽取文本并回填到 `media_items.details`（提取失败自动跳过，不中断整批任务），在 [src/ai/embeddings/pdfTextUtils.ts](../src/ai/embeddings/pdfTextUtils.ts#L1), [src/ai/embeddings/EmbeddingService.ts](../src/ai/embeddings/EmbeddingService.ts#L117), [src/ai/embeddings/pdfTextUtils.test.ts](../src/ai/embeddings/pdfTextUtils.test.ts#L1)
- PDF citation 已支持检索片段文本透传及 URL search 参数自动定位，各源 RAG citations 已捎带 300 字符内 snippet，在 [db.ts](../db.ts#L289), [src/hooks/useAiChat.ts](../src/hooks/useAiChat.ts#L1067), [src/pages/TranscriptionPage.tsx](../src/pages/TranscriptionPage.tsx#L200), [src/contexts/AiPanelContext.tsx](../src/contexts/AiPanelContext.tsx#L107)
- 已实现嵌入式 PDF.js 查看器组件（PdfViewerPanel），支持 Canvas 渲染、文本层与高亮、页码导航、search 参数自动定位，替代早期 iframe 方案，在 [src/components/PdfViewerPanel.tsx](../src/components/PdfViewerPanel.tsx#L1), [src/pages/TranscriptionPage.tsx](../src/pages/TranscriptionPage.tsx#L2638), [src/styles.css](../src/styles.css#L2717)

### 缺口
- PDF 字符级高亮目前用简单的黄色背景（基于文本匹配）；若需更精美体验可补：精准位置高亮、多色高亮、持久化高亮记录
- 检索融合已落地（向量+关键词+全文），但召回融合策略的参数治理（按场景模板化）尚未工程化

### 建议下一步
1. 检索融合参数治理模板化（按问答/审校/术语检索场景预设权重）
2. PDF 查看器样式优化（页面边距、缩放、打印）
3. 小规模用户测试：收集 PDF 定位体验反馈并迭代

---

## 3) 语音智能体专项（V1/V2/V3）

### Phase 1 完成（2026-03-19）

| 能力 | 状态 | 完成度 | 证据 |
|---|---|---:|---|
| V1 语音指令 -> UI操作 | ✅ 已完成 | 100% | [src/hooks/useVoiceAgent.ts](../src/hooks/useVoiceAgent.ts#L84), [src/services/IntentRouter.ts](../src/services/IntentRouter.ts#L1), [src/pages/TranscriptionPage.tsx](../src/pages/TranscriptionPage.tsx#L1451) |
| V2 实时语音输入 -> 分析建议 | ✅ 已完成（核心） | 90% | sendToAiChat 已接入 + LLM fallback 已实现，缺 AI 分析结果回填 utterance 层 |
| V3 语音转文字 -> 填充层 | ✅ 已完成 | 100% | [TranscriptionPage.tsx#L1451](../src/pages/TranscriptionPage.tsx#L1451)（insertDictation callback + IntentRouter dictation 规则链） |
| IntentRouter 规则引擎 | ✅ 已完成 | 100% | [src/services/IntentRouter.ts](../src/services/IntentRouter.ts#L1)，支持 16 种 ActionId + tool/slot-fill/chat/dictation 所有意图类型，dictation 模式不再短路 |
| EarconService 音效反馈 | ✅ 已完成 | 100% | [src/services/EarconService.ts](../src/services/EarconService.ts#L1)，Web Audio 合成，5 种音效 |
| VoiceAgentWidget UI | ✅ 已完成 | 100% | [src/components/VoiceAgentWidget.tsx](../src/components/VoiceAgentWidget.tsx#L1)，模式切换/VAD 指示/安全模式/确认对话框 |
| VAD 语音活动检测 | ✅ 已完成 | 100% | AudioContext RMS 检测，实测 silence 700ms + threshold 0.018 |
| Safe Mode 破坏性操作保护 | ✅ 已完成 | 100% | [useVoiceAgent.ts#L164](../src/hooks/useVoiceAgent.ts#L164)，safeMode=true 时 delete/merge/split 需二次确认 |

### Phase 2 计划（STT 引擎增强）

| 能力 | 状态 | 说明 |
|---|---|---|
| Ollama Whisper Local | ✅ 已完成 | Push-to-talk MediaRecorder + `/api/audio/transcriptions`，引擎切换下拉框，press-and-hold 交互 |
| Web Speech API | ✅ 已完成 | 已有完整实现 |
| 引擎自动 fallback 链 | ✅ 已完成 | `VoiceInputService` 自动 fallback：Web Speech → Ollama Whisper → Gemini（失败时尝试下一引擎） |
| Gemini Multimodal | 待开始 | V2 实时分析用，需接入 audio inlineData |

### 缺口与待办

1. Gemini Multimodal STT 接入（Phase 2 剩余项）
2. V2 分析结果回填 utterance 层（复用 useAiToolCallHandler 工具调用机制）
3. 多步 agent loop（当前仅单步工具调用）
4. Provider 自动 failover 与健康检查（F9 缺口）

---

## 4) 验证基线

- 全量测试通过：53 文件 / 429 用例（2026-03-18 基线）
- 增量验证通过：TaskRunner + Embedding + EafService 共 34 用例（2026-03-19）
- 增量验证通过：citationJumpUtils 共 6 用例（2026-03-19）
- TypeScript 类型检查通过：npm run -s typecheck（2026-03-19）
- 代表当前主干在“AI+语音”方向具备较高稳定性

---

## 5) 两周执行建议（可直接排期）

1. 第1周：F32 深化（PDF 与全文检索）
- 完成 PDF ingestion（上传 -> 抽取 -> 分块 -> 向量化）
- 引入全文索引（MiniSearch）并做向量+全文可配置融合
- 在 PDF 预览中补页内锚点/高亮落点

2. 第2周：F10 最小可用打通
- 先接 WebMAUS 远程链路（上传+轮询+回填）
- 在转写页增加“对齐任务状态卡片”
- 为失败场景补回退路径（保持纯手工流程可用）

3. 持续：F8 统一任务调度器
- 增加任务优先级与分级并发
- 统一重试/超时策略配置（按任务类型）
