# 术语表（Layer-first，1页版）

## 目标
- 统一术语，减少 layer / tier / track / lane 混用。
- 明确边界：业务域 layer-first，互操作域 tier-specialized。

## 当前产品语义（2026-03）
- 主轴（Axis）：唯一时间语义来源，等价于 utterance 时间边界集合。
- 单主轴原则：所有建段、拆分、合并、删除均作用于 utterance 主轴。
- Layer：内容视图层（转写/翻译/标注），不承载独立时间边界。
- Track：时间轴显示模式（single/multi-auto/multi-locked），仅影响展示与编辑避让。
- Lane：时间轴 UI 行与行号分配（锁定、重排、折叠等），不引入第二套时间语义。

## 术语定义
- Layer：业务层（转写层/翻译层），用于日常编辑、筛选、统计、权限。
- Tier：语言学/外部格式结构层（EAF/TextGrid/层级约束），用于导入导出和结构校验。
- Track：时间轴显示模式（single/multi-auto/multi-locked）。
- Lane：时间轴 UI 行与行号分配（锁定、重排、折叠等）。
- Utterance：语段实体（时间边界与说话人上下文）。
- Segment：历史独立边界模型实体（v2 研发产物），当前不作为主编辑语义。

## 边界规则
- 业务域（hooks/components/pages）默认只使用 layer 语义。
- tier 语义仅允许出现在桥接/互操作/语言学校验模块。
- 新增业务字段命名统一使用 layerId；不新增 tierId 作为业务字段名。
- tierId 仅用于 tier 原生语义或桥接上下文。

## 代码落地约束
- TierDefinitionDocType 的导入受静态检查约束（见 scripts/check-tier-boundary-imports.mjs）。
- TierBridgeService 是 tier 概念对业务域暴露的主要边界之一。
- 互操作特例文件（导入导出/快照恢复）允许保留 tierId：`useImportExport`、`useTranscriptionSnapshotLoader`。
- tierId 仅允许在互操作/桥接/测试代码中出现；业务域（hooks/components/pages）禁止新增 tierId。
- 不新增独立 segment 编辑入口；如需研究型能力，必须通过实验开关并在 PR 说明中声明。

## 阶段封版标准（术语与边界）
- `npm run check:tier-boundary-imports` 通过。
- `npm run check:tierid-diffusion` 通过，且 `offBoundaryMentions=0`。
- 新增代码在业务域仅使用 layer 语义（`layerId` / `LayerLinkEdge`）。
- 若需保留 tierId，必须在 PR 说明中标注“互操作特例”并给出回收计划。

## 迁移顺序（摘要）
1. 先做术语与边界收敛（文档 + 注释 + 门禁）。✅ 已完成。
2. 再做命名兼容改造（新增代码 layerId-first），持续压缩 tierId allowlist。
   - ✅ Hook 公开 API 已全部 layerId-first（`saveUtteranceText`、`saveTextTranslation` 等）。
   - ✅ components / pages / utils 已无 tierId 出现。
   - ✅ allowlist 已收紧：移除了 `/src/services/`、`/src/db/` 宽泛匹配，改为逐文件白名单。
   - ✅ DB 字段迁移完成：`UtteranceTextDocType.tierId` → `layerId`，`LayerLinkDocType.tierId` → `layerId`（DB v24）。
   - ✅ 剩余 tierId 仅存于 EAF 互操作协议（`parseEafMetaFromLayerKey`）和 tier 原生实体（`TierDefinitionDocType`/`TierAnnotationDocType`）。
3. 约束服务已拆层完成：LayerConstraintService 负责业务约束，TierBridgeService 负责 tier 互操作边界。✅ 已完成。
4. 结构性迁移。✅ 已完成：
   - ✅ `TranslationLayerDocType` → `LayerDocType`（48 文件全量重命名）。
   - ✅ `translation_layers` → `layers`（运行时集合，历史 Dexie 迁移代码保留原名）。
   - ✅ `validateTranslationLayerDoc` → `validateLayerDoc`。
   - ✅ `LayerIdBridgeService` 简化：移除 5 个 passthrough 函数，仅保留 `createLayerLink`/`withUtteranceTextLayerId`/`LayerLinkEdge`/`UtteranceTextWithoutLayerId`。
   - ✅ `segment_links` 新增 sourceLayerId / targetLayerId 索引（DB v23，历史能力）。
   - ✅ embedding 失效机制（EmbeddingInvalidationService）已落地并覆盖全部写入/删除路径。
   - ✅ legacy 测试已迁入 `src/services/`。
   - 基线：filesWithTierId=8, totalTierIdMentions=158, offBoundaryMentions=0。
   - v25 进度：`layer_segments` 新增 `utteranceId` 索引，迁移回填代码合法引用 `layerId` 前缀解析（+7 mentions / +1 file，历史兼容）。

## 历史能力说明
- segmentation v2（`layer_segments` / `layer_segment_contents` / `segment_links`）保留为历史研发能力与兼容数据结构；自当前运行时 v31 起，这三张表已从运行时集合面移除，不再作为现行实现依据。
- 当前主路径不再依赖独立 segment 编辑语义；对外验收与评审以“单主轴 + 多 lane + 多 track 展示”为准。
