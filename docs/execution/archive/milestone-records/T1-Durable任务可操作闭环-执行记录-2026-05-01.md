# T1 — Durable 任务可操作闭环 — 执行记录（2026-05-01）

对应规划：《[AI 智能体工业级史诗 — 落地方案（2026-05-01）](../plans/AI智能体工业级史诗-落地方案-2026-05-01.md)》**轨道 T1**（切片 T1-a / T1-b / T1-c）。

## 1. 目标回顾

- 长任务在 **刷新 / 重开** 后仍可理解状态：**取消**、**重试**、在约定范围内 **从 durable checkpoint 恢复**。
- **权威源**：`ai_tasks`（持久）与 `jieyu.aiChat.sessionMemory`（UX）有明确收敛顺序；能力上限见 `docs/architecture/ai-durable-agent-loop-recovery-scope.md`。

## 2. 本次交付（代码与测试）

| 切片 | 要点 |
|------|------|
| **T1-a** | 任务列表取消 `agent_loop` pending → DB 终态 + `clearPendingAgentLoopCheckpointIfTaskIdMatches`；桥接 `AssistantBridge` / `AnalysisRuntime` / `ReadyWorkspace` |
| **T1-b** | Retry 复用同一 `taskId`；成功后 `onAgentLoopTaskRetriedFromTaskList` 清 session checkpoint；任务列表文案 `resumeAgentLoopTaskRow` / `retryAgentLoopTaskRow` |
| **T1-c** | `reconcilePendingAgentLoopCheckpointFromDexie` + `useAiChat.agentLoopDexieReconcile`；多 tab 过期 `taskId` 后递归拉最新；`aiChatPendingAgentLoopFingerprint` 打通 deferred 浮窗；Vitest + Playwright `tests/e2e/aiAgentLoopHandoffAfterReload.spec.ts` |

**架构说明**：`docs/architecture/ai-durable-agent-loop-recovery-scope.md`

## 3. 合并 / 发布前验收命令（T1 收口工程化）

在包含聊天、IndexedDB 与转写页的改动上，建议 **按序** 执行：

1. `npm run typecheck`
2. `npm run check:docs-governance`（若改治理路径文档）
3. `npm run check:architecture-guard`
4. `npm run check:transcription-text-telemetry-contract`（若改 ActionId / 文案键）
5. Vitest（聊天 / embedding / reconcile / ReadyWorkspace.runtime 等相关子集，或 `npx vitest run` 全量视 CI 策略）
6. **`npm run build` 后** `npm run test:e2e:chromium`（含 `aiAgentLoopHandoffAfterReload`；`vite preview` 与当前 `dist` 须一致）
7. `npm run gate:release-evidence:core`（触及发布证据语义时）
8. 可选：`npm run gate:release-evidence:durable-orchestration`（强调 durable 编排证据时）

## 4. 收口验证执行（2026-05-01，本仓库一次跑通）

- `npm run typecheck`
- `npm run check:docs-governance`
- `npm run check:architecture-guard`
- `npm run check:transcription-text-telemetry-contract`
- `npm run gate:release-evidence:core`（生成 `docs/execution/release-gates/release-evidence/release-evidence-*-core.json`，按团队策略选择是否纳入版本库）
- `npm run build` → `npm run test:e2e:chromium`（15 passed，含 `aiAgentLoopHandoffAfterReload`）

**附带修复**：`useAiChat.sendPersistTurnAndBuildPromptContext.ts` 中 `AiChatSettings` 应从 `providerCatalog` 引入；`resolveAiChatResponsePolicy` 的 fallback locale 使用 `normalizeLocale`；`useAiChat.ts` 去除重复的 `persistOpeningTurnAndBuildPromptContext` import，并以 `sendTurnConversationId` 满足后台记忆 flush 的 `conversationId: string` 约束。

## 5. 当前状态

- **T1 主切片（a/b/c）已闭合**，整份史诗的 **T2–T4** 仍按原方案排期。
- **T2-a** 已落地：`docs/architecture/ai-execution-capability-strategy-matrix-v0.md`。**T2-b** 已落地：`src/ai/policy/resolveExecutionPolicy.ts`。**T2-c** 已落地：后台记忆 flush 每会话写配额（`featureFlags.aiBackgroundMemorySessionWriteQuota*`、`flushQuotaGate`）。下一步默认 **T3-a**（变更描述 DTO）或按史诗顺序推进 **T4**。
