# M7 执行记录（2026-04-12）

里程碑：M7（W13-W14）  
目标：扩展可控化（Manifest/Capability/Lifecycle/兼容策略 v1）

## 1. 已完成交付物

1. 扩展运行时契约
- `src/extensions/extensionRuntime.ts`
- 覆盖能力：
  - Manifest 校验（schema/version/engine/capabilities/entry）。
  - Capability 权限校验（未声明能力禁止调用）。
  - Lifecycle 协议（load/activate/deactivate/unload）。
  - 兼容协商（minHostVersion/maxHostVersion）。

2. 扩展契约测试
- `src/extensions/extensionRuntime.test.ts`
- 覆盖场景：
  - 未声明能力调用拦截。
  - 加载失败自动降级。
  - 兼容版本协商通过/拒绝分支。

3. M7 门禁脚本
- `scripts/check-m7-extension-foundation.mjs`
- `scripts/report-m7-extension-control-gate.mjs`

4. M7 门禁文档
- `docs/execution/release-gates/M7-扩展可控化门禁清单-2026-04-12.md`

## 2. 验收指标（落地）

1. 未声明能力调用拦截率 = 100%
2. 扩展加载失败可降级率 = 100%
3. 兼容性契约测试通过率 = 100%

## 3. 自动报告产物

1. `docs/execution/archive/release-gates-auto/M7-扩展可控化门禁判定报告-自动生成.md`

## 4. 本次实跑验收

1. `npm run report:m7-extension-gate`
- 结果：通过
- 自动结论：`go`

2. `npm run gate:m7-extension`
- 结果：通过（包含 `gate:m6-release` + M7 enforce 判定）
- 自动结论：`go`

3. `npm run gate:m7-extension:shadow`
- 结果：通过（shadow 告警模式）
- 自动结论：`go`

## 5. 当前状态

1. M7 门禁链路已接入并可执行（enforce/shadow）。
2. 扩展运行时已具备 Manifest/Capability/Lifecycle/兼容策略 v1。
3. M7 判定报告可自动输出 go/no-go 结果，当前为 `go`。
