# T2 — 执行层权限隔离 v1 — 执行记录（2026-05-04）

对应规划：《[AI 智能体工业级史诗 — 落地方案（2026-05-01）](../plans/AI智能体工业级史诗-落地方案-2026-05-01.md)》**轨道 T2**（切片 T2-a / T2-b / T2-c）；扩展入口盘点见《[F4 扩展入口 capability isolation 史诗（2026-05-01）](../plans/F4-扩展入口-capability-isolation-epic-2026-05-01.md)》。

## 1. 目标回顾

- 主链与至少一条后台写相关路径，在执行层走 **统一 policy 形状**（allow / ask / deny + reason），可审计、可渐进加 **quota**。

## 2. 本次交付（代码与测试）

| 切片 | 要点 |
|------|------|
| **T2-a** | 策略矩阵 v0：`docs/architecture/ai-execution-capability-strategy-matrix-v0.md` |
| **T2-b** | 真源 **`src/ai/policy/resolveExecutionPolicy.ts`**：**主链** `resolveUserDirectivePolicyDecision`（`useAiChat.toolDecisionPipeline.ts`、`useAiChat.streamCompletion.ts`）；**后台** `resolveAiChatBackgroundMemorySandboxPolicy`（`useAiChat.backgroundMemory.ts`） |
| **T2-c** | 后台记忆会话写 flush **quota**（`featureFlags.aiBackgroundMemorySessionWriteQuota*`，默认关）+ C5 `quotaSkipped` 可观测 |

**单测锚点**：`src/ai/policy/resolveExecutionPolicy.test.ts`；既有 `toolDecisionPipeline` / `backgroundMemory` 回归。

## 3. 史诗 §7 退出条件对齐（T2）

- **N=2** 类入口：**主链工具门控** + **后台记忆抽取 sandbox**，共用 **`resolveExecutionPolicy.ts`** 决策与可审计 reason 形状（与史诗 §3.3、§7 第 2 条一致）。

## 4. 合并 / 发布前验收命令（建议）

1. `npm run typecheck`
2. `npx vitest run src/ai/policy/resolveExecutionPolicy.test.ts`（并视改动跑 `toolDecisionPipeline` / `backgroundMemory` 相关用例）
3. 动治理文档时：`npm run check:docs-governance`
4. 触及发布证据：`npm run gate:release-evidence:governance` 或 **`npm run gate:release-evidence:governance:strict`**

## 5. 当前状态

- **T2 主切片（a/b/c）已在代码与架构文档中闭合**；史诗 **§7 第 2 条** 已标为整轨完成并指回本记录与 F4 史诗。
- 后续增量以 **F4 批次** 接入更多后台入口为主，不在本记录重复展开。
