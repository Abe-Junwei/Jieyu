# 批次A手工验收证据（2026-03-23）

## 1. 自动化验收证据

### 1.1 全量测试
- 命令：npm run test
- 结果：通过
- 摘要：92 files passed，874 tests passed，0 failed。

### 1.2 生产构建
- 命令：npm run build
- 结果：通过
- 已知非阻塞告警：
  - onnxruntime-web 使用 eval（上游依赖特性）
  - db.ts 动静态导入混用警告
  - chunk size 警告（> 500kB）

## 2. 关键场景证据点

### 2.1 Undo 冲突保护
- 行为：冲突时中断写入，UI 提示“检测到数据已被其他操作更新，请刷新后重试”。
- 覆盖测试：src/hooks/useTranscriptionUndo.test.tsx（含冲突用例）。

### 2.2 Recovery 冲突保护
- 行为：applyRecovery 冲突时返回 false，不隐藏恢复入口，给出友好错误提示。
- 覆盖测试：src/hooks/useTranscriptionRecoveryActions.test.tsx。

### 2.3 导入失败可见化
- 行为：readFileAsText 失败与 archive replace-all 失败均进入统一错误分支；冲突错误走专用提示。
- 覆盖测试：src/hooks/useImportExport.error.test.tsx。

### 2.4 批量操作失败链路
- 行为：主失败 error 日志 + rollback 失败 warn 日志；用户收到“失败，已回滚”提示。
- 覆盖测试：src/hooks/useTranscriptionUtteranceActions.test.tsx（日志矩阵用例）。

## 3. 截图与人工录屏
- 当前仓库尚未附带本轮截图/录屏文件。
- 建议后续补齐 3 组证据：
  1. 导入冲突提示
  2. 恢复冲突提示与重试入口
  3. 批量操作失败后的回滚提示
