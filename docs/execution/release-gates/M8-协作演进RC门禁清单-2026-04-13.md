# M8 协作演进 RC 门禁清单（2026-04-13）

里程碑：16周执行版路线图 M8（W15-W16）  
目标：协作演进起步（冲突语义、回滚预案、RC 自动评审）

## P0（阻断）
- 冲突可检测率 < 100%。
- 冲突解决后数据一致率 < 99.9%。
- 冲突解决契约测试通过率 < 100%。
- M8 基础文件或核心契约缺失。

## P1（灰度）
- shadow 与 enforce 报告结论不一致。
- RC 报告缺失关键指标表格或结论字段。

## P2（观察）
- 跨设备阶段 CRDT/OT 仅完成选型结论，尚未落地运行时。

## 门禁入口
1. `npm run check:m8-collaboration-foundation`
2. `npm run test:m8-collaboration-contract`
3. `npm run report:m8-collaboration-rc`
4. `npm run report:m8-collaboration-rc:shadow`
5. `npm run gate:m8-collaboration`
6. `npm run gate:m8-collaboration:shadow`

## 自动产物
1. `docs/execution/release-gates/M8-协作演进RC评审报告-自动生成.md`

## 判定语义
1. enforce 模式：出现任一 P0 即 no-go，并返回非零退出码。
2. shadow 模式：出现 P0 仅告警，不阻断流水线。
3. RC 阶段以冲突检测率/一致率为核心发布约束。
