# M12 多副本批量同步门禁清单（2026-04-13）

里程碑：M12（W21-W22）  
目标：多副本批量同步治理（分片同步、法定人数、批量回滚、一致性收口）。

## P0（阻断）
- 批量同步契约通过率 < 100%。
- quorum 契约通过率 < 100%。
- 回滚契约通过率 < 100%。
- 多副本一致性契约通过率 < 100%。
- M12 基础文件或核心契约缺失。

## P1（灰度）
- shadow 与 enforce 报告结论不一致。
- 回滚策略可执行但缺少冲突清单或共识比率字段。

## P2（观察）
- 当前批量同步以单实体副本集合为单位，后续可扩展跨实体事务同步。

## 门禁入口
1. npm run check:m12-multi-replica-foundation
2. npm run test:m12-multi-replica-contract
3. npm run report:m12-multi-replica-gate
4. npm run report:m12-multi-replica-gate:shadow
5. npm run gate:m12-multi-replica
6. npm run gate:m12-multi-replica:shadow

## 自动产物
1. docs/execution/archive/release-gates-auto/M12-多副本批量同步评审报告-自动生成.md

## 判定语义
1. enforce 模式：出现任一 P0 即 no-go，并返回非零退出码。
2. shadow 模式：出现 P0 仅告警，不阻断流水线。
3. M12 四项核心契约阈值均为 100%。
