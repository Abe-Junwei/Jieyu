# M10 协作可用化门禁清单（2026-04-13）

里程碑：M10（W19-W20）  
目标：协作可用化（多层对象编辑、性能优化、协作者提示、连续编辑稳定性）

## P0（阻断）
- 多层对象编辑契约通过率 < 100%。
- 协作性能计划契约通过率 < 100%。
- 协作者提示契约通过率 < 100%。
- 连续编辑稳定性契约通过率 < 100%。
- M10 基础文件或核心契约缺失。

## P1（灰度）
- shadow 与 enforce 报告结论不一致。
- 性能计划可执行但缺少预算判定字段。

## P2（观察）
- 协作者提示仅聚合 actor+layer，后续可扩展到角色和审阅状态维度。

## 门禁入口
1. npm run check:m10-collaboration-foundation
2. npm run test:m10-collaboration-contract
3. npm run report:m10-collaboration-gate
4. npm run report:m10-collaboration-gate:shadow
5. npm run gate:m10-collaboration
6. npm run gate:m10-collaboration:shadow

## 自动产物
1. docs/execution/archive/release-gates-auto/M10-协作可用化评审报告-自动生成.md

## 判定语义
1. enforce 模式：出现任一 P0 即 no-go，并返回非零退出码。
2. shadow 模式：出现 P0 仅告警，不阻断流水线。
3. M10 四项核心契约阈值均为 100%。
