# M13 跨实体事务同步门禁清单（2026-04-13）

里程碑：M13（W23-W24）  
目标：跨实体事务同步治理（分片提交、原子收口、事务回滚、跨实体一致性）。

## P0（阻断）
- 事务同步契约通过率 < 100%。
- 原子性契约通过率 < 100%。
- 回滚契约通过率 < 100%。
- 跨实体一致性契约通过率 < 100%。
- M13 基础文件或核心契约缺失。

## P1（灰度）
- shadow 与 enforce 报告结论不一致。
- 事务回滚可执行但缺少阻断实体列表或一致性比率字段。

## P2（观察）
- 当前事务同步以单项目实体集合为单位，后续可扩展跨项目事务协调。

## 门禁入口
1. npm run check:m13-transaction-sync-foundation
2. npm run test:m13-transaction-sync-contract
3. npm run report:m13-transaction-sync-gate
4. npm run report:m13-transaction-sync-gate:shadow
5. npm run gate:m13-transaction-sync
6. npm run gate:m13-transaction-sync:shadow

## 自动产物
1. docs/execution/release-gates/M13-跨实体事务同步评审报告-自动生成.md

## 判定语义
1. enforce 模式：出现任一 P0 即 no-go，并返回非零退出码。
2. shadow 模式：出现 P0 仅告警，不阻断流水线。
3. M13 四项核心契约阈值均为 100%。
