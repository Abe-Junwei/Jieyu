# M7 扩展可控化门禁清单（2026-04-12）

里程碑：16周执行版路线图 M7（W13-W14）  
目标：扩展可控化（Manifest/Capability/Lifecycle/兼容策略 v1）

## P0（阻断）
- 未声明能力调用拦截率 < 100%。
- 扩展加载失败可降级率 < 100%。
- 兼容性契约测试通过率 < 100%。
- M7 基础文件或核心契约缺失。

## P1（灰度）
- shadow 与 enforce 报告结论不一致。
- 关键门禁脚本可执行但无自动报告产物。

## P2（观察）
- 扩展能力枚举需新增业务域覆盖（当前先覆盖转写/语言资产/AI）。

## 门禁入口
1. `npm run check:m7-extension-foundation`
2. `npm run test:m7-extension-contract`
3. `npm run report:m7-extension-gate`
4. `npm run report:m7-extension-gate:shadow`
5. `npm run gate:m7-extension`
6. `npm run gate:m7-extension:shadow`

## 自动产物
1. `docs/execution/archive/release-gates-auto/M7-扩展可控化门禁判定报告-自动生成.md`

## 判定语义
1. enforce 模式：出现任一 P0 即 no-go，并返回非零退出码。
2. shadow 模式：出现 P0 仅告警，不阻断流水线。
3. M7 三项核心指标阈值均为 100%。

## 本次结果（2026-04-12）
1. `npm run gate:m7-extension`：通过，结论 `go`。
2. `npm run gate:m7-extension:shadow`：通过，结论 `go`。
