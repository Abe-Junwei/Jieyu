# M9 协作规则与恢复门禁清单（2026-04-13）

里程碑：M9（W17-W18）  
目标：协作规则与恢复机制（冲突优先级、仲裁入口与日志、断网重连一致性校验）

## P0（阻断）
- 冲突优先级契约通过率 < 100%。
- 仲裁入口与操作日志契约通过率 < 100%。
- 断网重连一致性契约通过率 < 100%。
- 断网-联机合并后存在结构损坏未被识别。
- M9 基础文件或核心契约缺失。

## P1（灰度）
- shadow 与 enforce 报告结论不一致。
- 关键门禁脚本可执行但未生成自动报告。

## P2（观察）
- 仲裁规则仅覆盖当前冲突类型，需扩展到跨对象批处理冲突。

## 门禁入口
1. `npm run check:m9-collaboration-foundation`
2. `npm run test:m9-collaboration-contract`
3. `npm run report:m9-collaboration-gate`
4. `npm run report:m9-collaboration-gate:shadow`
5. `npm run gate:m9-collaboration`
6. `npm run gate:m9-collaboration:shadow`

## 自动产物
1. `docs/execution/release-gates/M9-协作规则与恢复评审报告-自动生成.md`

## 判定语义
1. enforce 模式：出现任一 P0 即 no-go，并返回非零退出码。
2. shadow 模式：出现 P0 仅告警，不阻断流水线。
3. M9 三项核心契约阈值均为 100%。
