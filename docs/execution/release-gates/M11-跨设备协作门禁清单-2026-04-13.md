# M11 跨设备协作门禁清单（2026-04-13）

里程碑：M11（W19-W20）  
目标：跨设备协作治理（版本向量、时钟漂移治理、回滚策略、一致性校验）。

## P0（阻断）
- 版本向量契约通过率 < 100%。
- 时钟漂移治理契约通过率 < 100%。
- 回滚契约通过率 < 100%。
- 跨设备一致性契约通过率 < 100%。
- M11 基础文件或核心契约缺失。

## P1（灰度）
- shadow 与 enforce 报告结论不一致。
- 回滚策略可执行但缺少 checkpoint 标识。

## P2（观察）
- 向量冲突治理仅覆盖双副本场景，后续扩展到多副本批量同步。

## 门禁入口
1. npm run check:m11-cross-device-foundation
2. npm run test:m11-cross-device-contract
3. npm run report:m11-cross-device-gate
4. npm run report:m11-cross-device-gate:shadow
5. npm run gate:m11-cross-device
6. npm run gate:m11-cross-device:shadow

## 自动产物
1. docs/execution/release-gates/M11-跨设备协作评审报告-自动生成.md

## 判定语义
1. enforce 模式：出现任一 P0 即 no-go，并返回非零退出码。
2. shadow 模式：出现 P0 仅告警，不阻断流水线。
3. M11 四项核心契约阈值均为 100%。
