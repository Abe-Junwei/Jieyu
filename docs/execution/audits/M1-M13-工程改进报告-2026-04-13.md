# M1-M13 工程改进报告（2026-04-13）

日期：2026-04-13  
范围：M1-M13（数据内核、领域收口、观测与发布门禁、扩展治理、协作治理）

## 1. 结论摘要

M1-M13 完成后，工程从“功能可运行”升级为“可验证、可门禁、可回滚、可演进”的产品化工程体系。

当前状态（本次复核）：
1. M6 发布门禁结论为 go。
2. M7 扩展门禁结论为 go。
3. M8 协作 RC 门禁结论为 go。
4. M9-M13 联合门禁链（gate:m13-transaction-sync）实跑通过，自动结论均为 go。

## 2. 审查方法

1. 文档证据：执行记录、门禁清单、自动评审报告。
2. 代码证据：核心运行时模块、门禁脚本、契约测试入口。
3. 运行证据：实跑门禁链路（enforce 模式）。

## 3. 之前 vs 现在（工程改进矩阵）

| 维度 | 之前 | 现在（M1-M13 后） | 证据 |
|---|---|---|---|
| 数据正确性治理 | 依赖人工经验，回归不可重复 | 不变量测试 + 迁移回放基线可重复执行 | `src/db/schemaInvariants.test.ts`、`src/db/migrationReplayBaseline.test.ts`；M1执行记录 |
| 导入导出稳定性 | 有功能但幂等性证据弱 | round-trip 幂等门禁化（可阻断） | M2执行记录；`test:m2-import-export-idempotency` |
| 主路径回归 | 以人工冒烟为主 | 主路径 E2E 进入 CI 阻断链路 | M2执行记录；`test:m2-mainpath-e2e` |
| 页面-领域边界 | 页面易直连底层，耦合回流 | 应用服务编排层落地，旧入口转发与迁移清单并行治理 | `src/app/TranscriptionAppService.ts`、`src/app/transcriptionLegacyForwarding.ts`、`src/app/m3MigrationChecklist.ts` |
| 架构约束执行 | 规范在文档，执行弱 | 架构守卫脚本可执行、可门禁 | `check:architecture-guard`、`gate:m3-dependency-direction` |
| 观测能力 | 指标碎片化，难形成发布依据 | M5 指标目录、采集、趋势报告闭环 | `src/observability/metrics.ts`、M5执行记录 |
| 发布决策 | 口头 go/no-go，可追责性弱 | M6 SLO 分级门禁（P0 阻断、P1 灰度）+ 自动报告 | M6执行记录；M6自动报告 |
| 扩展安全边界 | 缺少统一能力声明与权限门禁 | Manifest/Capability/Lifecycle/兼容协商运行时 + 契约测试 | `src/extensions/extensionRuntime.ts`、M7执行记录 |
| 协作基础（M8） | 冲突处理偏策略描述 | 字段/实体/会话冲突检测、解决、一致性评估运行时化 | `src/collaboration/collaborationConflictRuntime.ts`、M8执行记录 |
| 协作恢复（M9） | 断网重连与仲裁留痕薄弱 | 冲突优先级、仲裁 ticket、操作日志、重连一致性校验 | `src/collaboration/collaborationRulesRuntime.ts`、M9执行记录 |
| 协作可用化（M10） | 多层编辑与稳定性缺少制度化验证 | 多层编辑、性能分块、协作者提示、连续编辑稳定性门禁化 | `src/collaboration/collaborationBetaRuntime.ts`、M10执行记录 |
| 跨设备与多副本（M11-M13） | 跨设备/多副本/事务语义分散 | 版本向量、quorum、多副本批同步、跨实体事务同步形成链式门禁 | `src/collaboration/collaborationCrossDeviceRuntime.ts`、`src/collaboration/collaborationMultiReplicaRuntime.ts`、`src/collaboration/collaborationTransactionSyncRuntime.ts` |

## 4. 关键量化结果（最新）

1. M6（enforce）：go，P0=0，build.success_rate=1.0000。
2. M7（enforce）：go，未声明能力拦截率 100%。
3. M8（enforce）：go，冲突可检测率 100%，一致性 100%。
4. M9-M13：本次串联门禁实跑均为 go（enforce）。

## 5. 工程形态变化（平台化价值）

1. 从“功能开发仓库”转向“工程治理仓库”：测试、门禁、报告形成闭环。
2. 从“单点修 bug”转向“分层演进”：数据内核、应用编排、发布治理、协作治理层次清晰。
3. 从“可交付”转向“可持续交付”：每个阶段都可在脚本层复验，不依赖个体记忆。

## 6. 当前边界与后续建议

1. M14 尚未在当前仓库定义（未发现 M14 计划/门禁脚本/自动评审报告）。
2. 建议新增 M14，重点聚焦：
- 生产样本替代 synthetic 样本（减少观测偏差）。
- 跨项目协作与补偿事务编排（承接 M13 后续）。
- 报告统一索引与周度审计摘要自动生成。

## 7. 证据索引

1. 路线图：`docs/execution/plans/16周执行版路线图-2026-04-12.md`
2. M1-M13 执行记录：`docs/execution/plans/`
3. M6-M13 自动评审报告：`docs/execution/release-gates/`
4. 门禁脚本链：`package.json`（gate:m6 ~ gate:m13）
