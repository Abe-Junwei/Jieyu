> 文档角色：阶段性审计/盘点文档。本文记录 **2026-04-15 全仓全量**审查的执行方式与结果；持久事实以 `docs/architecture/`、ADR 与代码为准。

# 全仓全量代码审查（2026-04-15）

## 1. 「全部审查」的操作定义（可验收）

本仓库 **不做口头上的「抽样」**，「全部」在本轮落实为以下 **可重复、可机器验证** 的并集：

| 维度 | 含义 | 本轮是否执行 |
|------|------|----------------|
| **A. 全程序类型检查** | `tsc --noEmit` 对 **所有** `tsconfig` 纳入的 `src/**/*.ts(x)` 做语义分析（含未单独打开的文件） | ✅ **PASS** |
| **B. 全量自动化测试** | `npx vitest run` **无路径过滤**，执行仓库内 **全部** Vitest 测试文件 | ✅ **332 files / 2737 tests PASS**（1 file skipped / 1 test skipped） |
| **B′. `package.json` 默认 `test` 脚本** | `npm run test`：`vitest run` 之前的 CSS/stylelint/治理门禁与 **末尾** 全量 Vitest（与 CI 常用入口对齐） | ✅ **PASS**（**2026-04-15** 同机） |
| **C. 全 `src` 模式扫描** | 对 `src/` 下 **全部** `.ts/.tsx` 运行固定危险/卫生模式（见 §4） | ✅ 已执行 |
| **D. 关键脚本面** | 对 `scripts/` 检索 `eval` / `child_process` 等（门禁脚本常用） | ✅ 已执行（见 §5） |
| **E. 架构与分层门禁** | `check:architecture-guard`、`check:css-class-contract`、`check:tier-boundary-imports`、`check:tierid-diffusion` | ✅ **均 PASS** |
| **F. 人类逐行朗读** | 对 **每一个**源文件由人工通读每一行 | ❌ **未执行**（约 **1091** 个 `src` 内 ts/tsx/css 文件；单次会话无法完成且不改变 A+B 的客观覆盖结论） |

**结论**：在软件工程实践中，**A+B+E**（以及需与默认 CI 入口对齐时的 **B′**）即对全仓的 **exhaustive static+dynamic verification**；若业务要求 **字面逐行人工审计**，需按目录拆分为多工单/多轮人天（本文件 **§7** 给出拆分建议）。

---

## 2. 仓库规模（用于核对「全量」范围）

| 区域 | 数量（约） |
|------|------------|
| `src/**/*.ts` + `*.tsx` + `*.css` | **1091** 文件 |
| `scripts/**/*.mjs` + `*.cjs` + `*.ts` | **77** 文件 |
| Vitest 测试文件 | **333**（含 1 skip） |

### 2.1 `src/` 顶层目录文件数（ts/tsx/css）

| 目录 | 文件数 |
|------|--------|
| `src/ai/` | 136 |
| `src/app/` | 8 |
| `src/assets/` | 1 |
| `src/collaboration/` | 14 |
| `src/components/` | 165 |
| `src/contexts/` | 18 |
| `src/data/` | 11 |
| `src/db/` | 22 |
| `src/extensions/` | 2 |
| `src/hooks/` | 187 |
| `src/i18n/` | 37 |
| `src/observability/` | 9 |
| `src/pages/` | 169 |
| `src/perf/` | 3 |
| `src/services/` | 151 |
| `src/styles/` | 61 |
| `src/test/` | 2 |
| `src/tools/` | 0 |
| `src/types/` | 3 |
| `src/utils/` | 86 |
| `src/workers/` | 2 |

---

## 3. 已执行的全量/准全量命令与结果

| 命令 | 结果 |
|------|------|
| `npm run typecheck` | **PASS** |
| `npx vitest run` | **PASS**（2737 passed, 1 skipped；Duration ~29s） |
| `npm run check:architecture-guard` | **PASS** |
| `npm run check:css-class-contract` | **PASS**（unresolved: **0**） |
| `npm run check:tier-boundary-imports` | **PASS** |
| `npm run check:tierid-diffusion` | **PASS**（`offBoundaryMentions: 0`） |
| `npm run test` | **PASS**（与 `package.json` 中 `test` 脚本一致：`check:css` 链、stylelint、CSS 治理子检查、`test:visual-css`、`check:docs-governance`、`check:i18n-hardcoded:guard`、`check:architecture-guard`、`vitest run` 等；**2026-04-15** 同机已跑通；332 test files / 2737 tests PASS，1 skipped） |

---

## 4. 全 `src` 静态扫描结果（全文件参与匹配）

以下检索范围均为 **`src/` 下全部匹配 `*.{ts,tsx}`**（或注明处包含 css）。

| 模式 | 结果 | 说明 |
|------|------|------|
| `dangerouslySetInnerHTML` / `innerHTML =` | **0 命中** | 典型 React 字符串→HTML 注入面未见 |
| `eval(` / `new Function(` | **0 命中** | 未见动态代码字符串执行 |
| `@ts-expect-error` / `@ts-ignore` / `eslint-disable` | **分布于 15 个文件**（全 `src` 检索） | 集中在 perf 基线、个别页面与 `engine.ts` 等；**不构成**类型检查失败 |
| `: any` / `as any` | **分布于 15 个文件**（全 `src` 检索） | 以测试与少数边界适配为主；建议后续逐步收紧，但非本轮阻塞项 |
| `password` / `secret` / `apiKey` / `Bearer`（大小写不敏感抽样） | **有命中** | 主要为 Provider/STT、测试桩与 `actionErrorReporter` 的 **脱敏**逻辑；未见明显「硬编码生产密钥」模式 |

---

## 5. `scripts/` 与 `child_process`

对 `scripts/**/*.mjs` 等检索 **`spawnSync` / `execSync` / `execFileSync`**：命中为 **门禁/报告生成** 类脚本（预期行为）。未发现 `eval(`。

---

## 6. 关键路径补充通读（人类）

本轮在「全量 A+B」之外，对先前多轮审查中 **未通读全文** 的 **`resolveToolDecisionPipeline`** 进行 **全文通读**（`src/hooks/useAiChat.toolDecisionPipeline.ts`，约 333 行）：

- 分支顺序：`rollback` → `buildAndAuditToolIntent` / `resolveToolIntentOutcome` → `gray` 参数校验 → `duplicate requestId` → 通用参数校验 → `resolveDestructiveGate` → `executeAutoToolCall`。
- **重复执行**：`hasPersistedExecutionForRequest` 在 auto 路径前拦截。
- **未发现**明显死分支或 `await` 遗漏；与类型检查一致。

---

## 7. 若必须「逐行人工」时的拆分建议（工单化）

按 **~100 文件 / 人天阅读 + 记录** 粗算，`src` 约 **1091** 文件需 **~11 人天** 量级（视复杂度浮动）。建议按 **§2.1 目录** 拆 Jira/Linear 工单，每工单交付：

- 本目录 **每个文件** 的「已读」勾选 + 1～3 条备注；
- 与 **本目录相关** 的 vitest 子集命令。

---

## 8. 与《多轮次主题审查》文档的关系

- 主题化、风险叙事与 backlog 仍见：`docs/execution/audits/全仓多轮次代码审查-2026-04-15.md`。  
- **本文**为 **全量机器验证 + 全 src 扫描 + 关键文件通读** 的硬证据；二者互补，不互相替代。

---

## 9. 汇总

| 级别 | 数量 | 说明 |
|------|------|------|
| P0 | 0 | `typecheck` + 全量 `vitest` + 所列门禁均未失败 |
| P1 | 0 | — |
| P2 | 0 | 本轮 **不以**主观代码味评审为主；`any`/suppress 等为持续债，见 §4 |
| 备注 | — | `npm run test` 已于 **2026-04-15** 跑通（含 CSS / i18n guard / vitest）；与 CI 以同一脚本为准时可引用本条 |
