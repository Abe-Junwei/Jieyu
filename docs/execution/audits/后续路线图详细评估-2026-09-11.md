---
title: 后续路线图详细评估（2026-09-11）
doc_type: audit
status: active
owner: repo
last_reviewed: 2026-09-11
source_of_truth: subsequent-roadmap-eval-2026-09-11
---

# 后续路线图详细评估（2026-09-11）

> **问的是「接下来做什么、按什么序、什么不要做」**，不是再列一遍已合入切片。代码锚点：`main` `b2a78575`。对照：[主路线图](../plans/解语-主路线图-master-roadmap-2026-06-01.md)、[代码核对](./主路线图代码核对-2026-09-11.md)、标注/语料子计划、开放门槛。
> **粒度口径**：主路线图 S/M/L（≈0.5–2d）对剩余项系统性偏低（2026-06-01 合理性审计已指出）。下文用 **侵入面**（文件数 / 是否碰 ChatWindow / 是否新 Dexie 版本 / 是否新 flag）代替日历。

## 0. 一句话

Stage B **P0 产品代码**已在 `main`（flag 关）。后续不是「再开占位页」，而是四条并行轨：**(1) 合入 B5c** → **(2) flag dogfood，不切默认** → **(3) 标注 M1 余量 / 词典编辑 / C3 导出，各开新切片 ID** → **(4) 转写内核 A1 与 Agent 放量最后**。B7 与「宣告工作台开放」现在都 **NO-GO**。

## 1. 现在真正还剩什么（按轨）

### 轨 1 — 收口已写完、未进 main 的代码

| 项 | 代码事实 | 侵入面 | 风险 |
| --- | --- | --- | --- |
| **B5c** HTML 剪贴板 + workset bundle | [#154](https://github.com/Abe-Junwei/Jieyu/pull/154) +790/−25；`corpusWorksetBundle.ts` 等。相对 `main` **CONFLICTING**（B1 滚动、B2 事件、本轮路线图文档） | 语料页 + clipboard helper + SDD；**不**接 ChatWindow | 冲突文件含 `CorpusLibraryPage.test.tsx`（必须同时保留 B1 scroll + B2 refetch + B5c HTML 用例） |
| 路线图校准 PR | [#165](https://github.com/Abe-Junwei/Jieyu/pull/165) | 纯 docs + CI heap | 应 **先于** #154 rebase，否则 B5c 再打一次路线图冲突 |

**建议**：#165 绿了再 rebase #154，不要并行改同一批 docs。

### 轨 2 — Flag dogfood（不是新功能）

| Flag | 代码完整度 | 切默认 `true` 的真实门槛 |
| --- | --- | --- |
| `annotationPageEnabled` | B4a-1/a-2/B4b 写路径 + vitest 已在 | 拍板：false 合并、自用 1 周。**另外**：[开放门槛](../plans/独立工作台开放门槛与复用边界-2026-03-30.md) 仍写「Annotation 是说明页」→ 未改该文之前 **不得宣告产品开放** |
| `corpusLibraryPageEnabled` | B5a+B5b；B5c 未合 | 至少等 B5c 合入再 dogfood HTML 路径；默认仍 false |
| `lexiconAttachmentsEnabled` | B8 已落地 | 词典仍无词条 **编辑表单**；附件可 dogfood，不构成 M3 完成 |
| `aiSemanticGuardEnabled` / `aiAgentUiPreviewEnabled` / `aiExternalMcp*` | 代码在，默认 false | 外连 MCP 放量另需 trust 操作手册；**不**与三页开放绑在一起 |

**不要做**：为了「看起来完成」把页面 flag 默认改 `true`。开放门槛第五节 go（全量 `npm test`、发布证据、专属手工脚本）仍未对标注/语料勾完。

### 轨 3 — 三页产品余量（必须新切片，禁止塞回 B4a/B5a）

标注子计划 M1「待完成」对照代码：

| 余量 | 代码 | 建议切片 | 侵入面 | 依赖 |
| --- | --- | --- | --- | --- |
| **段播放** | `annotationKeyboardMachine` 有 `playToggle`，**未**接到媒体 player | **B4c** | 标注 controller + 复用转写 playback runtime，禁止复制 Orchestrator | 可复用 `useTranscriptionWaveformBridgeController` 的只读 seek，不要拉 ChatWindow |
| **note / tag / selfCertainty** | 转写侧有 selfCertainty；标注 IGT **无**这些字段 UI | **B4d** | 新 controller；可能写 `user_notes` 或 unit 字段；须 R1–R8 | 与转写笔记分轨，禁止第二套真源 |
| **AutoGloss 采纳 UI** | `AutoGlossService` + `useAiToolCallHandler.annotationAdapters` 走 **转写 AI** | **B4e** | 若做在 `/annotation`：只读建议 + 确认写 `unit_tokens`；若继续只放转写 ChatWindow 则 **不算标注页完成** | A11 preview 若走写工具；禁止静默覆盖 `manualConfirmed` |
| **二次自动分词** | 明确不在 B4b | **B4f / 以后** | 高风险写 `unit_tokens` | 子计划：只出 `alternativeAnalysis`，默认不覆盖人工 |

词典 M3：

| 余量 | 代码 | 建议 |
| --- | --- | --- |
| 词条编辑表单 | `saveLexeme` API 在 LinguisticService；**页面无调用** | **B3b** 编辑（与只读 B3 分轨） |
| 附件 | B8 flag 关 | dogfood，不单开切片 |
| FLEx/LIFT 包导出 | 转写侧已有 flextext；词典页无独立入口 | 联评 R5；不要从 `/corpus` 做出词典包 |

语料 P1 之后（子计划 §8.0）：

| 余量 | 建议 |
| --- | --- |
| P2 短稿 / CM6 | **不排期**。与「写作页」历史定位重叠；无产品压力时不要开 |
| P1–P2 从 `/corpus` 触达 EAF profile | **复用** `EafService`，禁止第二管线；排在 B5c 之后、C3 之前或并行 |
| Concordance / SQLite-WASM | Spike **有量化触发**才开（§8.1），现在没有 p95 超预算证据 |

### 轨 4 — 转写内核与导出（Stage A / C）

| ID | 剩余真相 | 该不该现在做 |
| --- | --- | --- |
| **A1** | 三份 ReadyWorkspace params 仍 ~98 个 `: any` 字段 | 工程债，**不阻塞**三页。做则 L 级、须 architecture-guard；不要顺手重构编排 |
| **A2** | readout 已统一 | **低优先 polish**，可挂在声学 PR 里，不要单开 |
| **A3** | pin 已是 React state；无 Dexie readback；仅 1 个冻结点 | 要做就写 SDD + schema；**不**当三页前置 |
| **A4b** | effort scaling flag 关；JIT 默认 limit 仍开 | 等有「6 步 payload −30%」的 eval 证据再动 |
| **A7 余量** | 「B4 写工具样本走 A11」 | 依赖标注 AI 路径；**不要**为凑 A7 去改 ChatWindow |
| **A8 余量** | inbound MCP 未把 `agentRunId` 传入 ChatWindow | 外连 MCP 放量时再补 |
| **C3a SRT/VTT** | `useImportExport` 仅 eaf/textgrid/trs/flextext/toolbox/jyt/jym | **高杠杆**：只读序列化 + 菜单项，不碰编辑模型 |
| **C3b CSV/TSV** | 同上 | 紧挨 C3a，同一读模型 |
| **C3c IGT LaTeX** | `LeipzigValidator` 已有 | 须 SDD；可与标注页导出分轨（R5） |
| **C3d Word** | 新依赖 | **默认不排** |
| **C1** | 发布检查单，不是功能切片 | 真正对外前再跑 |
| **C2 i18n** | 持续债 | 不阻塞产品刀 |
| **C4** | 协作已有独立门禁 | 不阻塞本地闭环 |

### 轨 5 — 明确不要做

- **B7 语料 AI**：`corpusBridgeAdapter` 不存在。做成「转写 ChatWindow 里多一个 corpus 模式」会破坏会话隔离，且会重新胀 ChatWindow。需要独立壳 + adapter，侵入面是 **L**，不是路线图表上的 M。
- 往 `TranscriptionPage.ChatWindow.tsx` / Orchestrator / `useTranscriptionData` 堆三页逻辑。
- EAF/TextGrid **新**导出管线。
- 二次自动分词覆盖人工结果。
- 把分析页升格为第二转写台（ADR-0033）。
- 把 `/writing` 做成独立写作页（现重定向 `/corpus`）。

## 2. 推荐执行序（依赖，不是天数）

```text
0. 合入 #165 路线图校准（避免 B5c 再撞 docs）
1. rebase + 合入 B5c（#154）     ← 唯一「已写完」的产品缺口
2. 本地 flag dogfood（标注 → 语料(+B5c) → 附件）
   同时改写「开放门槛」现状段；仍 NO-GO 对外宣告
3a. C3a → C3b（转写导出菜单，与 flag 无关，可与 2 并行）
3b. B4c 标注段播放（键盘合同已有，缺接线）
4. 二选一（产品拍板，不要两条同时开）：
   - B3b 词典编辑表单（saveLexeme 已有）
   - B4d note/tag
5. 以后：B4e AutoGloss UI、C3c、A3 persist、A1 any
6. 更以后 / 有明确用户：B7、MCP flag 放量、C1 对外检查
```

**并行约束**：3a 与 1/2 可并行（不同代码域）。3b 与 B5c 可并行。**不要** A1 与 B4c 同 PR（ReadyWorkspace vs 标注）。

## 3. 对主路线图表的修正建议（本评估落地时已部分写入）

| 现状表述 | 评估 |
| --- | --- |
| B4a/B4b 🟡 flag 关 = 下一刀开标注 | **错**。下一刀不是再写 IGT，是 **播放 / note / AutoGloss** 或 **flag dogfood** |
| B7 粒度 M、blocked A7/A9/A12 | 代码阻塞已解除；真实是 **新 adapter + 会话隔离**，按 L 估 |
| A2/A3 ⬜ | 已改为 🟡；A3 不要当「没做 inspector」 |
| 开放门槛「三页仍是说明页」 | **文档债**，会让人误判不能 dogfood。Dogfood ≠ 产品开放 |

## 4. 开放 vs dogfood

| | Flag 开（本地/env） | 产品宣告开放 |
| --- | --- | --- |
| 含义 | 开发者自用，默认仓库仍 `false` | 满足开放门槛第五节全部 go |
| 标注 | 可以（B4b 写路径可测） | NO-GO：无段播放、无专属手工验收封账、门槛文过期 |
| 语料 | B5b 可；HTML 等 B5c | NO-GO：P1 未合入 |
| 词典 | 浏览已开、无编辑 | NO-GO：M3 编辑缺口 |

## 5. 验证本评估用到的代码点

- `JIEYU_DEXIE_TARGET_SCHEMA_VERSION = 53`
- ChatWindow **127 / 220**
- `playToggle` 仅 `annotationKeyboardMachine.ts`，无 player 接线
- 页面层无 `saveLexeme` 调用
- `useImportExport.ts` 导出：eaf / textgrid / trs / flextext / toolbox / jyt / jym（无 srt/vtt/csv）
- #154 `mergeable: CONFLICTING`，冲突面含三页测试与路线图 docs
