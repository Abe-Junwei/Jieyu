---
title: AI 智能体架构改进方案 — 第二轮全量审查（分批次 × 分角度）
doc_type: execution-audit
status: completed
owner: repo
last_reviewed: 2026-05-07
source_of_truth: code_snapshot_review
depends_on:
  - ../plans/AI智能体架构改进方案-2026-05-06.md
  - ../plans/AI智能体-战略规划与下一步-2026-05-07.md
  - ./P0-P3_coverage_matrix_and_review-2026-05-07.md
---

# AI 智能体架构改进方案 — 第二轮全量审查

> **分批次审查正式收口**见：[AI智能体架构改进方案-审查报告-分批次完结-2026-05-07.md](./AI智能体架构改进方案-审查报告-分批次完结-2026-05-07.md)（B0–B8 每批结论等级 + PR 完结表 + 文档修订清单）。

审查对象：《[AI智能体架构改进方案-2026-05-06.md](../plans/AI智能体架构改进方案-2026-05-06.md)》全文（含 §三 阶段、§九 PR 包、§十 UX 契约、§十一 Top5、里程碑与成功指标），对照 **当前仓库代码与 CI 脚本**。

**方法**：按预先约定的批次 **B0–B8** 与角度 **A1–A9** 扫查；证据为路径 + 必要时命令/计数。**不**替代 ADR-0030 / 0022 真源，仅标出规划书需修订的漂移项。

---

## 0. 结论摘要

| 维度 | 结论 |
|------|------|
| **P0 MCP 真实化** | 门面、`tools`、空 scope 门禁、auth/loopback/limit/timeout、flag、单测均已落地；**MCP `tools/call` 持久化审计**已落 **Dexie v48 表 `mcp_tool_call_audits`**（全量 `arguments` + `runtimeContextJson` + 失败路径同样写入，best-effort），与方案「可复核宿主 context / 防静默全库」意图 **满足**（正式收口见[分批次完结报告](./AI智能体架构改进方案-审查报告-分批次完结-2026-05-07.md) B1 增补）。 |
| **P1 segment_qa** | `sourceScopeSummary`、EvidencePacket `sourceSetId`/`sourceSetSnapshot`、低置信条、无证据 fallback（含 `rag_no_results`）、reflection 折叠面板（仅失败项）、`evidenceSourceRefs` 写入工具审计 metadata、segment_qa **10** 条 `workflow-segment-qa-*.json`、E2E `segmentQaEvidenceJump.spec.ts`、`suite.v1.json` 扩展 case 已对齐 **ADR-0030 分阶段口径**。规划书 **§三 P1 验收** 仍写 `citation[].sourceId` 与 `audit.toolCall.sourceRefs` 同源 `SourceRef` — 与当前实现（`UiChatMessage.citations` 无 `sourceId` 字段、`metadata.evidenceSourceRefs: string[]`）**不一致**，应修订规划或补类型层。 |
| **P2 结构减压** | `useAiChat.toolDecisionPipeline.ts` / `useAiChat.agentLoopRunner.ts` 已为 **薄 re-export**，主实现分别在 `src/ai/chat/toolDecisionPipeline.ts`、`src/ai/chat/agentLoopRunner.ts` — **超出** 旧矩阵「P2b 未做」表述，应**回写** `P0-P3_coverage_matrix_and_review-2026-05-07.md` 或在本轮后作废该行。`useAiChat.ts` **430** 行（`wc -l`），仍满足「&lt;430」边界或需将方案改为「≤430」。P2a「再下沉 4–5 个」**未在本报告逐文件计数**（保留为待办量化）。 |
| **P3 评测与证据** | `JudgeProvider`、`citationJudge`/`relevanceJudge` 实现接口、ADR-0022、`auditExportAdapter`、`skipTaxonomy`/`skipTaxonomyRollup`、`aiRuntimeReport`、`workflowExplainability`、`evalCandidateBackfill` 均已存在。缺陷 skip **清零**仍为**目标指标**（与拍板一致）。 |
| **P4 垂直复制 + Adoption** | `adoptionQueue.ts`、`AiAdoptionQueuePanel.tsx`、`annotationQaReflection.ts`、`lexemeCandidatesReflection.ts` 已存在且进 `suite.v1.json`（reflections + adoption-queue cases）。与方案 **§三 P4 验收 checkbox 全未勾** 的状态**脱节** — 建议将方案中「新增」改为「已实现/部分验收」并逐项勾选或拆「剩余缺口」。 |
| **P5 语料库 / ELAN-FLEx** | `corpusSourceSet.ts`、`elanFlexCompatibilityWorkflow.ts` 与多份 `compatibility-*.json` / `workflow-elan-flex-*.json` 已存在；**完整 source set 生命周期 UI、round-trip golden** 仍属方案长期项，本快照未逐项验证 UI。 |
| **§十一 Top5** | 优先级仍合理；**P5-2** 与主线并行度视 P4 采纳闭环而定，无代码冲突信号。 |

---

## 1. 批次 B0 — 元信息、真源与引用健康

| 项 | 结果 | 证据 / 备注 |
|----|------|-------------|
| 战略规划链接 | **存在** | `docs/execution/plans/AI智能体-战略规划与下一步-2026-05-07.md` |
| 方案状态「待审查」 | **建议更新** | 完成本轮后可改为 `已复核` 或 `执行中`，避免与 §1.2.1「已修正」并存时语义模糊。 |
| §八 → P0–P3 矩阵 | **存在** | `./P0-P3_coverage_matrix_and_review-2026-05-07.md`（其中 **P1/P2 部分行已过时**，见 §0）。 |
| ADR-0030 / 0022 | **存在** | `docs/adr/0030-vertical-workflow-template-contract.md`、`docs/adr/0022-ai-evaluation-judge-provider-contract.md` |

---

## 2. 批次 B1 — P0 + PR-P0-1～3（A1–A4 为主）

| PR / 验收点 | 状态 | 证据锚点 |
|-------------|------|----------|
| PR-P0-1 查询门面 | **满足** | `src/ai/queries/segmentReadQueries.ts`、`segmentReadQueries.test.ts` |
| PR-P0-2 MCP + 空 scope | **满足** | `tools.ts` `SEGMENT_READ_SCOPE_REQUIRED`、`tools.test.ts` |
| `localContextTools` 复用门面 | **满足** | `localContextTools.ts` import `segmentReadQueries` |
| PR-P0-3 flag / auth / loopback / limit / timeout | **满足** | `featureFlags.ts` `VITE_AI_MCP_SERVER_ENABLED`；`McpServer.ts` / `auth.ts`（与既有矩阵一致） |
| tools/call **持久化审计（宿主 scope + 全量 arguments）** | **满足（2026-05-07 增补）** | `mcpToolCallAudit.ts` + `McpServer.ts` → **`mcp_tool_call_audits`**；**非** `audit_logs`；失败亦写；细节见[分批次完结报告](./AI智能体架构改进方案-审查报告-分批次完结-2026-05-07.md) §二 B1。 |

---

## 3. 批次 B2 — P1 + PR-P1-1～3（A1、A5、A6）

| 验收点（对照 §三 阶段二 / §9.3） | 状态 | 证据锚点 |
|----------------------------------|------|----------|
| `sourceScopeSummary` 可见 | **满足** | `sourceScopeSummary.ts`；`AiChatAssistantMessage.tsx`；`useAiChat.sendTurnStreamPhase.ts` 等贴字段 |
| EvidencePacket ↔ source set 字段 | **满足** | `evidencePacket.ts`：`sourceSetId`、`sourceSetSnapshot`；`evidencePacket.test.ts` |
| 低置信视觉 + reflection 阈值对齐 | **满足** | `AiChatAssistantMessage.tsx`（`confidence` &lt; 0.5 banner）；`segmentQaReflection.ts` |
| 无证据 fallback | **满足** | `AiChatAssistantMessage.tsx` + `rag_no_results` / `sourceScopeSummary`；`AiChatAssistantMessage.test.tsx`（SSR 断言） |
| Reflection 面板 | **满足（实现口径已细化）** | 仅 **未通过** checks + `<details open>`；与方案初稿「列出通过/失败」**文案不一致** — 规划可改为「至少展示失败项」或「默认展开失败」 |
| 工具审计来源指针 | **满足（字段名与方案不同）** | `toolCallHelpers.ts` `buildToolDecisionAuditMetadata` / `buildToolIntentAuditMetadata` 含 **`evidenceSourceRefs`**；`evidenceSourceRef.ts` `evidenceSourceRefsFromToolCallForAudit` |
| `citation[].sourceId` = `SourceRef` 统一 | **不满足规划字面** | `chatDomain.types` / UI `citations` 仍为 `type`+`refId` 等；**应修订 §三 P1 验收第 3 条**或实现 `sourceId` 字段 |
| segment_qa eval **≥10** 专属 JSON | **满足（文件级）** | `scripts/agent-evals/cases/` 下 **`workflow-segment-qa-*.json` 共 10 份** |
| suite + semantic | **满足** | `scripts/agent-evals/suite.v1.json`（含 `agent-evals-semantic-cases`、`vertical-evidence-contracts`、`segment-qa-assistant-message-p1`） |
| E2E citation jump | **满足** | `tests/e2e/segmentQaEvidenceJump.spec.ts` |
| PR-P1-1 验证命令写 `AiChatCard.input.test.tsx` | **漂移** | 与 P1 助手消息/evidence **关联弱**；建议改为文档列出的 vitest / `check:agent-evals` |

---

## 4. 批次 B3 — P2 + PR-P2a（A2、A3）

| 项 | 状态 | 证据 |
|----|------|------|
| `useAiChat.ts` 行数 &lt;430 | **临界满足** | `wc -l` → **430**（建议方案写「≤430」或 ratchet 后余量 1 行） |
| architecture-guard `useAiChat` maxLines | **满足** | `scripts/architecture-guard.config.mjs` → **1100** |
| `VoiceAgentService.ts` &lt;950 | **满足** | `wc -l` → **938** |
| **整文件** `toolDecisionPipeline` / `agentLoopRunner` | **已迁移主实现** | `src/ai/chat/toolDecisionPipeline.ts`、`src/ai/chat/agentLoopRunner.ts`；hooks 侧 **12/11 行 re-export** |
| P2a 再下沉 4–5 个模块 | **未量化** | 需单独脚本或人工列表对照 §三 行动项 |

---

## 5. 批次 B4 — P3 + PR-P3-1～3（A7、A5）

| 项 | 状态 | 证据 |
|----|------|------|
| JudgeProvider + judges | **满足** | `src/ai/eval/citationJudge.ts`、`citationJudge.ts`、`relevanceJudge.ts` |
| ADR-0022 | **满足** | `docs/adr/0022-ai-evaluation-judge-provider-contract.md` |
| `auditExportAdapter` | **满足** | `src/ai/audit/auditExportAdapter.ts` + test |
| skip taxonomy / rollup | **满足** | `generate-release-evidence-bundle.mjs`（`skipTaxonomy`、`skipTaxonomyRollup`） |
| `AiRuntimeReport` / workflow 维度 | **满足** | `aiRuntimeReport.ts`（与既有矩阵一致） |
| eval candidate 回灌 | **部分** | `evalCandidateBackfill.ts` + test；与「周节奏固化 fixture」**流程文档**仍需对齐执行计划 |

---

## 6. 批次 B5 — P4 + PR-P4-1～3（A6、A8）

| 项 | 状态 | 证据 |
|----|------|------|
| `annotationQaReflection` / `lexemeCandidatesReflection` | **已实现** | `src/ai/vertical/*.ts` + `suite.v1.json` `vertical-workflow-reflections` |
| AdoptionQueue + 面板 | **已实现** | `adoptionQueue.ts`、`AiAdoptionQueuePanel.tsx`、`adoptionQueue.test.ts`、`vertical-workflow-adoption-queue` |
| 方案 §三 P4 **验收 checkbox** | **未同步** | 文档仍大量 `[ ]`；与代码事实 **不一致** — **高优先级修订规划书** |
| `VerticalWorkflowTemplate` 独立 ADR | **已由 ADR-0030 承担** | 与方案 §9.5「契约 ADR 已发布」叙述一致；若保留「VerticalWorkflowTemplate」字样，建议明确 = ADR-0030 命名空间 |

---

## 7. 批次 B6 — P5（A2）

| 项 | 状态 | 证据 |
|----|------|------|
| `corpusSourceSet` / registry / compatibility workflow | **代码存在** | `corpusSourceSet.ts`、`elanFlexCompatibilityWorkflow.ts`、`workflow-elan-flex-compatibility-01.json` 等 |
| 完整 UI 生命周期 / round-trip | **本快照未验证** | 需 UI+E2E 或语料库专项计划交叉审查 |

---

## 8. 批次 B7 — §六成功指标 + §九.7 里程碑 + §十一 Top5

- **成功指标表**：MCP、segment_qa eval 条数、guard 阈值、Voice 行数等与当前仓库 **大体一致**；**release evidence 缺陷 skip 清零**仍为过程指标。
- **里程碑 M1–M5**：为规划节奏参考；**未对照 git 里程碑标签验证**（若需，另开「发布里程碑」审查）。
- **§十一 Top5**：顺序仍成立；无代码层面冲突。

---

## 9. 批次 B8 — §十 UX 契约 + §四边界（A6、A9）

| 契约（§10.1） | 快照结论 |
|---------------|----------|
| Source scope 可见 | segment_qa 路径 **满足**；多 workflow **统一程度**需 spot-check `annotation_qa` / `lexeme` UI |
| Evidence 口径一致 | **核心已收敛**；规划书需去掉过时 `SourceRef` 三字段硬验收或改代码 |
| 低置信 / 无证据 / reflection | segment_qa **满足**；P4 workflow **建议**各走一轮 UI 对照 |
| 写入门禁 | **架构未变**；Adoption `accept` 路径需在代码审中保持与 `propose_changes` 一致（已有 `no-silent-write-01.json` 等） |
| §四 不做清单 | 未见远程 MCP / swarm 实现 |

---

## 10. PR 全覆盖速查表（§九）

| PR | 结论（相对当前代码） |
|----|----------------------|
| P0-1～P0-3 | **已覆盖**（含 P0-3 `tools/call` 独立表审计，2026-05-07） |
| P2a-1 | **部分**（需计数下沉模块） |
| P2a-2 | **超额**（pipeline/runner 主逻辑已在 `src/ai/chat/`，hooks 为 facade） |
| P1-1～P1-3 | **已覆盖**（验证命令漂移见 B2） |
| P3-1～P3-3 | **已覆盖**（P3-1 dogfood 数据仍属运营项） |
| P4-1～P4-3 | **代码已覆盖**，**规划 checkbox 未更新** |
| P5-1～P5-3 | **部分**（核心模块与 fixture 有；完整产品面待核） |

---

## 11. 建议的规划书修订清单（执行编辑时逐项打勾）

1. **§三 P1 验收**：删除或改写「`citation[].sourceId` / `audit.toolCall.sourceRefs` / 同一 `SourceRef`」；改为 **`evidenceSourceRefs`（工具审计 metadata）+ `EvidencePacketV0` + `UiChatMessage.citations`** 的对账关系，并引用 ADR-0030 §5。  
2. **§三 P1 PR-P1-1 验证**：改为 `npx vitest run …` / `npm run check:agent-evals` / E2E 等与实现一致。  
3. **§三 P1 reflection 验收**：与「仅失败项 + details」行为对齐。  
4. **§三 P2**：更新 **toolDecisionPipeline / agentLoopRunner** 已下沉至 `src/ai/chat/`、`useAiChat.ts` **430** 行。  
5. **§三 P4**：将已实现项改为 `[x]` 或增加「**代码已落地，产品验收待 …**」子状态。  
6. **文首状态**：`待审查` → `已复核（2026-05-07）` 或等价。  
7. **`P0-P3_coverage_matrix_and_review-2026-05-07.md`**：更正 **P1「EvidencePacket 无 sourceSet」**、**P2b 未迁移** 等过时行，避免双真源。  
8. **§三 P0 `tools/call` 审计**：与代码对齐为 **`mcp_tool_call_audits` + v48 + 失败亦写 + best-effort**（完结报告 B1 / `mcpToolCallAudit.ts`）。

---

## 12. 验证命令（本报告生成后建议跑）

```bash
npm run check:docs-governance
npm run check:agent-evals
npm run check:architecture-guard
```

（本轮写审计文件前未强制重跑；合入前应由 CI 或作者本地执行。）

---

## 13. 与首轮矩阵文档的关系

- **`P0-P3_coverage_matrix_and_review-2026-05-07.md`**：仍可作为 **P0 细项** 的详细锚点；**P1/P2 若干行已被后续代码超越或推翻**，以 **本文件 §0–§4、§11** 为准同步修订。  
- 后续若开启 **第三轮**，建议在矩阵中增加一列 **`second_round_ref`** 指向本文件锚节，避免重复叙述。
