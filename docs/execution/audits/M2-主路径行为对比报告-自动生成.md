# M2 主路径行为对比报告（自动生成）

- 生成时间：2026/4/17 10:28:51
- 执行来源：`npm run gate:m2`
- 发布判定：通过（可继续）

## 执行矩阵

| 套件 | 目标 | 测试文件数 | 用例通过 | 用例失败 | 套件通过 | 耗时 |
|---|---|---:|---:|---:|---:|---:|
| 导入导出幂等性矩阵 | 同一快照重复导入后，导出结果保持一致；异常样本不污染现有数据 | 2 | 6 | 0 | 是 | 2.01s |
| 主路径 E2E 门禁矩阵 | 覆盖转写创建 / 编辑 / 保存 / 导出的核心用户路径 | 4 | 28 | 0 | 是 | 2.96s |

## 详情

### 导入导出幂等性矩阵

- 命令：`npx vitest run src/db/importExportRoundTripIdempotency.test.ts src/db/importDatabaseFromJson.test.ts --reporter=json --outputFile /Users/junwei/Documents/百度网盘同步/Obsremote/（50）开发/Jieyu/.tmp/m2/import-export-idempotency.vitest.json`
- JSON 报告：`.tmp/m2/import-export-idempotency.vitest.json`
- 退出码：0
- 套件状态：通过

### 主路径 E2E 门禁矩阵

- 命令：`npx vitest run src/pages/useTranscriptionSegmentCreationController.test.tsx src/pages/useTranscriptionSegmentMutationController.test.tsx src/hooks/useTranscriptionPersistence.test.tsx src/hooks/useImportExport.export.test.tsx --reporter=json --outputFile /Users/junwei/Documents/百度网盘同步/Obsremote/（50）开发/Jieyu/.tmp/m2/main-path-e2e.vitest.json`
- JSON 报告：`.tmp/m2/main-path-e2e.vitest.json`
- 退出码：0
- 套件状态：通过

## 门禁规则

1. 任一套件失败即阻断发布。
2. 阻断条件与 CI 的 `m2-mainpath-gate` 作业保持一致。
3. 仅当全部套件通过时判定为 `go`。

