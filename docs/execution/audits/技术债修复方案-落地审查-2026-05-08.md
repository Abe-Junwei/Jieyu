---
title: 技术债修复方案 — 落地审查（2026-05-08）
doc_type: execution-audit
status: active
owner: repo
last_reviewed: 2026-05-08
source_of_truth: tech-debt-plan-implementation-audit
---

# 技术债修复方案 — 落地审查（2026-05-08）

本文件含 **首轮差异表（§2）** 与 **第二轮无死角补遗（§7 起）**。第二轮在相同仓库快照上补做：台账逐行对读、`§4` W1 逐项映射、`knip.json` 与例外 ledger **逐项对账**、断链 **分桶统计**、CI 对照、方案**内部自相矛盾**摘录、及其它未在首轮展开的死角。

> **2026-05-08 工程+文档跟批**：`depcheck` / `test:visual-css:write-baseline` / `test:visual-css` 已绿；[方案](../plans/技术债修复方案-2026-05-08.md)、[CODE_REVIEW](./CODE_REVIEW_REPORT_2026-05-07.md)、[knip ledger](../governance/knip-exception-ledger-2026-05-08.md)、[P0–P3 矩阵](./P0-P3_coverage_matrix_and_review-2026-05-07.md) 已按差异表修订。下文 §2–§14 中**首轮快照**与跟批后现状并存：以**粗体「已修复」**行与 §2 表内「**已修复**」状态为准。

## 1. 审查范围与方法

- **对照真源**：[技术债修复方案-2026-05-08.md](../plans/技术债修复方案-2026-05-08.md)（下称「方案」）、[未落地项汇总-2026-04-24.md](../governance/未落地项汇总-2026-04-24.md)、[knip-exception-ledger-2026-05-08.md](../governance/knip-exception-ledger-2026-05-08.md)。
- **证据等级**：`E0` 仅文档；`E1` grep/文件；`E2` 命令输出；`E3` 构建/测试。
- **执行环境（本机摘录）**：Node `v25.9.0`，npm `11.12.1`。仓库 `.nvmrc` 为 `22`；审查在 Node 25 下执行，结论以命令退出码与输出为准；发版/CI 应以 `.nvmrc`/CI 矩阵复验。

## 2. 差异总表（方案主张 vs 本审查证据）

| 方案主张 / 章节 | 证据 | 状态 | 建议动作 |
|-----------------|------|------|----------|
| §5「P0 清零」、`npm audit` high=0；含 `test:visual-css` 通过 | 首轮：`npm audit` 0 vuln（E2）；`test:visual-css` **失败**（基线含已删 CSS）。**跟批**：`test:visual-css:write-baseline` + `test:visual-css` → **通过**（E2） | **已修复** | 首轮建议已落实；方案 §5 / §4 W1 已与证据对齐 |
| 阶段 D「`npm run check:depcheck` 持续通过」 | 首轮：depcheck **255**（`@types/language-tags` / `language-subtag-registry`）。**跟批**：移除前者、声明后者为 `devDependencies`，`check:depcheck` → **通过**（E2） | **已修复** | 首轮建议已落实 |
| CODE_REVIEW / 历史口径「madge 循环 = 0」 | 首轮曾 **2** 条 `localContext` 小环（E2） | **已修复（代码+文档）** | `localContextToolTypes` / `localContextToolScopeNormalize` + config 直连；`madge` → **0**；[CODE_REVIEW](./CODE_REVIEW_REPORT_2026-05-07.md) 已回写为 **已清零** |
| 阶段 E「broken link target = 83」 | `rg -c 'broken link target'` → **0**（E2，2026-05-08 末：`scripts/normalize-docs-link-targets-once.mjs` + 路径修正） | **已修复** | 见 [技术债滚动-2026-05-08.md](../plans/技术债滚动-2026-05-08.md) §1 |
| §5「`language-mapping-runtime` 341 KB」类叙述 | `ls -l dist/assets/language-mapping-runtime-*.js` → **341 841 B**（E3）；与方案 G2「降至 341 KB」**一致** | **一致** | 维持；G3「~50 KB + 独立 chunk」仍为未达标目标，保留为开放项 |
| Source Set：删除 `toRuntime*` / `fromRuntime*`；架构文档 | 全仓 `rg`：仅 `docs/architecture/ai-chat-send-turn-pipeline.md` 与方案/归档计划提及已删除符号；`corpusSourceSet.ts` 无残留（E1）；架构文档写明 `resolveCorpusSourceSet(aiContext)`（E1） | **一致** | `AiSourceSetBar.test.tsx` 含 `excludes invalidated sets from dropdown`（E1），与方案 B3 叙述一致 |
| 行数：`localContextTools` ~149、`ReadyWorkspace` 2185、`sendTurnStreamPhase` 1130、`confirmExecution` 872 | `wc -l`（E1）；另 `localContextToolTypes` / `localContextToolScopeNormalize` 卫星文件 | **一致** | 方案 §1.2 已同步 ~149 |
| `noUnusedParameters` 全仓开启 | `tsconfig.json` 中 `"noUnusedParameters": true` 且 `npm run typecheck` 通过（E1+E2） | **一致** | 无 |
| Knip CLI 通过；`ignore` 与 ledger §1 | `npx knip --no-progress` 退出 0（E2）；`knip.json` 的 `ignore` 七条与 [knip-exception-ledger §1](../governance/knip-exception-ledger-2026-05-08.md) 表路径一致（E1） | **一致** | 维持 |
| Knip **`ignoreIssues` / `ignoreDependencies` 与 ledger §2–§3 完全对齐** | 首轮：§7.5 三文件缺 ledger 行；`language-subtag-registry` 曾仅在 `ignoreDependencies`。**跟批**：ledger §2 已补三行；`language-subtag-registry` 已改为真实 `devDependencies` 并从 `knip.json` `ignoreDependencies` 移除（E1） | **已修复** | 维持「每次改 `knip.json` 同步 ledger」纪律 |
| Ledger 声称 `knip --include-entry-exports` 通过 | `npx knip --no-progress --include-entry-exports` 退出 0、无 stderr（E2） | **一致** | 无 |
| 架构热点 WARN=0 | `npm run report:architecture-hotspots`、`check:architecture-guard:core` 均 OK，无 WARN 行（E2） | **一致** | 无 |
| CSS：unused=21、raw color=0、inline effective=0 | `check:css-unused-selectors` / `check:css-token-usage` / `check:css-debt-thresholds` 输出与方案 §1.4 / §5 一致（E2） | **一致** | 无 |
| ESLint 已引入、可运行 | `eslint.config.js` 存在；`npm run lint` 退出 0，**138** 条 `exhaustive-deps` / `no-unused-vars` **warnings**（E1+E2） | **部分** | 方案应注明「lint 不阻塞但 warn 规模大」；若 CI 未跑 `lint`，建议在 quality job 或文档中写清策略 |
| useAiChat 生产侧行数聚合 ~11940（不含 test） | `wc -l` 对 `useAiChat.ts` + `useAiChat.*.ts` 排除 `*.test.*`（E1） | **一致（口径补充）** | 方案 P1-2 若写「含测试 18438」需标明含 `useAiChat.test.tsx` 等大测试文件 |

## 3. 分阶段证据摘录

### 3.1 阶段 0 / F（供应链）

- `npm audit --audit-level=moderate`：`found 0 vulnerabilities`（E2）。
- `npm audit --omit=dev`：`found 0 vulnerabilities`（E2）。

### 3.2 阶段 D（depcheck / knip / TS）

- `npm run check:depcheck`：**通过**（跟批后复验，E2）。
- `npx knip --no-progress`：无控制台输出，退出码 0（E2）。
- `npm run typecheck`：通过（E2）。

### 3.3 阶段 C / 架构守卫

- `npm run report:architecture-hotspots`：`architecture hotspot scan completed.`（E2）。
- `npm run check:architecture-guard:core`：`OK`（E2）。
- `npm run check:architecture-guard`（全量子项串行）：**退出码 0**（第二轮补跑，E2；输出尾部为 telemetry / fire-and-forget 等子检查通过）。
- `npx vitest run src/pages/TranscriptionPage.structure.test.ts`：37 tests passed（E3，定向）。

### 3.4 阶段 H / CSS

- `check:css-unused-selectors`：`currentUnused=21`（E2）。
- `check:css-token-usage`：`raw color matches ... 0`（E2）。
- `check:css-debt-thresholds`：`inlineStyleOccurrences: current=0`；`rawTotal=11` 全 whitelist；`unusedSelectors: current=21`（E2）。
- `npm run test:visual-css`：**通过**（跟批后复验，E2）。

### 3.5 阶段 G（bundle）

- `npm run build` 成功；`language-mapping-runtime-*.js` 磁盘大小 **341 841** 字节（E3）。

### 3.6 阶段 E（文档断链）

- `broken link target` 行计数：**0**（E2，2026-05-08 末；`npm run report:docs-link-debt`）。

## 4. 对关联文档的修订建议（另开 PR）

以下三条已在 **2026-05-08 跟批** 中落实（方案、CODE_REVIEW、depcheck/visual 证据、knip ledger、P0–P3 坏链）。

1. ~~方案：visual-css / §5 / W1 对齐~~ **已修订**。
2. ~~CODE_REVIEW：`noUnusedLocals` / madge 口径~~ **已修订**。
3. ~~depcheck + 方案阶段 D 陈述~~ **已修订**。

## 5. 未执行项（范围控制）

- 全量 `npm test` / `npm run validate`：**未跑**（本审查以子集为主）。若需发布级签字，应在本机 Node 22（`.nvmrc`）上补跑并与 CI 对齐。

## 6. 结论摘要

方案中 **Source Set 收尾、行数快照、`ignore` 七条与 ledger §1、TS 严格度、多数 CSS 数值门禁、`npm audit`（moderate）、`knip` 默认模式、`architecture-guard:core`、全量 `check:architecture-guard`** 与仓库现状**一致或可通过**（证据见 §2–§3、§7）。

**首轮高优先级不一致项**（1）–（5）均在 **2026-05-08 跟批 + 末工程批** 闭合；其中 **madge** 已由代码拆边至 **0 环**（见 [CODE_REVIEW](./CODE_REVIEW_REPORT_2026-05-07.md) §1.3）。详见 **§7–§14**（含首轮快照）。

---

## 7. 第二轮：台账 ↔ 方案（逐行对读）

| 台账主题（[未落地项汇总](../governance/未落地项汇总-2026-04-24.md)） | 台账状态口径 | 方案 §1.1 / §1.2 / §5 对应主张 | 审查结论 |
| --- | --- | --- | --- |
| ARCH-7 | 已缓解 | §1.2 C：部分完成、行数一致 | **一致**（口径：台账「已缓解」↔ 方案「部分完成」可并存） |
| ARCH-3 | 热点 WARN=0 | §1.1 A1、§5；专文 [ARCH-3](../governance/arch3-architecture-guard-hotspots-2026-05-05.md) 仍写 `last_reviewed: 2026-05-05` | **部分**：事实与 guard 输出一致，但 **ARCH-3 专文 `last_reviewed` 未随 05-08 台账刷新**（E0） |
| 勘误 §3 P2/P3 | 随实现滚动 | 方案 §1.2 表末行 | **一致**（无新证据要求） |
| CSS 未使用选择器 | 已关闭 | §1.4、§5 CSS 行 | **一致**（`currentUnused=21`，E2） |
| Source Set | 已关闭 | §1.1 B2/B3、§5 | **一致**（代码与测试见首轮） |

**台账 ↔ 方案互链**：台账「权威交叉引用」含方案链接；方案 `depends_on` 含台账路径（E1）。**双向可读**：一致。

---

## 8. 第二轮：方案 §4「W1（已执行）」逐项映射证据

| §4 单元格主张 | 最低证据 | 本轮结果 |
| --- | --- | --- |
| 阶段 0：`npm audit fix` + P0 安全 | `npm audit --audit-level=moderate` | **0 vuln**（E2）；`package-lock.json` 中 `serialize-javascript` **resolved 7.0.5**（>7.0.4）（E1），与方案 P0-1「<=7.0.4」修复方向一致 |
| 阶段 0：CSS snapshot `--write-baseline` / visual-css 通过 | `npm run test:visual-css` | 首轮 **失败**（§12）；**跟批** `write-baseline` 后 → **通过**（E2） |
| B1：删悬空函数 + 架构文档 | `rg toRuntimeCorpusSourceSet` | **无**源码符号（E1）；`ai-chat-send-turn-pipeline.md` 已写 Removed（E1） |
| G2-D：删 `iso6393Seed.generated.ts` | `test -f scripts/build-language-tag-mappings.mjs`（E1） | **文件不存在**（`absent`） |
| G1：ESLint + 14 个 rules-of-hooks errors 修复 | `eslint.config.js` + `package.json` 含 `eslint` devDependency + `npm run lint` | **eslint 存在**（E1）；`lint` **0 errors / 138 warnings**（E2）；「14 个 error 已修」为**历史陈述**，本轮未复算首次引入前数量（**需补证**若要在 §4 保留精确「14」） |
| 捎带：阶段 A 台账 | 见 §7 | 台账已更新为 05-08 状态（E1） |

**结论（更新）**：首轮 **仅 visual-css** 与 W1 矛盾；跟批后 **visual-css 已绿**。其余在粗粒度上可解释；**G1 的「14」为弱证据**（历史 ESLint error 计数）。

---

## 9. 第二轮：方案正文**内部**自相矛盾（E0，仅文档逻辑）

以下为首轮在 [方案](../plans/技术债修复方案-2026-05-08.md) 中发现的**历史**自相矛盾；**2026-05-08 跟批已合并修订**（§1.2 D/E/F、P0/P1、§2 C3、§5 madge 等），下表仅作审计留痕：

| 位置（首轮） | 陈述 A | 陈述 B |
| --- | --- | --- |
| §1.2 表「D / TS」 | `noUnusedParameters` **未开启**；`depcheck` 口径混乱 | §1.4 / §5：`noUnusedParameters` **已开启**；depcheck **通过** |
| §1.2 表「F」 | `npm audit` **3 high** | §5 / 命令：`npm audit --audit-level=moderate` → **0** |
| §1.1 vs §2 C3 | 行数已 ~149 + 卫星文件 | C3 曾仍写 2988 行待拆分 |
| §1.1 P1-3 vs 行数表 | 专文历史行号 vs 当前 `wc -l` | 属演进噪声；方案 P1-3 已更新为当前行数 |

**建议（仍有效）**：大版本修订时保持 §1.2 / §1.4 / §5 **单一真源**，避免再分叉。

---

## 10. 第二轮：`knip.json` ↔ [knip-exception-ledger](../governance/knip-exception-ledger-2026-05-08.md) 逐项矩阵

### 10.1 `ignore`（7 条）

全部在 ledger §1 表中有对应行（E1）。**一致**。

### 10.2 `ignoreIssues`（`knip.json` 共 **11** 个 key）

| `knip.json` key | ledger §2 是否有行 |
| --- | --- |
| `src/ai/chat/toolCallSchemas.ts` | 有 |
| `src/annotation/analysisGraph.ts` | 有 |
| `src/annotation/structuralRuleProfile.ts` | 有 |
| `src/data/languageCatalogRuntimeCache.ts` | 有 |
| `src/data/languageNameTypes.ts` | 有 |
| `src/db/index.ts` | 有 |
| `src/utils/transcriptionUnitLaneReadScope.ts` | 有 |
| `src/workerThreads.browser.ts` | 有 |
| `src/integrations/supabase/storage.ts` | **有**（跟批补ledger） |
| `src/pages/transcriptionAcousticSummary.ts` | **有** |
| `src/pages/transcriptionTimelineWorkspacePanelTypes.ts` | **有** |

### 10.3 `ignoreDependencies`

| `knip.json` | ledger §3 |
| --- | --- |
| `country-state-city` | 有 |
| ~~`language-subtag-registry`~~ | **已不作为 ignore**：改为 `package.json` `devDependencies`（跟批） |

**结论（更新）**：`ignoreIssues` **11** key 与 ledger §2 **一致**；`ignoreDependencies` 仅 `country-state-city` 与 ledger §3 一致。

---

## 11. 第二轮：文档断链 **分桶**（`report:docs-link-debt`）

- **总断链行数**：**0**（`rg -c 'broken link target'` 于命令输出，E2，2026-05-08 末）。历史「83/85/81」为旧快照。
- **按来源 Markdown 路径分桶**（对 `broken link target` 行的**来源文件**路径计数，E2）：

| 分桶 | 近似条数 | 说明 |
| --- | ---: | --- |
| `docs/execution/archive/**` | **0**（本轮后） | 曾集中断链；已批量归一化 |
| `docs/execution/audits/**` | **0** | P0–P3 等 active 审计内坏链已修 |
| `docs/architecture/**` | **0** | 本轮样本中无「来源文件落在 architecture」的断链行（不代表 architecture 内无坏目标链接于其它报告方式） |
| `docs/execution/plans/**`（active） | **0** | 同上 |

**对阶段 E1 策略的审查结论**：「active 与 archive 分治」**仍正确**；**active 面**坏链已压至 **0**（本轮）；**archive** 仍占断链绝大多数。

---

## 12. 第二轮：`test:visual-css` 根因（基线 JSON 与磁盘不一致）

[`scripts/css-visual-baseline.json`](../../../scripts/css-visual-baseline.json) 的 `fileHashes` **仍包含**：

- `src/styles/foundation/lucide-jieyu.css`
- `src/styles/panels/grounding-context.css`

而工作区 **已无**上述文件（E1：基线 JSON 内仍有键）。`npm run test:visual-css` 报 **baseline file missing** 与 **`timeline-paired-reading.css` 内容哈希变更**（E2）。

**结论（更新）**：首轮结论如上。**跟批**已执行 `npm run test:visual-css:write-baseline` 并重跑 `test:visual-css` → **通过**；基线 JSON 已与磁盘对齐。

---

## 13. 第二轮：CI（`.github/workflows/ci.yml`）与方案阶段 G1 的对照

- `quality` job 含 **`npx knip --no-progress`**（E1），**未**包含 `npm run lint` 或 `npm run test:visual-css`（E1）。
- **含义**：方案 G1 写「`lint` 不阻塞 CI」与现状 **一致**；但若团队期望「视觉基线」防回归，**需显式增加 CI 步骤**，否则仅靠本地/发版前记忆。

---

## 14. 第二轮：其它死角（首轮未展开）

| 主题 | 证据 | 说明 |
| --- | --- | --- |
| **madge 循环** | `npx madge --circular --extensions ts,tsx src` → **0**（E2，2026-05-08 末） | 首轮 2 环已消除；CODE_REVIEW / 方案 §5 已同步 |
| **ARCH-3 专文 vs 当前体量** | `wc -l`：`SettingsModal` 1722、`LinguisticService` 1684、`AiChatCard` 1174、`LanguageMetadataAdministrativeDivisionPicker` 653（E1） | 专文「历史登记热点」表仍为 2026-05-05 快照；**建议在专文加「行数为历史，当前以 guard 输出为准」**或更新表 |
| **`src` 内 `eslint-disable` 行数** | `rg "eslint-disable" src -g'*.{ts,tsx}' \| wc -l` → **5**（E2） | 与方案「例外注释需审计」一致；量级小 |
| **`@ts-expect-error` / `@ts-ignore`** | 同上口径 `rg` → **5** 行命中（主要在测试）（E2） | 可接受；若需可附文件清单于下一轮 |
| **P3-1「DB Schema v30→v49」** | `src/db/migrations/*.ts` 文件数 **13**（Glob，E1） | 与方案「20 个版本」**口径不一致**（可能计数含非 ts 或历史分支）；标记为 **文档需复核计数口径** |
| **Node 版本** | 审查机 Node **25** vs `.nvmrc` **22** | CI 使用 22；**建议在 Node 22 上复跑** `npm test` / `depcheck` / `knip` 以排除引擎差异 |

---

## 15. 第二轮执行命令清单（便于复验）

```bash
node -v && npm -v
npm audit --audit-level=moderate
npm audit --omit=dev
npm run typecheck
npm run check:depcheck
npx knip --no-progress
npx knip --no-progress --include-entry-exports
npm run report:architecture-hotspots
npm run check:architecture-guard:core
npm run check:architecture-guard
npm run check:css-unused-selectors && npm run check:css-token-usage && npm run check:css-debt-thresholds
npm run test:visual-css
npm run report:docs-link-debt
npx madge --circular --extensions ts,tsx src
npm run lint
npx vitest run src/pages/TranscriptionPage.structure.test.ts
# 可选：npm test / npm run validate（本轮仍未跑）
```
