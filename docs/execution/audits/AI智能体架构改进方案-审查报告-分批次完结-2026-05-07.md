---
title: AI 智能体架构改进方案 — 分批次审查完结报告
doc_type: execution-audit
status: completed
owner: repo
last_reviewed: 2026-05-07
amended_note: 'B1 MCP tools/call 独立表审计落地后增补总表 §一、§二 B1、§三 P0-包、§四 条目 3（同日）'
source_of_truth: code_snapshot_review
depends_on:
  - ../plans/AI智能体架构改进方案-2026-05-06.md
  - ./AI架构改进方案-第二轮审查-全量-2026-05-07.md
---

# AI 智能体架构改进方案 — 分批次审查完结报告

本报告按既定规划 **B0～B8** 全部分批完成审查，每批给出 **结论等级**（满足 / 部分 / 未满足 / 不适用）与 **可复核证据**（路径或计数）。与《[AI智能体架构改进方案-2026-05-06.md](../plans/AI智能体架构改进方案-2026-05-06.md)》对照；与《[AI架构改进方案-第二轮审查-全量-2026-05-07.md](./AI架构改进方案-第二轮审查-全量-2026-05-07.md)》为姊妹全量摘要，**以本完结报告为分批次审查的正式收口**。

---

## 一、总表（按批次）

| 批次 | 审查范围 | 结论 | 说明 |
|------|-----------|------|------|
| **B0** | 元信息、真源链接、§八 引用 | **满足** | 战略规划文件存在；§八 矩阵链接有效；建议将方案文首 `待审查` 改为已复核状态。 |
| **B1** | P0 + PR-P0-1～3（信任边界、门面、MCP） | **满足（2026-05-07 增补）** | 门面、空 scope、auth、loopback、limit/offset、30s 超时、flag、单测 **满足**。**MCP `tools/call` 持久化审计已实现**：Dexie **v48** 独立表 **`mcp_tool_call_audits`**（**非** `audit_logs`），每条写入 **`arguments` 全量 JSON**、`runtimeContextJson`（`JSON.stringify(McpServerRuntimeContext)`，可复核宿主 scope）、outcome（含 **validation_error / tool_not_found / success / timeout / execution_error**）；**失败路径同样 best-effort 落库**（插入失败只打日志，不阻断 MCP）。成功时 `toolResult` JSON 超长截断。详见 `src/ai/mcp/server/mcpToolCallAudit.ts`、`McpServer.ts`。应用侧另有 `ai_segment_qa_reflection` 等审计（见 B8）。 |
| **B2** | P1 + PR-P1-1～3 | **满足（文档需修订）** | EvidencePacket 绑定、`sourceScopeSummary`、低置信/无证据/reflection UI、`evidenceSourceRefs`、10×segment_qa JSON、suite、E2E **满足**。规划书 **P1 验收** 中 `citation[].sourceId` / `audit.toolCall.sourceRefs` / 单一 `SourceRef` **与代码不符** → 修订文档，非阻塞功能。 |
| **B3** | P2 + PR-P2a/b | **满足** | `useAiChat.ts` **430** 行（`wc -l`）；guard `useAiChat` **maxLines 1100**；`VoiceAgentService.ts` **938** 行（&lt;950）。`useAiChat.toolDecisionPipeline.ts` / `useAiChat.agentLoopRunner.ts` 为 **薄 re-export**，主逻辑在 `src/ai/chat/toolDecisionPipeline.ts`、`agentLoopRunner.ts`。P2a「再下沉 4～5 个」：**已远超** — `src/ai/chat/` 内与编排/会话/工具相关的非测试模块 **≥15**（如 `applyAssistantMessageResult`、`backgroundMemoryRuntimeFactory`、`coordinationAuditLog`、`coordinationPhase`、`createIdleTaskSession`、`metricBump`、`settingsConnectionReset`、`sendTurnPersistRecoveryUi`、`useAiChatPureHelpers` 等）；方案可改为「已达标并改为防回潮 ratchet」。 |
| **B4** | P3 + PR-P3-1～3 | **满足（运营项除外）** | JudgeProvider、judges、ADR-0022、`auditExportAdapter`、`skipTaxonomy`/`skipTaxonomyRollup`、`aiRuntimeReport`、`workflowExplainability`、`evalCandidateBackfill` 均存在。dogfood 缺陷 skip **清零**为 **目标指标**（不适用代码「满足/不满足」二值）。 |
| **B5** | P4 + PR-P4-1～3 | **满足（文档 checkbox 未同步）** | `annotationQaReflection` / `lexemeCandidatesReflection`、`adoptionQueue`、`AiAdoptionQueuePanel`、`sendTurnStreamPhase` 内 annotation/lexeme **push Adoption**、suite 中 reflections/adoption cases **满足**。§三 P4 **验收表仍 `[ ]`** → **文档未同步**。 |
| **B6** | P5 + PR-P5-1～3 | **部分** | `corpusSourceSet.ts` 含完整生命周期纯函数与注释；`AiChatCard.tsx` + `AiSourceSetBar.tsx` 提供 **create / switch / project scope** 等 UI；`elanFlexCompatibilityWorkflow` + fixture JSON **存在**。**未**在本轮做 E2E/手工跑通「rename/bind/invalidate/delete/export_reference_summary」全按钮路径；**round-trip golden** 未验证 → **部分**。 |
| **B7** | §六 成功指标、§九.7 里程碑、§十一 Top5 | **不适用 / 文档级** | 仓库 **无** `milestone M1…` 类 git 标签可对齐 §九.7；里程碑仅作计划节奏。**Top5 优先级**与代码无冲突结论。成功指标表中定量项已在 B1–B6 分散核对。 |
| **B8** | §十 UX 契约 + §四 边界 | **满足** | `AiChatAssistantMessage` 对 **sourceScopeSummary / 低置信 / 无证据 / reflection / workflowExplainability（sr-only）** 为 **工作流无关渲染**，数据来自 `UiChatMessage`。`useAiChat.sendTurnStreamPhase.ts` 对 **segment_qa、annotation_qa、lexeme_candidates、elan_flex_compatibility** 均 **run*Reflection** 并写 **audit_logs**（`ai_*_reflection`）且向消息写入 `reflectionChecks` — **四 workflow 与 §10.1「reflection / explainability」一致**。§四 不做项未见远程 MCP/swarm 实现。 |

**整体验收口**：**P0～P4 工程主线按规划已大体完成**；**缺口集中在（1）规划书与代码 checkbox/措辞（2）P5 全路径 UI+E2E 证明）**。**MCP `tools/call` 专用持久化审计（独立表）已于 2026-05-07 合入，B1 该项关闭。**

---

## 二、分批次详细记录

### B0 — 元信息与引用

- **战略规划**：`docs/execution/plans/AI智能体-战略规划与下一步-2026-05-07.md` 存在。
- **首轮矩阵**：`docs/execution/audits/P0-P3_coverage_matrix_and_review-2026-05-07.md` 存在；其中 **P1 EvidencePacket、P2b 迁移** 等行 **已被后续代码超越**，应以本报告 + 代码为准修订该矩阵（见第七节）。

### B1 — P0 / MCP（含审计链深挖）

| 子项 | 结论 | 证据 |
|------|------|------|
| `segmentReadQueries` + 测试 | 满足 | `src/ai/queries/segmentReadQueries.ts`、`.test.ts` |
| MCP handlers + `SEGMENT_READ_SCOPE_REQUIRED` | 满足 | `src/ai/mcp/server/tools.ts`、`tools.test.ts` |
| `tools/call` **独立持久化审计**（宿主 scope 可复核 + 全量 arguments） | **满足** | `src/ai/mcp/server/mcpToolCallAudit.ts`（`persistMcpToolCallAudit`）；`McpServer.ts` 在 `routeMethod` 内各 outcome **await** 审计；Dexie **`mcp_tool_call_audits`**（`src/db/engine.ts` **v48**）；`src/db/engine.mcpToolCallAudits.test.ts`、`McpServer.test.ts`（mock 审计） |
| 与规划原文「context 摘要」 | **对齐说明** | 独立表字段 **`runtimeContextJson`** = `McpServerRuntimeContext` 全量 JSON，用于事后核对「当时绑定的 project/media/layer/text」；**不**写入 `audit_logs`，与聊天侧工具决策审计分离。 |

### B2 — P1 / segment_qa

- **EvidencePacket**：`sourceSetId`、`sourceSetSnapshot` 在 `evidencePacket.ts`；`evidencePacket.test.ts`。
- **审计同源字符串**：`evidenceSourceRefs` 于 `toolCallHelpers.ts` metadata；`evidenceSourceRef.ts`。
- **segment_qa cases**：`scripts/agent-evals/cases/workflow-segment-qa-*.json` **10** 文件。
- **E2E**：`tests/e2e/segmentQaEvidenceJump.spec.ts`。
- **suite**：`scripts/agent-evals/suite.v1.json` 含 semantic / evidence / assistant-message cases。

### B3 — P2 / Hook 减压

- **行数**：`wc -l src/hooks/useAiChat.ts` → **430**；`VoiceAgentService.ts` → **938**。
- **guard**：`scripts/architecture-guard.config.mjs` `hookRule('useAiChat', { maxLines: 1100 })`。
- **整文件下沉**：`useAiChat.toolDecisionPipeline.ts`（12 行）、`useAiChat.agentLoopRunner.ts`（11 行）仅 re-export；实现于 `src/ai/chat/toolDecisionPipeline.ts`、`agentLoopRunner.ts`。
- **P2a 计数**：在 `src/ai/chat/` 中除测试外，与「从 hook 抽出编排/会话/工具」直接相关的模块 **远多于 4～5 个**（列举见总表 B3）。

### B4 — P3

- 与首轮矩阵一致；**bundle** `skipTaxonomyRollup` / 节点 `skipTaxonomy`：`scripts/generate-release-evidence-bundle.mjs`。

### B5 — P4

- **Reflection**：`segmentQaReflection.ts`、`annotationQaReflection.ts`、`lexemeCandidatesReflection.ts`；测试进 `vertical-workflow-reflections` case。
- **Adoption**：`adoptionQueue.ts`、`AiAdoptionQueuePanel.tsx`、`adoptionQueue.test.ts`；`sendTurnStreamPhase.ts` **L851+** `annotation_qa` / `lexeme_candidates` push；`elan_flex_compatibility` push **L808+**。
- **文档**：方案 §三 P4 **验收 checkbox 仍为未勾**。

### B6 — P5

- **域逻辑**：`corpusSourceSet.ts`（create/rename/bind/switch/invalidate/delete/export 等在注释与导出函数中声明）。
- **UI**：`AiChatCard.tsx`（`handleCreateSourceSet`、`handleSelectSourceSet`、`savedSourceSets`、`activeSourceSetId`）；`AiSourceSetBar.tsx`（下拉、创建按钮）。
- **workflow**：`elanFlexCompatibilityWorkflow.ts`；cases：`workflow-elan-flex-compatibility-01.json`、`compatibility-*.json`。
- **缺口**：未跑通 **全生命周期 UI** 与 **round-trip** E2E → 报告 **部分**。

### B7 — 里程碑与成功指标表

- **Git 标签**：`git tag -l` 无与 §九.7 一一对应的里程碑标签（本环境快照）。
- **处理**：里程碑保留为 **计划层**；不在本技术审查中判「满足/不满足」。

### B8 — §十 UX 契约 / 多 workflow

- **统一渲染面**：`AiChatAssistantMessage.tsx` 使用通用 `UiChatMessage` 字段，**不**按 `workflowId` 分支 UI。
- **统一数据面**：`useAiChat.sendTurnStreamPhase.ts` **L658–715** 对 **四种** `workflowId` 调用对应 `run*Reflection` 并 **insert audit_logs**；**L788–791** 将 `reflectionChecks` 写入助手消息。
- **结论**：§10.1 中与 **reflection / source scope / explainability（sr-only）** 相关的契约，在 **数据生产侧已对齐多 workflow**；**§10.1 全文六条** 中「候选 accept/ignore」等需在 **AdoptionQueue 产品验收** 中单列（B5 已证代码存在）。

---

## 三、PR 包（§九）完结核对

| PR 包 | 完结判定 | 备注 |
|-------|-----------|------|
| P0-1～P0-3 | **已达成** | B1：含 MCP `tools/call` 独立表审计（2026-05-07） |
| P2a-1、P2a-2 | **已达成** | B3 |
| P1-1～P1-3 | **已达成** | B2；PR 验证命令建议改文档 |
| P3-1～P3-3 | **已达成（数据飞轮运营除外）** | B4 |
| P4-1～P4-3 | **已达成（文档未勾选）** | B5 |
| P5-1～P5-3 | **部分** | B6 |

---

## 四、必须在规划书中修订的条目（执行清单）

1. **§三 P1 验收**：删除或改写 **`citation[].sourceId`**、**`audit.toolCall.sourceRefs`**、**同一 `SourceRef`**；改为 **`evidenceSourceRefs` + `EvidencePacketV0` + `citations`**，并引用 ADR-0030。  
2. **§三 P1**：reflection 验收与 **「仅失败项 + details」** 行为对齐。  
3. **§三 P0 验收**：**MCP `tools/call` 持久化审计** — **已实现（a）**：Dexie **`mcp_tool_call_audits`** + `mcpToolCallAudit.ts` / `McpServer.ts`；规划书验收表与行动项 3 字面应同步为「独立表 + 全量 arguments + runtimeContext JSON + 失败也写 + best-effort」。  
4. **§三 P2**：写明 **toolDecisionPipeline / agentLoopRunner** 已在 `src/ai/chat/`；**P2a 数量目标已超额**。  
5. **§三 P4**：将已实现项改为 **`[x]`** 或增加子状态「代码已合入，待产品验收 …」。  
6. **文首状态**：`待审查` → **已审查（日期）**。  
7. **`P0-P3_coverage_matrix_and_review-2026-05-07.md`**：同步 **P1 EvidencePacket、P2b、reflection UI** 等过时行，避免双真源。

---

## 五、建议的验证命令（报告归档用）

```bash
npm run check:docs-governance
npm run check:agent-evals
npm run check:architecture-guard
```

---

## 六、文档关系

| 文件 | 角色 |
|------|------|
| 本文件 | **分批次审查正式完结报告** |
| [AI架构改进方案-第二轮审查-全量-2026-05-07.md](./AI架构改进方案-第二轮审查-全量-2026-05-07.md) | 同轮次全量摘要，可与本报告交叉阅读 |
| [P0-P3_coverage_matrix_and_review-2026-05-07.md](./P0-P3_coverage_matrix_and_review-2026-05-07.md) | 首轮矩阵；**需按第四节修订** |
