---
title: 多宿主译文层（方案 A）对照审计报告
doc_type: execution-audit-report
status: snapshot
owner: repo
last_reviewed: 2026-04-20
source_of_truth: code-vs-plan-gap
plan_ref: docs/execution/plans/多宿主译文层-方案A-执行方案-2026-04-20.md
code_anchor_commit: d2ecae1d57d28e6d043424f53dde737ef8e51266
code_anchor_note: 工作区存在未提交变更；以提交 hash 为基线、以实际文件为准复核。
---

# 多宿主译文层（方案 A）对照审计报告

**审计基准**：[`docs/execution/plans/多宿主译文层-方案A-执行方案-2026-04-20.md`](../plans/多宿主译文层-方案A-执行方案-2026-04-20.md)（含 §0.1 无 legacy 硬单源、§1.6 `parent*` 按 `layerType` 划界、§1.8–§1.10 执行补强、§2.0 脚本化门禁、G15/G16 协议层、批次 A0/A/B/C、§4.1 测试纪律、§4.4-13 至 §4.4-16）。  
**代码锚点**：`d2ecae1d57d28e6d043424f53dde737ef8e51266`（当时 `git rev-parse HEAD`；若已 rebase 请以本段为准替换）。  
**代码快照**：本报告撰写时对 `src/` 的静态核对（未宣称运行期全覆盖）。  
**总论**：实现仍明显处于方案所述的 **「规范领先于实现」+ parent/link 双轨** 阶段；**硬切完成条件（译文宿主 link-only、链上宿主 id 化、协议层与 DB 同构）尚未达成**。以下按方案章节与门禁 ID 对照列差距。

---

## 1. 阶段零与门禁（§2.0、G11、T20）

| 要求 | 现状 | 结论 |
| --- | --- | --- |
| `scripts/check-translation-host-link-ssot.mjs` 及 `package.json` 中 `check:translation-host-link-ssot` / `:changed` | 仓库内 **未找到** 该脚本名；`package.json` 无对应 npm script | **未落地**（G11/T20 阻塞） |
| CI 禁止新增「以 `translation.parentLayerId` 判定译文宿主」 | 无自动化守卫证据；大量既有读点仍存在（见 §3） | **待落地** |

---

## 2. 数据层与链上标识（§3、G14、G15）

| 要求 | 现状 | 结论 |
| --- | --- | --- |
| `LayerLinkDocType` 宿主端统一为 **转写层 id**；字段名建议 `hostTranscriptionLayerId` 等 | [`src/db/types.ts`](../../../src/db/types.ts)、[`schemas`](../../../src/db/schemas.ts)、[`engine`](../../../src/db/engine.ts) 仍为 **`transcriptionLayerKey`** 索引与字段语义 | **未达成 G15**；与方案 §1.5、§4.4-4 一致判定为技术债 |
| 从 **`translation`** 持久化移除宿主用 `parentLayerId`/`parentLayerIds` | `LayerDocType` 仍含通用 `parentLayerId`；业务与约束仍依赖译文 parent | **未达成 G14** |
| **`transcription.parentLayerId`** 层树语义保留 | `LayerOrderingService` 等仍用 `parentLayerId` 归类 bundle | **与 §1.6 一致（应保留）**；勿误删 |

---

## 3. 读路径与排序（§2.2、G9a、G9b）

| 区域 | 方案要求摘要 | 现状摘要 | 门禁 |
| --- | --- | --- | --- |
| [`translationLayerTargetResolver.ts`](../../../src/utils/translationLayerTargetResolver.ts) | host 命中走 **link 图** | `resolveHostAwareTranslationLayerId` 仍以 `layer.parentLayerId === hostLayerId` 匹配宿主（约 L60–L61） | **G9a 未达成** |
| [`TranscriptionTimelineVerticalView.tsx`](../../../src/components/TranscriptionTimelineVerticalView.tsx) | 宿主集合与 source **交集** | 仍存在单条 `tl.parentLayerId` 等逻辑（如约 L285、L346） | **G9a 未达成** |
| [`useLayerSegments.ts`](../../../src/hooks/useLayerSegments.ts) | preferred 与 link 一致 | 仍读 `layer.parentLayerId` 解析父层（约 L62–L65） | **G9a 未达成** |
| [`useVoiceInteraction.ts`](../../../src/hooks/useVoiceInteraction.ts) | 不经 `translation.parentLayerId` 间接命中 | 仍调用 `resolveHostAwareTranslationLayerIdFromSnapshot`，进而依赖上述 resolver | **G9a 连带未达成** |
| [`LayerOrderingService.ts`](../../../src/services/LayerOrderingService.ts) | 译文在 bundle 中靠 **preferred link**；**不**读 `translation.parentLayerId` | `buildLayerBundles` 仍将译文挂入 bundle 时依赖 **`layer.parentLayerId`**（译文，`translationDependents` 路径，约 L204–L207） | **G9b 未达成**（与方案 §2.2 明示冲突） |
| [`LayerConstraintService.ts`](../../../src/services/LayerConstraintService.ts) | 译文宿主边 **link 图**；可保留转写树 parent | 多处仍以 **`translation` + `parentLayerId`** 表达依赖（如约 L511–L552 注释与实现） | **G9b 未达成** |

---

## 4. 写路径与同步（§2.1、G12、G6）

| 要求 | 现状 | 结论 |
| --- | --- | --- |
| 删除或收窄 **`syncTranslationParentLinks`**，禁止 parent 触发 | [`useTranscriptionLayerActions.ts`](../../../src/hooks/useTranscriptionLayerActions.ts) 仍定义并在持久化路径中调用（约 L80、L632） | **G12 未达成** |
| `toggleLayerLink` / 创建译文：**只**写 link，不写译文 `parent*` | 需与 `syncTranslationParentLinks` 联读做事务审计；当前结构仍指向双轨 | **待代码审查与 G16 同批收紧** |

---

## 5. AI 与协议层（§0.1-6、§2.4、G7、G16）

| 区域 | 方案要求 | 现状 | 结论 |
| --- | --- | --- | --- |
| [`useAiToolCallHandler.layerAdapter.ts`](../../../src/hooks/useAiToolCallHandler.layerAdapter.ts) | link/unlink 以 **link 图**判定；禁止 `trl.parentLayerId === trc.id` 判「已链接」 | `link_translation_layer` / `unlink` 仍以 **`trlLayer.parentLayerId === trcLayer.id`**（约 L220–L221） | **G7/G16 未达成** |
| [`toolCallSchemas.ts`](../../../src/ai/chat/toolCallSchemas.ts) | `transcriptionLayerId` 主轴；key 仅入口兼容 | schema 仍允许 **`transcriptionLayerId` 或 `transcriptionLayerKey` 二选一** 满足校验（约 L134–L158） | **部分符合入口兼容**；**未**做到「核心写路径仅 id」（G16） |
| [`useTranscriptionCloudSyncActions.ts`](../../../src/hooks/useTranscriptionCloudSyncActions.ts) | `upsert_relation` 实体键与 payload **host id 同构** | `toggleLayerLink` 仍以 **`transcriptionLayerKey`** 为主参数；`entityId` 形如 **`${transcriptionLayerKey}:${layerId}`**（约 L1154–L1171） | **G16 未达成**；与 §4.4-13「DB id 化、协议仍 key 化」风险一致 |

---

## 6. 辅助模块与 UI（G13、§2.3）

| 项 | 现状 | 结论 |
| --- | --- | --- |
| [`layerParentIds.ts`](../../../src/utils/layerParentIds.ts) | 方案要求删除或去译文宿主语义（G13） | 文件仍存在（未在本次逐行审计是否已收窄） | **G13 待关闭**：建议阶段零产出「仍引用该工具」清单 |
| `LayerCreateInput` 使用 `hostTranscriptionLayerIds` 等 | [`transcriptionTypes.ts`](../../../src/hooks/transcriptionTypes.ts) 未见 `hostTranscriptionLayerIds` 等字段（grep） | **§2.3 未落地** |
| [`LayerActionPopover.tsx`](../../../src/components/LayerActionPopover.tsx) | 新建/选项仍向 `createLayer` 传入 `parentLayerId`（约 L303–L472 一带） | **§2.3 未完成**：仍为单宿主式 parent 写入，未见多选宿主 / `toggleLayerLink` 主导流 |
| [`SidePaneSidebar.tsx`](../../../src/components/SidePaneSidebar.tsx) | `toggleLayerLink: (transcriptionKey: string, translationId: string)`；`createLayer` 输入仍透传 `parentLayerId`（约 L37、L430） | **§2.3 + G16 未落地**：API 仍为 **key** 语义，与方案 §2.3、G16 不一致 |
| [`useLayerActionPanel.ts`](../../../src/hooks/useLayerActionPanel.ts) | 类型仍含可选 `parentLayerId`（约 L15） | **配合 SidePane 仍为旧签名** |

---

## 7. 测试基线（批次 A0、G17、§4.4-14、§4.4-15）

| 项 | 现状 | 结论 |
| --- | --- | --- |
| [`translationLayerTargetResolver.test.ts`](../../../src/utils/translationLayerTargetResolver.test.ts) | 用例数据与断言仍围绕 **`parentLayerId`** 宿主命中 | **G17 未启动**：与方案「先迁测试基线再硬切」相反，当前测试 **固化 parent 语义** |
| 方案 §8 所列 T12–T20 | 未做全量核对 | 推断多数 **尚未实现或未挂 CI** |

---

## 8. 与方案交付顺序的对齐建议（仅复述 §9 并标注当前阻塞）

1. **批次 A0（G11 + G17）**：当前 **脚本缺失 + 测试仍 parent**，应优先，否则后续 PR 噪声大。  
2. **批次 A（G15 + G16 + G12 + G14 + 约束/事务）**：数据链路与云协同、AI 仍 **key / parent** 主轴，属 **硬切主干阻塞**。  
3. **批次 B（G9a → G9b）**：读面与排序仍大量 parent，应在 A 之后或与其严格分批以免半套 SSOT。  
4. **批次 C**：G5/G7/G8/G13 及互操作 polish。

---

## 9. 审计局限性

- 未运行全量 `vitest` 或 E2E；未枚举全仓每一处 `translation` + `parentLayerId`（建议由 §2.0 盘点脚本补齐）。  
- `layerParentIds.ts`、导入导出、EAF、Tier 第二宿主等仅按方案优先级抽样；深度可另开专题审计。

---

## 10. 与方案文档的同期增补（本次已写入计划）

以下条目在原审计之后已并入执行方案，供对照时**不遗漏验收口径**：

- **§1.8**：读路径统一只读适配器（避免 G9a/G9b 重复实现 link 过滤）。  
- **§1.9**：G15/G16 **同一发布边界**锁步；云旧客户端 / 旧 `upsert_relation` 键形态**不保证**回放。  
- **§1.10**：`LayerOrderingService` 须注入 `layerLinks` 或预计算索引；Vitest **禁止新增**纯 `translation.parent*` 宿主断言（§4.1 复述）。  
- **§4.4-15 / §4.4-16**：无 link 非法态的 UI 可见性；协议版本与发布说明引用 §1.9。

若本报告与后续代码不一致，以 **`docs/execution/plans/多宿主译文层-方案A-执行方案-2026-04-20.md`** 与 **`src/`** 为准更新本快照。

---

## 附录 A. 粗量扫描（非门禁）

- `rg -l 'layerType === \\'translation\\'|layerType: \\'translation\\'' src --glob '*.ts' --glob '*.tsx'`：**74** 个文件（含测试）；**不等于**均为译文宿主 parent 读点，仅说明译文逻辑分散，阶段零全盘清单仍有必要。
