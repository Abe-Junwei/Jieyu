# M1 行为快照对比样例（DB 内核基线）

适用模板：docs/execution/templates/行为快照对比模板.md

## 1. 变更信息

- 变更主题：DB 模块拆分后保持行为一致
- 关联需求 / 缺陷：M1 内核基线封账
- 变更范围（文件）：src/db/types.ts、src/db/schemas.ts、src/db/adapter.ts、src/db/engine.ts、src/db/io.ts、src/db/index.ts
- 执行人：Copilot（GPT-5.3-Codex）
- 日期：2026-04-12

## 2. 对比目标

- 目标行为：导入、迁移修复、核心文档校验在拆分前后行为一致
- 非目标行为：UI 展示层和非 DB 领域逻辑
- 影响用户路径：数据导入、项目迁移、转写数据读取

## 3. 输入快照（Before 与 After 完全一致）

### 3.1 数据输入
- 数据文件/构造脚本：`src/db/migrationReplayBaseline.test.ts` 固定 fixture
- 固定时间种子：2026-04-12T00:00:00.000Z
- 固定随机种子：无随机输入

### 3.2 交互输入
- 操作步骤：
1. 执行不变量矩阵与迁移回放测试。
2. 执行导入与迁移补丁测试。
3. 执行 TypeScript 全量类型检查。

## 4. 输出快照

### 4.1 结构化输出
- Before 输出：基线用例全部通过（作为历史验收基线）
- After 输出：
  - `src/db/migrationReplayBaseline.test.ts`：2/2 通过
  - `src/db/schemaInvariants.test.ts`：30/30 通过
  - `src/db/importDatabaseFromJson.test.ts`：3/3 通过
  - `src/db/segmentationV2Migration.test.ts`：6/6 通过
- 对比方式（JSON Diff / Snapshot）：固定 fixture + 测试断言
- 差异结论：未发现行为回归

### 4.2 用户可见输出
- Before 可见结果：导入与迁移链路稳定
- After 可见结果：导入与迁移链路稳定
- 差异结论：未观察到用户可见行为差异

## 5. 断言清单

1. 功能结果一致：通过
2. 关键字段一致：通过
3. 错误处理一致：通过
4. 性能阈值未退化：本轮未引入退化信号

## 6. 证据

- 命令与结果：
  1. `npx vitest run src/db/schemaInvariants.test.ts src/db/migrationReplayBaseline.test.ts src/db/importDatabaseFromJson.test.ts src/db/segmentationV2Migration.test.ts` -> `Test Files 4 passed, Tests 41 passed`
  2. `npx tsc --noEmit` -> 无输出（通过）
- 关键测试文件：
  1. src/db/schemaInvariants.test.ts
  2. src/db/migrationReplayBaseline.test.ts
  3. src/db/importDatabaseFromJson.test.ts
  4. src/db/segmentationV2Migration.test.ts
- 构建产物/日志：Vitest 与 tsc 终端输出

## 7. 发布判定

- 判定：通过
- 阻断级别：P2（无阻断）
- 结论说明：M1 范围内，数据内核链路具备发布条件。

## 8. 回滚预案

- 回滚触发条件：出现迁移结果不一致、导入结构破坏、核心集合校验失败
- 回滚目标版本：回滚到 DB 拆分前稳定标签
- 回滚验证步骤：
  1. 恢复稳定版本后重新运行四个 DB 基线测试。
  2. 再次执行 `npx tsc --noEmit`。
  3. 抽检导入与迁移主流程。