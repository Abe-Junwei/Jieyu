---
title: 解语主路线图代码核对（2026-09-11）
doc_type: audit
status: active
owner: repo
last_reviewed: 2026-09-11
source_of_truth: roadmap-code-audit-2026-09-11
---

# 解语主路线图代码核对（2026-09-11）

> **范围**：对照 `origin/main` (`b2a78575`，含 B1 #164 + B2 #163) 审查 [主路线图](../plans/解语-主路线图-master-roadmap-2026-06-01.md)。不排 B4 新产品、不改 flag 默认值。
> **方法**：读 `src/App.tsx`、`featureFlags.ts`、LinguisticService emit、标注/语料/词典页、声学 inspector、architecture-guard 规则、开放 PR #154。

## 1. 总判

主路线图作为 **优先级框架** 仍可用；作为 **「现在做什么」** 已被 2026-06-01 校准块与 ChatWindow 766/800 门槛带偏。Stage B 占位页在 `main` 上已经不是空白：B4a/B4b、B5a/B5b 代码在 flag 默认 `false` 后完整；B1/B2/B3/B6/B8/B10 已合入。

## 2. 与代码冲突的陈述

| 原文（过时） | 代码事实 |
| --- | --- |
| 2026-06-01：剩余工作是「开放 B4/B5 占位、B6 UI、可选 B2」 | B4a-1/a-2/B4b、B5a/B5b、B6、B2 均已进 `main`（B4/B5 flag 关） |
| B1「部分✓」、B3「待回归」 | B1 深链+滚动 ✅；B3 回归已补（2026-09-04） |
| ChatWindow.tsx 766/800＝96%，B4/B5 须先拆 | `TranscriptionPage.ChatWindow.tsx` **127/220**；B4/B5 已用独立 controller |
| B7 blocked on A7/A9/A12 | A6 ✅；A7/A9/A12 **代码已进 main**（A9/A11 flag 关）。B7 真正缺口是 `corpusBridgeAdapter` **不存在** + ChatWindow **会话隔离**（不是行数） |
| 「B5c 已在其它分支完成、待合入」 | [#154](https://github.com/Abe-Junwei/Jieyu/pull/154) 仍 OPEN，相对 `main` **CONFLICTING**；`main` 无 `corpusWorksetBundle` / HTML MIME |
| A2/A3 ⬜ 未开始 | A2 统一 `WaveformReadoutCard` 已有；A3 会话内 pin + live-vs-pinned 比较已有，**未** Dexie 持久化 |

## 3. Flag 默认值（`src/ai/config/featureFlags.ts`）

全部仍为 `?? false`：`annotationPageEnabled`、`corpusLibraryPageEnabled`、`lexiconAttachmentsEnabled`、`aiSemanticGuardEnabled`、`aiAgentUiPreviewEnabled`、`aiExternalMcp*`、`aiAgentLoopEffortScalingEnabled`。

## 4. 剩余可执行下一刀（建议序）

1. **B5c**：rebase #154 再合入（语料 P1 HTML/bundle；不接 ChatWindow）。
2. **B4/B5/B8 flag dogfood**（默认仍 `false`；自用一周后再谈切默认）。
3. **A1** ReadyWorkspace 参数簇仍大量 `any`（如 `readyWorkspaceTrackEditControllersParams.ts`）。
4. **A3 余量**：pin 写入 Dexie → reload → readback；多冻结点（当前仅 1 个 pin）。
5. **C3a/b/c** 字幕 / 表格 / IGT LaTeX（转写导出菜单仍无 SRT/VTT/CSV 语段表）。
6. **B7**：等产品明确要语料 AI 再开；禁止往 ChatWindow 堆 corpus 写路径。

刻意不排：EAF/TextGrid 新管线、C3d Word、二次自动分词 / note/tag（标注 M1 余量，非本轮 B4 切片）。
