# M2 执行记录（2026-04-12）

里程碑：M2（W3-W4）  
目标：内核治理可执行（导入导出幂等测试 + 主路径 E2E 门禁 + 自动行为对比报告）

## 1. 已完成交付物

1. 导入导出幂等测试矩阵
- 新增：`src/db/importExportRoundTripIdempotency.test.ts`
- 复用：`src/db/importDatabaseFromJson.test.ts`
- 覆盖点：
  - 同一快照重复导入后导出结果一致。
  - 无效快照重复导入不会污染既有数据。

2. 主路径 E2E 门禁
- 新增脚本：`scripts/generate-m2-behavior-report.mjs`
- 新增 npm 命令：
  - `test:m2-import-export-idempotency`
  - `test:m2-mainpath-e2e`
  - `report:m2-behavior-compare`
  - `gate:m2`
- CI 接入：`.github/workflows/ci.yml` 新增 `m2-mainpath-gate` 作业。

3. 自动行为对比报告
- 报告文件：`docs/execution/audits/M2-主路径行为对比报告-自动生成.md`
- 原始报告：`.tmp/m2/import-export-idempotency.vitest.json`、`.tmp/m2/main-path-e2e.vitest.json`

4. 门禁清单文档
- 新增：`docs/execution/release-gates/M2-主路径E2E门禁清单-2026-04-12.md`

## 2. 本次验收命令

1. `npm run gate:m2`

## 3. 量化结果（本次）

1. 导入导出幂等一致率
- 套件：导入导出幂等性矩阵
- 结果：5 通过 / 0 失败（100%）

2. 主路径 E2E 成功率
- 套件：主路径 E2E 门禁矩阵
- 结果：27 通过 / 0 失败（100%）

3. 发布判定
- 结果：通过（可继续）
- 证据：`docs/execution/audits/M2-主路径行为对比报告-自动生成.md`

## 4. 结论

1. M2 关键交付项已可执行并接入 CI 阻断链路。
2. 当前门禁失败会直接阻断发布，不再依赖人工口头判定。
3. 可进入 M3（领域边界建模）阶段。
