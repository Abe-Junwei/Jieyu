# M6 执行记录（2026-04-12）

里程碑：M6（W11-W12）  
目标：发布门禁化（SLO 阈值 + 分级门禁 + go/no-go 自动判定）

## 1. 已完成交付物

1. 发布门禁判定脚本
- `scripts/evaluate-m6-release-gate.mjs`
- 能力：
  - enforce/shadow 双模式。
  - P0/P1/P2 分级判定。
  - 自动生成 go/no-go 报告。

2. 发布门禁命令链路
- `package.json` 新增：
  - `report:m6-release-gate`
  - `report:m6-release-gate:shadow`
  - `gate:m6-release`
  - `gate:m6-release:shadow`
- 默认强制链路：`gate:m6-release = gate:m5-observability + report:m6-release-gate`

3. 门禁文档
- `docs/execution/release-gates/M6-发布门禁清单-2026-04-12.md`

## 2. SLO 首版阈值（落地）

1. build.success_rate = 100%
2. `business.e2e.main_path_success_rate.p95 >= 0.99`
3. `ai.chat.first_token_latency_ms.p95 <= 8000ms`
4. `business.transcription.segment_action_latency_ms.p95 <= 300ms`

## 3. 自动报告产物

1. 发布门禁判定报告：`docs/execution/release-gates/M6-发布门禁判定报告-自动生成.md`
2. 观测事件流水：`docs/execution/audits/M5-观测指标事件流水-v1.ndjson`
3. 趋势报告：`docs/execution/audits/M5-观测趋势报告-v1-2026-04-12.md`

## 4. 本次实跑验收

1. `npm run report:m6-release-gate`
- 结果：通过
- 自动结论：`go-with-gray`

2. `npm run gate:m6-release`
- 结果：通过（包含 `gate:m5-observability` + M6 enforce 判定）
- 自动结论：`go-with-gray`

3. `npm run gate:m6-release:shadow`
- 结果：通过（shadow 告警模式）
- 自动结论：`go-with-gray`

## 5. 当前状态

1. M6 门禁已落地并实跑通过：enforce/shadow 均可执行。
2. 当前主路径成功率可自动入流水并参与判定（最新样本 p95=1.0000，达到阈值 >= 0.99）。
3. 当前 AI 首包与转写操作在本地样本仍偏少，按规则归入 P1 观察项（不触发 P0 阻断）。
