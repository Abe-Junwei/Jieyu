---
title: 技术债修复方案（2026-05-08 审查修订版）
doc_type: execution-plan
status: superseded
owner: repo
last_reviewed: 2026-05-08
closed_at: 2026-05-08
source_of_truth: tech-debt-remediation
superseded_by: ../../architecture/code-governance-plan-2026-05-06.md
depends_on:
  - ../governance/未落地项汇总-2026-04-24.md
  - ../governance/arch3-architecture-guard-hotspots-2026-05-05.md
  - ../audits/CODE_REVIEW_REPORT_2026-05-07.md
  - ../../remediation-plan-2026-04-24.md
  - ./AI智能体-垂直产品化架构与落地方案-2026-05-05.md
---

# 技术债修复方案（2026-05-08 审查修订版）

> **状态**：本文 **`status: superseded`**。后续滚动排期与开放项见《[**代码治理计划**](../../architecture/code-governance-plan-2026-05-06.md)》**§十一**（持续滚动与横切项）。

> **2026-05-08 文档修复批**：`depcheck` 已通过（`language-subtag-registry` 写入 `devDependencies`、移除未用 `@types/language-tags`）；`npm run test:visual-css` 已通过（重写 `scripts/css-visual-baseline.json`）；下文 §1/§2/§5 已与当前命令输出对齐。**2026-05-08 末工程批**：`localContext` **madge** 三角已拆（`localContextToolTypes` / `localContextToolScopeNormalize` / config 直连）；`npx madge --circular --extensions ts,tsx src` → **0**；`VoiceAgentWidget` 缺失 CSS 类已补、`check:i18n-hardcoded` baseline 已刷新；**Node 22** 上 `npm test` 全绿。

> **审查动作**：2026-05-08 对仓库进行全量技术债扫描（typecheck、build、madge、depcheck、npm audit、architecture-guard、CSS 守卫、代码体积、TODO 扫描）。本版在原方案基础上合并新发现债务，并修正已完成的待办状态。

## 0. 文档定位

本文件汇总仓库内**已识别、可切片偿还**的技术债，并给出 **PR 边界、依赖顺序与验收命令**。不替代下列权威文书的正文：

- 开放项台账：[未落地项汇总-2026-04-24.md](../governance/未落地项汇总-2026-04-24.md)
- ARCH-3 热点专文：[arch3-architecture-guard-hotspots-2026-05-05.md](../governance/arch3-architecture-guard-hotspots-2026-05-05.md)
- 审查基线：[CODE_REVIEW_REPORT_2026-05-07.md](../audits/CODE_REVIEW_REPORT_2026-05-07.md)
- 代码级整改清单：[remediation-plan-2026-04-24.md](../../remediation-plan-2026-04-24.md)
- 语料 / Source Set 产品向规划：[AI智能体-垂直产品化架构与落地方案-2026-05-05.md](./AI智能体-垂直产品化架构与落地方案-2026-05-05.md)

**不在本方案中展开**：单文件逐行读码；各阶段实现细节以 PR 与测试为准。

---

## 1. 2026-05-08 审查结论

### 1.1 现有方案中已实际完成（应从待办移除或标记 closed）

| 原方案阶段 | 内容 | 实际状态 | 证据 |
|-----------|------|---------|------|
| B2 | Source Set 持久化 + 列表读回 | **已完成** | `AiChatCard.tsx` 中 `persistSourceSets` + `persistQueueRef` 串行化写入 Dexie；`AiSourceSetBar.tsx` 从 state 读取并渲染 |
| B3 | 运行时失效检测 + UI 可见失效态 | **已完成** | `AiChatCard.tsx` mount effect 调用 `pruneInvalidatedSourceSets`；`AiSourceSetBar.tsx` 过滤 `status !== 'invalidated'`；测试 `excludes invalidated sets from dropdown` 通过 |
| A1（部分） | ARCH-3 hotspot WARN 口径对齐 | **已完成** | `npm run check:architecture-guard` 输出 OK；专文明文「当前 WARN=0」；[未落地项汇总](../governance/未落地项汇总-2026-04-24.md) 已更新为 **热点 WARN=0**（与台账一致） |

> **审查判断**：原方案对 Source Set 的进度跟踪滞后于代码实际。失效检测与持久化已在 2026-05-07 M7 迭代中落地，本方案将其归档，不再占用工程排期。

### 1.2 现有方案中仍有效的待办

| 原方案阶段 | 内容 | 当前状态 |
|-----------|------|---------|
| B4 | `toRuntimeCorpusSourceSet` / `fromRuntimeCorpusSourceSet` 接真实调用方 | **已关闭**。采用方案 A：删除悬空函数并修正架构文档；`resolveCorpusSourceSet` 已覆盖当前生产场景。 |
| C | ARCH-7 转写编排减厚 | **部分完成**。`localContextTools.ts` 已拆分（2988 → **~149** 行门面 + `localContextToolTypes.ts` / `localContextToolScopeNormalize.ts`；工具实现仍在 `localContextToolExecutors.ts` + `localContextToolFormatters.ts`）。`TranscriptionPage.ReadyWorkspace.tsx` 仍 2185 行，但已高度模块化（30+ hooks），拆分重点转为 hooks 聚合层。 |
| D | Knip / depcheck / TS 严格度 | **部分开放**。`npm run check:depcheck`：**通过**（`language-subtag-registry` 已写入 `devDependencies` 供 `build-language-tag-mappings.mjs`；已移除未使用的 `@types/language-tags`）。`noUnusedParameters`：**已在** `tsconfig.json` **全仓开启**且 `npm run typecheck` 通过。后续重点：**继续收敛** `knip.json` 的 `ignore` / `ignoreIssues`（见 [knip-exception-ledger](../governance/knip-exception-ledger-2026-05-08.md)）。 |
| E | 文档链接债务 | **已收口（链接校验）**。`npm run report:docs-link-debt`（`--all-links`）：**0** `broken link target`（2026-05-08：归一化脚本 + 增补架构锚点 + superseded 内路径修正）。新增 archive 时仍须遵守相对路径纪律。 |
| F | 供应链 audit / 例外注释 | **节奏化开放**。`npm audit --audit-level=moderate`：**当前 0 vulnerabilities**（以 lock 为准；`serialize-javascript` 已 ≥7.0.5）。仍建议发版前跑 `npm audit` / `npm audit --omit=dev` 并跟踪上游 advisory。 |

### 1.4 代码核验补充（2026-05-08）

- `npm run check:architecture-guard`：通过（WARN=0 口径保持一致）。
- `npm run check:depcheck`：通过（No depcheck issue）。
- `npm run report:docs-link-debt`：已执行，当前 **0** 处 `broken link target`（以脚本输出为准；历史「83/85/81」为旧快照）。
- `npm run check:css-debt-thresholds`：`inlineStyleOccurrences` 当前 effective=0，rawTotal=11（全部 whitelist）。
- `.nvmrc`：当前为 `22`（LTS 轨道），实验版 Node 风险已解除。

### 1.3 本次审查新增技术债（2026-05-08）

#### 🔴 P0 — 立即处理

| # | 问题 | 详情 | 修复方式 |
|---|------|------|----------|
| P0-1 | **High Severity 安全漏洞**（`serialize-javascript`） | 历史：`<=7.0.4` 经 `workbox-build` 链引入 | **已缓解**：`package-lock.json` 解析到 **serialize-javascript 7.0.5**；`npm audit --audit-level=moderate` → **0**；仍建议随上游定期复跑 |
| P0-2 | **Visual CSS 基线漂移** | 删除 `grounding-context.css` / `lucide-jieyu.css` 后基线 JSON 仍含路径；`timeline-paired-reading.css` 哈希变更 | **已修复**：`npm run test:visual-css:write-baseline`；`npm run test:visual-css` → **OK** |

#### 🟠 P1 — 高优先级

| # | 问题 | 详情 | 建议 |
|---|------|------|------|
| P1-1 | **构建产物体积** | 历史峰值：`language-mapping-runtime` 曾 ~1.05MB（未压缩）。**当前**：`language-mapping-runtime` 约 **342KB**（未压缩，审查日 `dist`）；`map-vendor` / `pdf-vendor` 等仍大 | 继续 G3：按需加载 / 独立 registry chunk / 懒加载边界 |
| P1-2 | **`useAiChat` 模块膨胀** | 模块总计 **18,438 行**（含测试，生产约 9,766 + 测试约 8,672）。`useAiChat.sendTurnStreamPhase.ts` 1130 行（已超 architecture-guard 阈值 1100）、`confirmExecution.ts` 872 行 | 拆分 send-turn 子阶段；将 `confirmExecution` 批量逻辑提取为独立模块 |
| P1-3 | **超大业务文件** | `localContextTools.ts` **~149** 行（+ 类型/归一化卫星文件）；`TranscriptionPage.ReadyWorkspace.tsx` 2185 行；`SettingsModal.tsx` 1722 行；`LinguisticService.ts` 1684 行 | 编排层继续按 ARCH-7 下沉；Settings / Linguistic 按专文拆分 |
| P1-4 | **ESLint 缺失** | ~~历史：无 `eslint.config.js`~~ | **已引入**：`eslint.config.js` + `npm run lint`（**warn 规模大**，CI 未默认跑 lint）；后续按文件收敛 `exhaustive-deps` |
| P1-5 | **依赖更新滞后** | 20+ 个包有更新。关键：`onnxruntime-web` 1.24→1.25、`pdfjs-dist` 5.6→5.7、`react` 19.2.5→19.2.6、`zod` 4.3→4.4、`lint-staged` 16→17 major | 批量更新 patch/minor；`lint-staged` major 需查看 breaking changes |

#### 🟡 P2 — 中优先级

| # | 问题 | 详情 | 建议 |
|---|------|------|------|
| P2-1 | **CSS 未使用选择器接近阈值** | ~~当前 77 个~~ → **当前 21 个**（阈值 100）。已删除废弃 `grounding-context.css`（含 44 个 GC 选择器）和 `lucide-jieyu.css`（含 12 个 Lucide 选择器） | **已修复**：余量安全 |
| P2-2 | **CSS raw color 违规** | `timeline-paired-reading.css` 有 6 处未使用 design token 的裸色值 | **已修复**：`#4f7cff` 为 `var(--accent-primary)` 的 fallback，因 token 已定义故移除 fallback；`check:css-token-usage` 通过 |
| P2-3 | **内联样式白名单债务** | `check:css-debt-thresholds` 当前 `inlineStyleOccurrences=0`（effective），但 rawTotal=11 且全部来自 whitelist | 逐步去白名单并收敛 rawTotal，避免阈值回退掩盖新增内联样式 |
| P2-4 | **生产代码 `console.error`** | `AiChatCard.tsx` 2 处（source set hydration/prune 失败、persistSourceSets 失败） | **已修复**：接入 `fireAndForget`（`background-quiet` policy），错误不再裸输出到 console，而是进入统一日志/Sentry 通道 |
| P2-5 | **TODO 注释残留** | `corpusSourceSet.ts` 中 TODO(P1-3)、TODO(P1-4) 对应功能已完成 | **已修复**：改为 ARCHITECTURE-NOTE 说明历史 |
| ~~P2-6~~ | ~~**depcheck false positive**~~ | ~~`cross-env` 被报告为未使用~~ | ~~**误报**：`npm run check:depcheck` 实际通过；`.depcheckrc.json` 已正确配置~~ |

#### 🟢 P3 — 低优先级 / 观察项

| # | 问题 | 详情 | 建议 |
|---|------|------|------|
| P3-1 | **DB Schema 版本频繁** | Dexie schema v1→v49 共 49 个版本声明；独立迁移脚本 `src/db/migrations/*.ts` 12 个（含测试/辅助文件）| 监控生产迁移稳定性；合并小规模 schema 变更 |
| P3-2 | **Node.js 版本策略跟踪** | `.nvmrc` 当前为 v22（LTS），实验版风险已解除 | 维持 LTS 轨道，并在依赖升级窗口评估次版本升级 |
| P3-3 | **条件 skip 测试** | `otel.collectorSmoke.test.ts`（otelSmokeEnabled 条件）和 `m18LinguisticUnitCutover.upgradeStress.test.ts`（STRESS_N 条件）默认不运行 | 保持现状；属预期行为 |

---

## 2. 阶段与 PR 切片（修订版）

### 阶段 0 — P0 紧急修复（1 个 PR，可立即合入主干）

| 步骤 | 内容 | 验收 |
| --- | --- | --- |
| 0-1 | `npm audit fix` 修复 `serialize-javascript` 漏洞；确认 `workbox-build` 依赖链已升级 | `npm audit --audit-level=moderate` 输出 0 vulnerability |
| 0-2 | 调查 `grounding-context.css` snapshot 变更原因：确认是预期变更（如 Source Set UI 新增样式）还是意外漂移 | `npm run test:visual-css` 通过；若预期变更则 `npm run test:visual-css -- --write-baseline` |

> **排期**：W1 Day 1–2；可与任何功能 PR 并行。

### 阶段 A — 台账与审查结论对齐（文档 PR）

| 步骤 | 内容 |
| --- | --- |
| A1 | 更新 [未落地项汇总-2026-04-24.md](../governance/未落地项汇总-2026-04-24.md)：ARCH-3 改为「热点 WARN=0，但大文件仍为演进约束区」；Source Set 持久化/失效检测标记为已关闭；更新 `last_reviewed`。 |
| A2 | 清理 `corpusSourceSet.ts` 中已完成 TODO(P1-3)/(P1-4)：改为 ARCHITECTURE-NOTE 或删除，避免误导。 |

**验收**：`npm run check:docs-governance`

> **排期**：W1；可与阶段 0 同一 PR 或独立文档 PR。

### 阶段 B — Source Set 悬空函数收尾（1 个功能 PR）

**决策结论（已拍板）**：采用 **方案 A** —— 删除悬空函数并修正架构文档。

理由：`resolveCorpusSourceSet` 已完全覆盖当前所有生产场景（从实时 prompt context 构造 `CorpusSourceSet`）；`toRuntimeCorpusSourceSet` / `fromRuntimeCorpusSourceSet` 自创建以来从未被调用，且架构文档 `ai-chat-send-turn-pipeline.md` 的描述与实际代码不符。若未来需要「持久化 Source Set → runtime」转换路径，由产品需求驱动重新设计。

| 步骤 | 内容 | 验收 |
| --- | --- | --- |
| B1 | 删除 `toRuntimeCorpusSourceSet` 和 `fromRuntimeCorpusSourceSet` 函数及其测试用例 | `npm run typecheck`；`rg toRuntimeCorpusSourceSet\|fromRuntimeCorpusSourceSet` 无生产残留 |
| B2 | 更新 `docs/architecture/ai-chat-send-turn-pipeline.md`：将「`SavedCorpusSourceSet` → `toRuntimeCorpusSourceSet`」修正为实际调用路径「`AiPromptContext` → `resolveCorpusSourceSet`」 | `npm run check:docs-governance` |
| B3 | 删除 `corpusSourceSet.ts` 中 P1-3/P1-4 的 TODO 注释（若 A2 未处理） | `rg TODO\(P1-[34]\)` 无残留 |

> **排期**：W1–W2；低冲突，可独立 PR。

### 阶段 C — ARCH-7 转写编排减厚（多个小 PR，节奏化）

策略不变：每次单域下沉，页面仅减编排、行为不变。

| 步骤 | 内容 | 验收 |
| --- | --- | --- |
| C1 | PR 描述附 `npm run check:architecture-guard` 输出片段作基线 | 无新增 WARN |
| C2 | 继续按特性拆 `TranscriptionPage.ReadyWorkspace.tsx`：候选方向—— overlays 状态聚合、layout style 输入、audio capture 控制器进一步下沉 | `npm run check:architecture-guard`；`TranscriptionPage.structure.test.ts` 通过 |
| C3 | ~~`localContextTools.ts`（2988 行）拆分~~ → **已完成**（门面 **~149** 行 + `localContextToolTypes` / `localContextToolScopeNormalize` + executors/formatters）；**madge 三角已清** | `npm run typecheck`；`npx madge --circular --extensions ts,tsx src` → **0** |

> **排期**：W2 起持续进行；每轮 1 个 PR。

### 阶段 D — Knip / depcheck / TS（专项或随功能 PR 捎带）

| 步骤 | 内容 | 验收 |
| --- | --- | --- |
| D1 | `knip.json`：每次从 `ignore` / `ignoreIssues` 移除 1～3 项，修死代码或补入口 | `npx knip --no-progress` 通过；配置变短 |
| D2 | 保持 `.depcheckrc.json` 与 `package.json` 脚本一致，避免 false positive 回归 | `npm run check:depcheck` 持续通过 |
| D3 | `noUnusedParameters`：先单目录试点（如 `src/ai/vertical/`），再全仓打开 | `npm run typecheck` |

> **排期**：W1 D3 起捎带；D1 可并入阶段 0 PR。

### 阶段 E — 文档链接债务（并行、低阻塞）

策略不变。

| 步骤 | 内容 | 验收 |
| --- | --- | --- |
| E1 | 已跑 `npm run report:docs-link-debt`，**0** `broken link target`（2026-05-08 批量归一化） | 新增/搬迁 Markdown 时继续跑 `report:docs-link-debt`；禁止机器绝对路径入链 |
| E2 | 每 PR 修一类路径 | `check:docs-governance` |

### 阶段 F — 供应链与例外注释（节奏化）

| 步骤 | 内容 |
| --- | --- |
| F1 | 定节奏执行 `npm audit`，区分 prod 与 dev；可修复项入独立 PR |
| F2 | 新增 `eslint-disable` / `@ts-expect-error` 必须附理由；周期性审计数量 |
| F3 | 批量更新 patch/minor 依赖（排除 major  breaking change 风险包） | `npm run typecheck` + `npm run build` + `npm test` |

> **排期**：W2 起；F3 建议每两周一次依赖更新 PR。

### 阶段 G — 构建产物与工具链优化（新增，P1）

| 步骤 | 内容 | 验收 |
| --- | --- | --- |
| G1 | **ESLint 最小化引入（方案 B）**：安装 `eslint` + `typescript-eslint` + `eslint-plugin-react-hooks`；配置 `eslint.config.js` 仅启用 3 条规则：`react-hooks/rules-of-hooks` (error)、`react-hooks/exhaustive-deps` (warn)、`@typescript-eslint/no-unused-vars` (warn, `argsIgnorePattern: '^_'`)。不引入 stylistic 规则，不与现有 TS strict mode 重复。首轮扫描后按文件分批修复 `exhaustive-deps`；`lint` 脚本不阻塞 CI | `npm run lint` 可运行；产出首轮违规数量报告；`rules-of-hooks` + `exhaustive-deps` 已启用 |
| G2 | **Bundle 分析**：跑 `npm run build` 并分析 `language-mapping-runtime` 体积。根因已明确：1.05 MB 来自 `language-tags` → `language-subtag-registry`（1.11 MB source），而非 `iso6393Seed.generated.ts`（该文件为死代码）。`manualChunks` 对 `with { type: 'json' }` 的拆分失效，registry JSON 被强制内联到 `language-mapping-runtime`。 | **已实施**：预构建 `language-tag-mappings.json`（316 KB → 运行时 4.4 KB gzip），`langMapping.ts` 已移除 `language-tags` 依赖；`language-mapping-runtime` 从 1,054 KB 降至 341 KB；`language-tags` 从 `dependencies` 移至 `devDependencies` |
| G3 | **体积优化实施**：分两步。第一步（立即）：删除死代码 `iso6393Seed.generated.ts` 及生成脚本。第二步：尝试修复 `manualChunks` 将 `language-subtag-registry` 强制拆出为独立 chunk；若 Vite 插件方案脆弱，则退而求其次实施懒加载（将所有 `language-tags` 调用移至异步边界） | `language-mapping-runtime` 降至 ~50 KB（逻辑）+ 独立 registry chunk；或实现懒加载后首屏不携带 1 MB |
| G4 | **`useAiChat` 模块瘦身**：将 `sendTurnStreamPhase.ts`（1130 行）按 stream 消费 / retry / agent loop 拆分为 2–3 个子文件；将 `confirmExecution.ts`（872 行）的批量逻辑提取 | `npm run check:architecture-guard` 无新增 WARN；相关测试通过 |

> **排期**：G1 W1；G2–G3 W2–W3（需分析后决策）；G4 W3–W4。

### 阶段 H — CSS 债务清理（新增，P2）

| 步骤 | 内容 | 验收 |
| --- | --- | --- |
| H1 | 清理 77 个未使用 CSS 选择器：优先删除 `gc-fatigue*`、`gc-hotspot-bar-bg` 等明显废弃的 GC 样式 | `npm run check:css-unused-selectors` 余量下降（目标 < 50） |
| H2 | `timeline-paired-reading.css` 6 处 raw color 替换为 semantic token | `npm run check:css-token-usage` 通过（raw color = 0） |
| H3 | 内联样式迁移：每次处理 1–2 个组件，将 inline style 改为 CSS class | `npm run check:css-debt-thresholds` 中 inlineStyleOccurrences 下降 |

> **排期**：W2 起持续进行；每步独立小 PR。

---

## 3. 验收命令矩阵（每 PR 至少子集）

| 命令 | 何时必跑 |
| --- | --- |
| `npm run typecheck` | 所有改代码 PR |
| `npm run check:architecture-guard` | 阶段 C、G4、或任何可能影响体量/钩子的改动 |
| `npm run check:docs-governance` | 阶段 A、E2、及任何 `docs/` 改动 |
| `npm run check:css-debt-thresholds` | 阶段 H |
| `npm run check:css-token-usage` | 阶段 H2 |
| `npm run check:css-unused-selectors` | 阶段 H1 |
| `npx knip --no-progress` | 阶段 D |
| `npm run check:depcheck` | 改动 `package.json` / `.depcheckrc.json` 时 |
| `npm audit --audit-level=moderate` | 阶段 0、F1、F3 |
| `npm run test:visual-css` | 阶段 0-2、任何 CSS 文件改动 |
| 定向 `vitest run …` | 阶段 B/C/G4/H 在 PR 描述中显式列出 |

全量 `npm test`：发版前或大改动后按仓库惯例执行。

---

## 4. 建议排期（修订版）

| 周次 | 主力交付 | 捎带 |
| --- | --- | --- |
| **W1（已执行）** | 阶段 0 ✅（安全漏洞 `npm audit fix` + CSS snapshot `--write-baseline`）+ B1 ✅（删除悬空函数 + 更新架构文档）+ G2-D ✅（删除 `iso6393Seed.generated.ts` + 更新生成脚本）+ G1 ✅（ESLint 引入 + 修复 14 个 `rules-of-hooks` errors） | 阶段 A（台账对齐文档 PR） |
| **W2** | C2（ReadyWorkspace 继续拆分）+ G2（Bundle 分析）+ H1（CSS 未使用选择器清理） | F3（依赖 patch/minor 更新） |
| **W3** | G4（`useAiChat` 瘦身）+ H2（raw color 复核） | D2（Knip 减 ignore） |
| **W4** | G3（按需加载决策实施，若 G2 结论为可行）+ H3（白名单内联样式收敛） | E2（文档链接 debt 分类持续修复） |
| **持续** | C2/C3 持续轮次、H 持续轮次 | E、F 穿插 |

---

## 5. 关闭本方案的条件（修订版）

当满足以下**全部**且台账已更新时，可将本文 `status` 改为 `superseded` 并指向新的滚动计划文件：

- [x] **P0 清零**：`npm audit` high severity = 0；`test:visual-css` 通过。
- [x] **Source Set 收尾**：`toRuntimeCorpusSourceSet` / `fromRuntimeCorpusSourceSet` 已删除；相关 TODO 改为 ARCHITECTURE-NOTE。
- [x] **ARCH-7 达到团队定义的「已缓解」**：`localContextTools.ts` 已从 2988 行拆至 **~149** 行门面（类型与 scope 归一化见卫星文件）；`TranscriptionPage.ReadyWorkspace.tsx` 仍 2185 行但已高度模块化（30+ hooks），需进一步聚合 hooks 层。
- [x] **ESLint 已引入**：`eslint.config.js` 已落地，`npm run lint` 可运行；`rules-of-hooks` (error) + `exhaustive-deps` (warn) 已启用；14 个 hooks errors 已修复。
- [x] **Bundle 分析结论已记录**：`language-mapping-runtime` 体积根因已明确（`language-subtag-registry` 内联）；`iso6393Seed.generated.ts` 死代码已删除；分析报告已产出；建议方案 A（动态导入）优先。
- [x] **CSS 债务余量安全**：`unusedSelectors` = 21 < 50；`inlineStyleOccurrences`（effective）= 0；`raw color` = 0（timeline-paired-reading.css 已修复）。
- [x] **Knip** 收敛至团队认可的剩余集合并有书面例外清单（`docs/execution/governance/knip-exception-ledger-2026-05-08.md`）。
- [x] **`noUnusedParameters`** 已全仓开启（`tsconfig.json`），`npm run typecheck` 通过。
- [x] **`madge` 全仓 `src` 无向环 = 0**：`npx madge --circular --extensions ts,tsx src` → **0**（2026-05-08：`localContextToolTypes` / `localContextToolScopeNormalize` + 字符预算直连消除三角）。
