# 证据化发布与 AI 工具治理完整方案（2026-04-20）

状态：可执行规格（拆卡后直接落地）
日期：2026-04-20
治理：归档于 [docs/execution/plans](docs/execution/plans)，符合 [.cursor/rules/jieyu-docs-governance.mdc](.cursor/rules/jieyu-docs-governance.mdc)

---

## 0. 文档定位

本方案把「结论—证据索引」方法论、AI 工具权限策略单一事实源、会话与项目记忆统一编排、工具契约可 diff、扩展与观测可证明性、性能门限与发布联动、语音运行时渐进平台化等事项，收敛为**分阶段、可验收、可回滚**的执行规格。与下列既有能力对齐，避免重复造轮子：

- 工具审计与回放：[src/ai/auditReplay.ts](src/ai/auditReplay.ts)、[src/hooks/useAiChat.toolAudit.ts](src/hooks/useAiChat.toolAudit.ts)
- 指标聚合：[src/observability/metrics.ts](src/observability/metrics.ts)（`summarizeMetrics`）
- 工具决策管道与闸门：[src/hooks/useAiChat.toolDecisionPipeline.ts](src/hooks/useAiChat.toolDecisionPipeline.ts)、[src/hooks/useAiChat.destructiveGate.ts](src/hooks/useAiChat.destructiveGate.ts)
- 转写页风险检查：[src/pages/transcriptionAiToolRiskCheck.ts](src/pages/transcriptionAiToolRiskCheck.ts)
- 扩展运行时与审计尾：[src/extensions/extensionRuntime.ts](src/extensions/extensionRuntime.ts)、[src/extensions/extensionRegistry.ts](src/extensions/extensionRegistry.ts)
- Schema 与校验：[src/ai/chat/toolCallSchemas.ts](src/ai/chat/toolCallSchemas.ts)
- 会话循环与 token 预算：[src/hooks/useAiChat.agentLoopRunner.ts](src/hooks/useAiChat.agentLoopRunner.ts)
- 项目记忆检索：[src/services/ProjectMemoryStore.ts](src/services/ProjectMemoryStore.ts)（`getRagContextVector`）
- 会话记忆归一化：[src/ai/chat/sessionMemory.ts](src/ai/chat/sessionMemory.ts)
- 发布与观测脚本：[package.json](package.json)（`report:m5-trend`、`gate:m6-release`、`gate:m7-extension`…、`perf:*` 等）
- 语音服务：[src/services/VoiceAgentService.ts](src/services/VoiceAgentService.ts)

### 0.1 外部参考与映射原则（架构级借鉴，非代码移植）

对照阅读（公开索引，便于评审溯源）：

- [Abe-Junwei/claude-code-analysis](https://github.com/Abe-Junwei/claude-code-analysis)：对公开流传的 Agent 平台源码做**静态阅读**后的主题化笔记（Memory、Tool、Sandbox、Context、Session/Resume、Multi-Agent、证据索引等章）。
- [Abe-Junwei/cc-haha](https://github.com/Abe-Junwei/cc-haha)：基于同源泄露代码的**可运行分叉**说明（无头模式、Recovery、多 Agent、记忆、远端 Channel 等运维与产品面）。

**映射原则**：本方案只吸收**行为级 / 治理级**教训（例如分层记忆、沙箱 fail-closed、会话恢复卫生、CI 可确定性），不在 Jieyu 中逐行移植第三方源码。外部仓库的版权声明与使用边界以各仓库 README 为准。

**稳定性声明（新增）**：外部仓库仅用于方案评审阶段的灵感对照与反例校验；若外部链接失效、仓库下线或内容更新，本方案仍以本仓代码与 [docs/architecture/](docs/architecture/) 为唯一落地依据，不将外部内容视为实现前提。

**主题对照（评审用，非实现依赖）**：本方案工单与上述分析主题的粗略对应为 — C1/C4/C5 ↔ Memory / Context / Resume；C6 ↔ Sandbox；B1–B3 ↔ Tool 权限与扩展面；P1-ObsSec ↔ 观测与数据外流面；P1-CostGuard ↔ 上下文与计费策略面。

### 0.2 与本仓 `package.json` 的事实对齐（避免规格漂移）

编写 A1/A3 与触发条件时，以仓库内**已存在脚本名为准**，规格中若提出新脚本名，须在落地 PR 中同步 `package.json`。

- **观测与发布门**：除 `report:m5-trend`、`gate:m6-release`、`report:m7-extension-gate` 外，尚存在 `**gate:m8`～`gate:m14`** 与 `gate:collaboration-cloud` 等协作向长链（见第 8 节 Release Profile）。
- **时间轴索引 perf**：`src/perf/TimelineUnitViewIndexPerformanceBaseline.test.ts` 当前随 `**npm run test:timeline-cqrs-phase9`** 一并执行，**无**独立 `perf:timeline`。A3 应优先调用该现有入口；若将来抽离为 `perf:timeline`，在证据报告中记录**实际 npm script 名**即可。
- **轨道布局 perf**：`npm run perf:track` 已存在。
- **AI perf 基线**：`npm run perf:ai` 已存在。

---

## 1. 术语、命名空间与发布证据契约

### 1.1 命名空间（实现与文档须一致）


| 类别                   | 含义                                                       | 示例                                                  |
| -------------------- | -------------------------------------------------------- | --------------------------------------------------- |
| **审计 reason / 事件标签** | 写入工具决策审计或门控路径的**字符串原因**，用于回放与发布卡                         | `ambiguous_target`、`auto_failed:*:ambiguous_target` |
| **指标 id**            | `src/observability/metrics.ts` 注册的 **metric id**，用于聚合与趋势 | `blocked_write_without_explicit_target_total`       |
| **报告字段名**            | `release-evidence.json` 等产物中的 JSON 键                     | `progressiveDisclosure.cards[]`、`perf.cards[]`      |


触发条件、告警与工单描述中，**禁止混用**「审计 reason」与「metric id」而不加标注。

### 1.2 Release Profile（发布证据 bundle 的运行档）

A1  orchestrate 的子步骤必须声明本次使用的 **profile**，并写入报告元数据，避免读者默认「全仓库所有 gate 均已执行」。


| Profile        | 典型包含                                                                                    | 适用场景       |
| -------------- | --------------------------------------------------------------------------------------- | ---------- |
| `lite`         | `check:docs-governance`、`check:architecture-guard`、核心 vitest 子集、跳过或 shadow 部分重门限        | 日常 PR、快速反馈 |
| `core`         | 在 `lite` 基础上纳入 `gate:m6-release`（或 shadow）、`report:m5-trend`、扩展 gate 等与 AI/观测主链路直接相关的条目 | 常规里程碑      |
| `collab-cloud` | 在 `core` 基础上串联 `gate:collaboration-cloud` 及团队约定的协作里程碑子集                                 | 协作能力发布周    |
| `full`         | 在选定基线上纳入完整 perf、shadow 对比、Week 6 DoD 所列必检项等                                             | 发布候选、重大版本  |


**首版 A1 落地约束**：首版 bundle 至少串联 `**lite` 或 `core` 之一** 的可运行子集并产出有效证据索引；`full` 为演进目标，避免第一版即绑定全部 `gate:m`* 导致不可维护。

### 1.3 编排器行为契约（对齐无头/CI 产品的工程习惯）

`generate-release-evidence-bundle` 建议约定：

1. **退出码**：任一 enforce 子步骤失败 → 非零退出；若配置为「失败仍写报告」，则同时设置报告根字段 `degraded: true` 并列出失败步骤（与「静默成功」区分）。
2. **部分产物**：子进程失败时仍写出 JSON/Markdown 摘要，且 `证据索引表` 中失败行含**退出码、日志路径、首条错误摘要**。
3. **可复现**：报告元数据含 git 短 SHA、profile 名、主要 npm script 版本（与 A1 正文一致）。

---

## 2. 总目标与非目标

### 2.1 总目标（约 6 个月滚动达成）

1. **发布决策可证据化**：里程碑或 CI 能产出固定版式的发布资产，含结论、证据索引、未通过项与复现命令。
2. **工具与扩展策略可审计、可扩展**：单一策略源描述工具与扩展能力与确认规则；拒绝原因码稳定，可统计。
3. **记忆召回在预算内可解释**：统一编排层输出带「为何命中」元数据的上下文块。
4. **契约与脱敏可证明**：工具参数契约可自动生成并与前版 diff；敏感字段脱敏有覆盖率与回归清单。

### 2.2 非目标（本方案不承诺）

1. 替换现有全部 `gate:m`* 链为单一脚本（仅在其上增加统一呈现、索引层与 profile 调度）。
2. 一次性重写语音栈或引入未讨论的新引擎。
3. 将本文件升格为产品架构唯一真源；架构事实仍以 [docs/architecture/](docs/architecture/) 与代码为准。

---

## 3. 路线图、渐进披露与近 6 周节奏

### 3.1 分阶段路线图（A → B → C）

执行顺序：**必须先完成阶段 A 的可运行最小闭环**，再进入 B；C 仅在触发条件满足时启动（见第 7.1 节）。


| 阶段  | 时间盒（建议）           | 主题                 | 核心交付物                                      |
| --- | ----------------- | ------------------ | ------------------------------------------ |
| A   | 4–8 周             | 证据化主链路             | 统一发布报告生成器 + AI 证据卡 + 时间轴 perf 证据卡 + 文档符号守卫 |
| B   | 6–10 周（可与 A 尾部重叠） | 策略 SSOT            | 工具/扩展策略表 + 渐进迁移 + 发布报告中的策略摘要               |
| C   | 触发式               | 记忆 broker 与语音/扩展加深 | 见第 7 节触发条件与工单                              |


### 3.2 渐进披露（Progressive Disclosure）约束

为避免“功能都在但首屏过载”，本方案补充以下治理约束（对齐 NNGroup / IxDF 的通用定义）：

1. 首层仅承载高频任务必需能力；低频或高风险能力按需展开，不默认灌入。
2. 次层入口必须可发现、可预期（文案明确：展开后会看到什么）。
3. 核心任务应可在首层完成；仅在信息不足或风险升高时进入澄清/确认。
4. 默认不超过 2 级披露；如需更多层级，必须提供分组依据与可用性测试证据。
5. 渐进披露不是“隐藏能力”；需提供可追溯路径（入口、提示、回退）。

### 3.3 当前实现判定（2026-04-25 复核）

以本仓代码与脚本实证复核，形成本轮排期基线：

| 工单 | 复核结论 | 代码/脚本证据 | 备注 |
| --- | --- | --- | --- |
| A1 发布报告生成器 | 已落地（A1a） | `scripts/generate-release-evidence-bundle.mjs`、`report:release-evidence`、`gate:release-evidence:core/full` 已可运行并产出 `release-evidence-*.json` | A1b 持续收口 |
| A2 AI 证据卡 | 部分存在 | `src/ai/auditReplay.ts`、`src/hooks/useAiChat.toolAudit.ts` 已存在 | 字段链路需对齐 |
| A3 时间轴性能卡 | 基础存在 | `perf:track` 已存在；`TimelineUnitViewIndexPerformanceBaseline` 已纳入 `test:timeline-cqrs-phase9` | 待接入 A1 报告 |
| A4 文档符号对齐 | 已落地 | `docs/architecture/timeline-unit-governance.md` 已改 canonical-only；新增 `scripts/check-doc-code-symbol-parity.mjs` 并接入 `check:architecture-guard` | 持续扩允许列表 |
| B1 策略 SSOT | 已完成 | 已新增 `src/ai/policy/aiToolPolicyMatrix.ts`，并完成 `transcriptionAiToolRiskCheck` / `useAiChat.destructiveGate` / `toolCallHelpers` / `toolFeedback` / `useAiToolCallHandler`（含 `segment` 与 `layer-link` 入口/动作语义）接线 | 第 3 周 |
| B2 渐进迁移执行器 | 已完成 | `isNonDeleteSegmentWriteTool` 和 `requiresWriteTargetMaterialization` 均已替换为 SSOT 派生（`isAiToolSegmentTargetMaterializationTool + isAiToolDestructive`）；无硬编码工具名字符残留在执行器内 | 第 3 周 |
| B3 扩展能力审计进报告 | 已完成 | `generate-release-evidence-bundle.mjs` 已含 `buildExtensionCapabilityAuditSection`；证据索引已有 `b3.extensions.capability-audit-summary` 节；测试 5 passed | 第 3 周 |
| P1-CostGuard | 部分存在 | `src/ai/chat/contextBudget.ts` 提供预算逻辑 | 缺硬阈值与会话级 stop |
| P1-ProgressiveDisclosure | 缺失 | `metrics.ts` 未见本方案定义的披露专项指标 | 依赖 A1 报告承载 |
| P1-RagShapeTelemetry | 部分存在 | `ai.rag_citation_read_model_miss` 已存在 | 缺候选规模/抑制率等分母指标 |
| C1 Memory Broker | 缺失 | `src/ai/memory/memoryBroker.ts` 不存在 | 触发后实施 |
| C5 后台子代理 | 缺失 | 未见对应实现入口 | 触发后实施 |
| C6 工具沙箱 | 缺失 | 未见对应实现入口 | 触发后实施 |

**汇总（本轮）**：`已存在/基础存在 2` + `部分存在 3` + `缺失/待修复 6`。执行上先收敛阶段 A 的可运行闭环。

### 3.3.1 周进度看板（新增，执行强制）

每周例会按本表更新，未更新视为该周未完成 DoD：

| 周次 | 目标工单 | 状态 | 验收命令/产物 | 阻塞与责任人 |
| --- | --- | --- | --- | --- |
| 2026-04-25 当周 | A1a + A4 | 已完成 | `npm run report:release-evidence -- --profile=lite` + `npm run check:architecture-guard` + `npm run check:docs-governance` | 后续仅做 A1b 字段与证据索引收口；负责人：发布工程 |
| 第 2 周 | A1b + A2 | 已完成 | `npm run -s export:release-evidence:ai-audit` + `npm run -s gate:release-evidence:core`（`RELEASE_EVIDENCE_AI_REQUEST_IDS=toolreq_runtime_ci_001`，`RELEASE_EVIDENCE_AI_AUDIT_EXPORT=docs/execution/audits/ai-tool-decision-audit-export-v1.ndjson`）；产物：`docs/execution/release-gates/release-evidence/release-evidence-20260425-111614-core.json` | 已切换到审计导出路径并接入运行时自动导出；后续继续对接真实产品会话导出；负责人：发布工程 |
| 第 3 周 | B1 + B2 + B3 | 已完成 | `npx vitest run src/ai/policy/aiToolPolicyMatrix.test.ts src/pages/transcriptionAiToolRiskCheck.test.ts src/hooks/useAiChat.destructiveGate.test.ts scripts/generate-release-evidence-bundle.test.ts` | B1 SSOT 全工具在廎；B2 两个执行器内硬编码已全部替换为 SSOT 派生调用；B3 扩展审计证据卡已进发布报告 |
| 第 4 周 | A3 + P1-CostGuard v1 | 已完成 | 报告含 perf 卡 + 成本卡；`npx vitest run scripts/generate-release-evidence-bundle.test.ts`；`npx vitest run src/hooks/useAiChat.test.tsx` | 已完成运行时 cost guard 主链（会话预算硬阈值、output cap 升级重试、审计 reason 链）并通过全量 useAiChat 回归 |
| 第 5 周 | 复盘与重排 | 已完成 | 书面决议：`2026-04-25` 不启动 C 阶段实现，下一阶段转入 B 并行 P1（ToolsDoc / ObsSec / ProgressiveDisclosure）并保持 default behind-flag | 依据 `release-evidence-20260425-111614-core.json`、`release-evidence-20260425-111926-full.json` 与周内回归记录 |

### 3.4 近 5 周执行节奏（2026-04-25 重排）

从“技术主题优先”改为“可交付密度优先”，每周必须产出可被发布证据索引引用的真实资产：

1. **第 1 周（本周）**：A1a + A4
  - A1a：先交付 `release-evidence.json` 骨架（metadata/summary/evidenceIndex）与 `report:release-evidence` 脚本入口。
  - A4：修正文档错误符号引用（或补齐对应实现）并接入守卫。
2. **第 2 周**：A1b + A2
  - A1b：补齐子进程编排（至少 `check:docs-governance`、`check:architecture-guard`、`report:m5-trend`、`gate:m6-release`）。
  - A2：接入 requestId 样本证据卡，形成回放闭环。
3. **第 3 周**：B1
  - 建立策略 SSOT（模型、确认模式、审计 reason 对齐），并完成最小迁移路径。
4. **第 4 周**：A3 + P1-CostGuard v1
  - A3：perf 输出结构化进入报告。
  - P1-CostGuard：落地会话级硬阈值、超限原因可审计。
5. **第 5 周**：证据复盘 + 优先级重排
  - 仅在前四周证据证明必要时，启动 C 阶段细化与实施。

**执行约束（更新）**：

1. 未完成 A1a 前，不启动 C 阶段实现工作。
2. 每周只允许一个主能力面进入 `enforce`，其余能力 behind-flag。
3. 当周 DoD 失败时，下一周默认只做收口，不新增工单。

### 3.5 跨仓对照新增的 7 项借鉴（代码证据导向）

1. 后台记忆抽取子代理（与主链路互斥，避免重复写入）。
2. 后台任务工具权限沙箱（只读优先、受限写入范围）。
3. 检索形态遥测（含空选、候选规模、抑制率等分母指标）。
4. 成本治理硬阈值（会话预算上限与可解释终止原因）。
5. 输出 token 封顶 + 升级重试（capped default + escalation）。
6. 上下文压缩后的状态回注一致性（任务状态、摘要、最近工具上下文）。
7. 轻量协调模式与并行策略规范（research / synthesis / implementation / verification）。

### 3.6 第 5 周书面决议（2026-04-25）

**输入证据**：

1. 阶段 A/B 关键产物均已进入 release-evidence 索引（A1~A4、B1~B3、P1-CostGuard v1）。
2. 周内关键回归已通过：`scripts/generate-release-evidence-bundle.test.ts`、`src/hooks/useAiChat.test.tsx`、B1/B2/B3 目标测试子集。
3. 成本治理与扩展能力审计已接入 `core/full` gate，暂无新的 P0 级失败链要求立刻进入 C 阶段。

**决议结论**：

1. 本轮**不启动 C 阶段实现**，仅保留 C 阶段为触发式候选池。
2. 下一阶段进入 **B 并行 P1 收口**：`P1-ToolsDoc`、`P1-ObsSec`、`P1-PerfReport`、`P1-ProgressiveDisclosure`。
3. 所有新增能力继续 default behind-flag；仅在 evidence 卡稳定两个发布周期后评估是否进入 enforce。

**下一阶段排期（执行版）**：

1. 第 6 周：`P1-ToolsDoc`（契约快照导出 + drift 检查）与 `P1-ObsSec`（脱敏清单覆盖率）并行，要求产物均可被 release-evidence 索引引用。
2. 第 7 周：`P1-ProgressiveDisclosure` 指标与门限接入报告，完成首版阈值卡与 skipped 原因模板。
3. 若第 6~7 周任一能力未达 DoD，则第 8 周仅做收口，不新增工单。

### 3.7 第 6~7 周阶段总结（2026-04-25）

**已完成并入证据链**：

1. `P1-ToolsDoc`：工具契约快照导出 + drift 检查命令已落地。
2. `P1-ObsSec`：`otel/sentry/logger` 敏感键脱敏策略已收敛为单一模块，并有固定 payload 回归测试。
3. `P1-PerfReport`：`perf.cards[]` 已支持 JSON reporter 输入并纳入 release-evidence。
4. `P1-ProgressiveDisclosure`：四项比率卡（first-pass / clarify-then-success / advanced-entry / clarify-loop）已进入报告与 evidence 索引。
5. `P1-RagShapeTelemetry`：`memoryRecallShape` 卡已进入报告与 evidence 索引，覆盖候选规模/空选率/重复抑制率/预算抑制率/新鲜度分布。

**当前剩余（B 并行 P1）**：

1. `P1-CostGuard`：推进到“多发布周期对照与口径版本稳定”，补齐趋势跟踪证据。

**下一步执行约束**：

1. 先收敛 `P1-CostGuard` 趋势跟踪，再评估是否需要触发 C 阶段。
2. 若任一能力未达到 DoD，仅做收口，不新增并行工单。

---

## 4. 阶段 A：证据化主链路（P0）

### 4.1 工单 A1 — 统一发布报告生成器（Release Evidence Bundle）

为确保可快速落地，A1 强制拆分为两个子工单：

#### 4.1.1 A1a（半天）— 报告骨架与脚本入口

**目标**：最短路径交付可运行的 `report:release-evidence`，先产出结构稳定的 JSON 骨架。

**必须产出**：

1. 新增 `scripts/generate-release-evidence-bundle.mjs`（或 `.ts`）最小版本。
2. [package.json](package.json) 新增 `report:release-evidence`（可带 `--profile=lite`）。
3. 产物包含：
  - `metadata`（git SHA、timestamp、releaseProfile、env）
  - `summary`（passed/failed/skipped 计数）
  - `evidenceIndex`（允许空数组，但字段必须存在）
  - `degraded`（失败/跳过标记）

**A1a 验收标准**：

1. 本地可运行并生成 JSON：`npm run report:release-evidence -- --profile=lite`。
2. `npm run check:docs-governance` 通过。
3. 脚本失败时仍能写出带 `degraded` 的报告骨架。

#### 4.1.2 A1b（1 天）— 子进程编排与证据索引最小闭环

**目标**：在 A1a 基础上接入可复用的 gate 编排与证据索引，形成首个评审可用版本。

**报告必选章节（A1b 起强制）**：

1. **元数据**：git 短 SHA、时间戳、`releaseProfile`、运行环境（ci/local）。
2. **摘要**：通过/未通过/跳过门限数量。
3. **证据索引表**：`结论 id`、`证据类型`、`命令/产物路径`、`模块`、`exitCode`。
4. **子进程输出引用**：至少覆盖 `check:docs-governance`、`check:architecture-guard`、`report:m5-trend`、`gate:m6-release`。
5. **渐进披露指标卡**：先支持 `skipped + skipReason`，避免空壳字段。

**A1b 验收标准**：

1. `evidenceIndex` 至少 5 行有效引用。
2. 任一子步骤失败时，报告可定位到首个失败步骤与日志路径。
3. `lite/core` 两种 profile 均可生成报告。

**风险与回滚**：低风险；回滚仅撤销脚本入口与 CI job，不影响业务代码。

---

### 4.2 工单 A2 — AI 工具调用「证据卡」

**目标**：在发布报告中嵌入结构化「AI 工具证据卡」片段（可多条），便于发布评审与回归对照。

**字段建议（v1）**：

- `requestId`、`toolName`、`replayable`、`latestDecision.decision`、`diff.matches`（若有 baseline 文件路径则填）
- `goldenSnapshotPath`（可选，CI 产出路径）
- `auditLogQueryHint`：例如 `collection=ai_messages` + `field=ai_tool_call_decision` + `requestId`

**数据来源**：

1. 回放装配：`loadAiToolReplayBundle`、`buildAiToolGoldenSnapshot`、`serializeAiToolGoldenSnapshot`、`diffAiToolSnapshot`（[src/ai/auditReplay.ts](src/ai/auditReplay.ts)）。
2. 审计写入：`writeToolDecisionAuditLog`（[src/hooks/useAiChat.toolAudit.ts](src/hooks/useAiChat.toolAudit.ts)）。

**实现要点**：

1. 在 A1 脚本中增加可选步骤：从最近一次 E2E 或指定 fixture 导出 sample `requestId` 列表（或由环境变量注入）；无 DB 时跳过并标记 `skipped`。
2. 不在热路径增加同步 IO；发布流水线专用。

**参数速查（2026-04-25 已落地）**：

1. `--ai-request-ids=<id1,id2>`：显式指定证据卡 requestId 列表（最高优先级）。
2. `--ai-cards-fixture=<path>`：读取 fixture 卡片；支持数组、`{ cards: [...] }` 与单对象 `{ requestId: ... }`。
3. `--ai-audit-export=<path>`：读取审计导出（JSON/NDJSON），支持 `requestId/request_id`、`newValue/new_value`、`metadataJson/metadata_json` 字段归一。
4. `--ai-request-limit=<N>`：未显式传 `--ai-request-ids` 时，从审计导出中自动抽样最近 N 个 requestId（默认 3，上限 20）。
5. 等价环境变量：`RELEASE_EVIDENCE_AI_REQUEST_IDS`、`RELEASE_EVIDENCE_AI_CARDS_FIXTURE`、`RELEASE_EVIDENCE_AI_AUDIT_EXPORT`、`RELEASE_EVIDENCE_AI_REQUEST_LIMIT`。
6. 缺失数据口径：若 requestId 在导出中不存在，证据卡写 `status=skipped` 且 `skipReason=request_id_not_found_in_audit_export|request_id_not_found_in_fixture`，禁止伪造 `ready`。

**验收标准**：报告中至少 1 张证据卡字段齐全；与手工从 DB 导出对照一致。

**风险**：fixture 与真实环境差异 —— 用 `skipped` 显式表达，不伪造数据。

---

### 4.3 工单 A3 — 时间轴性能「证据卡」

**目标**：将时间轴与轨道布局 perf 门限结果结构化写入发布报告。

**数据来源（以本仓现有脚本为优先）**：

1. `npm run perf:track` → [src/perf/TrackLayoutPerformanceBaseline.test.ts](src/perf/TrackLayoutPerformanceBaseline.test.ts)
2. **时间轴索引 perf**：优先 `npm run test:timeline-cqrs-phase9`（已包含 `src/perf/TimelineUnitViewIndexPerformanceBaseline.test.ts`）。若未来抽离独立脚本（如 `perf:timeline`），报告卡中记录**实际执行的 npm script**。
3. 可选：`npm run perf:ai` → [src/ai/perf/AiPerformanceBaseline.test.tsx](src/ai/perf/AiPerformanceBaseline.test.tsx)

**实现要点**：

1. v1：解析 vitest verbose 输出中的关键行（已有 `console.info` 前缀的可作为稳定锚）；v2：改为 `--reporter=json` 写入临时文件再解析（与第 6.3 节 P1-PerfReport 联动）。
2. 报告中记录：`medianMs` 或 `elapsed`、门限 `maxMs`、是否通过。

**验收标准**：同机两次运行报告数值可复现（允许微小浮差，文档说明容差）。

**风险**：CI 时长 —— 默认放入 `gate:release-evidence:full`（见第 1.2 节），日常 PR 的 `lite` profile 跳过 perf 全量。

---

### 4.4 工单 A4 — 架构文档与代码符号一致性守卫

**背景**：[docs/architecture/timeline-unit-governance.md](docs/architecture/timeline-unit-governance.md) 提及 `LEGACY_LOCAL_CONTEXT_TOOL_ALIAS_MAP` 与 legacy 工具名；当前 [src/ai/chat/localContextTools.ts](src/ai/chat/localContextTools.ts) 为 canonical 集合与 `normalizeToolName`，仓库内无该常量符号。

**目标（已拍板，2026-04-25）**：采用“组合路径”，且顺序固定为“先修订架构文档，再启用/扩大守卫”；不得跳过第 1 步直接扩守卫。

**执行顺序（已拍板，降低误杀与返工）**：

1. **先修订架构文档**：按“别名阶段已结束”口径更新 governance 文档：删除对已不存在的移除闸门的虚构前提，并明确「当前实现为 canonical-only，无 LEGACY map」。
2. **再启用/扩大守卫**：新增 `scripts/check-doc-code-symbol-parity.mjs`（名称可调整），对允许列表中的架构文档扫描反引号内 `src/...` 路径与标识符，校验符号在目标文件存在（或反向：禁止引用不存在符号）。豁免用注释或清单维护。

**验收标准**：`npm run check:architecture-guard` 与新建检查脚本在 CI 中通过；文档与代码无矛盾陈述。

**风险**：误杀合法文档 —— 用允许列表与豁免注释标记。

---

## 5. 阶段 B：统一 AI 工具与扩展策略（P0）

### 5.1 工单 B1 — 策略 SSOT 数据模型与存放位置

**目标**：引入单一结构化策略源（建议：`src/ai/policy/aiToolPolicyMatrix.json` + 生成的 `aiToolPolicyMatrix.types.ts`，或纯 TS `const` 满足 typecheck），描述每一工具：

- `toolName`（与 [src/ai/chat/toolCallSchemas.ts](src/ai/chat/toolCallSchemas.ts) 中 `AiChatToolName` 对齐）
- `riskTier`：`low` | `medium` | `high`
- `destructive`：boolean
- `requiresExplicitTarget`：boolean 或更细粒度字段集
- `confirmationMode`：`none` | `pending_propose_changes` | `host_modal` 等枚举
- `auditReasonCodes`：与 `writeToolDecisionAuditLog` 中 `newValue` / metadata 可映射的稳定码列表（与第 1.1 节「审计 reason」命名一致）
- `extensionCapabilityHints`：可选，映射到 [src/extensions/extensionRuntime.ts](src/extensions/extensionRuntime.ts) 中 `ExtensionCapability`
- **可选（P1 并行）**：`toolOutputTrust` 或等价枚举，标注该工具结果是否允许**未经净化**进入 RAG/摘要回灌，用于与第 6.2 节 ObsSec 的「工具输出—上下文」信任边界对齐（默认保守）。

**权威规则**：以**用户可见最终结果**（拒绝文案、待确认状态、任务会话状态）反推各层校验；SSOT 为规范，现有模块为执行器。

**验收标准**：新增工具时仅改 SSOT + 测试即可通过 pipeline。

---

### 5.2 工单 B2 — 渐进迁移执行器

**迁移顺序（建议）**：

1. [src/pages/transcriptionAiToolRiskCheck.ts](src/pages/transcriptionAiToolRiskCheck.ts) 从 SSOT 读取「需显式 target」类规则，删除重复硬编码列表。
2. [src/hooks/useAiChat.destructiveGate.ts](src/hooks/useAiChat.destructiveGate.ts) 对齐 `destructive` 与 `confirmationMode`。
3. [src/hooks/useAiChat.toolDecisionPipeline.ts](src/hooks/useAiChat.toolDecisionPipeline.ts) 仅保留编排，不保留分散的魔法字符串比较（逐步收）。

**兼容策略**：保留一期 `LEGACY_TOOL_POLICY_OVERRIDE` 环境变量或特性开关，默认关闭；对照测试覆盖审计 reason `ambiguous_target`、`invalid_proposed_changes`、扩展 capability deny。

**验收标准**：现有 vitest 全绿 + 新增「策略表与运行时一致性」单测文件。

**风险**：迁移遗漏 —— B1 中 `auditReasonCodes` 与日志解析脚本对齐，发布报告统计异常码突增可报警。

---

### 5.3 工单 B3 — 扩展能力审计进发布报告（轻量）

**目标**：在 A1 报告中增加一节 `extensions.capabilityAuditSummary`：按 `extensionId` 聚合最近 N 条 [extensionRegistry.ts](src/extensions/extensionRegistry.ts) `getCapabilityAuditTail` 的成功率。

**扩展元数据（建议增强）**：除成功率外，可增加轻量 **manifest 式字段**（扩展 id、声明能力列表版本、加载来源类型），与「MCP 式供应链」治理思路对齐；v1 可为空，不阻塞 B3 主线。

**验收标准**：无扩展加载时章节为空数组且不失败。

---

## 6. 阶段 B 并行项（P1）：契约、观测、性能与成本

### 6.1 工单 P1-ToolsDoc — 从 schema 导出契约与 diff

**状态（2026-04-25）**：已完成（v1）

**目标**：

1. `scripts/export-tool-call-contract.mjs`：从 `toolArgumentSchemas` 与 `AI_CHAT_TOOL_NAMES` 相关导出生成 `docs/execution/audits/tool-call-contract.snapshot.json`（路径可调整，须落在 [docs/execution/audits/](docs/execution/audits/) 或 release-gates 子目录）。
2. CI：`npm run check:tool-call-contract:drift` 对比快照与当前生成结果。

**验收标准**：故意改 schema 时 drift 检查失败且 diff 可读。

**验收命令（v1）**：

1. `npm run export:tool-call-contract`
2. `npm run check:tool-call-contract:drift`

---

### 6.2 工单 P1-ObsSec — 脱敏策略单一清单与覆盖率

**状态（2026-04-25）**：已完成（v1）

**目标**：

1. 将 [src/observability/otel.ts](src/observability/otel.ts)、[src/observability/sentry.ts](src/observability/sentry.ts)、[src/observability/logger.ts](src/observability/logger.ts) 中敏感键模式收敛为 `src/observability/sensitiveKeyPolicy.ts`（新文件）单一导出。
2. 增加 vitest：对固定 payload 断言 scrub 后不含明文 token。
3. 可选：脚本统计「策略正则数量」写入发布报告（弱证明，配合测试强证明）。

**信任边界（建议纳入同一里程碑或紧随其后的 PR）**：在规格层区分「用户输入」「模型输出」「工具返回值」「RAG 片段」进入观测与持久化前的处理要求；对**工具输出 / RAG 回灌**的 prompt 注入面，在审计或 SSOT 中预留归类位（与第 5.1 节可选字段呼应），避免仅做键名脱敏而忽略内容型风险。

**验收标准**：三处观测入口均引用同一策略模块；相关测试通过。

**验收命令（v1）**：

1. `npx vitest run src/observability/logger.test.ts src/observability/sentry.test.ts src/observability/otel.test.ts src/observability/sensitiveKeyPolicy.test.ts`
2. `npm run typecheck`

---

### 6.3 工单 P1-PerfReport — perf 输出结构化接入 A1

**状态（2026-04-25）**：已完成（v2）

**目标**：A3 的 v2：vitest JSON reporter 产出解析后写入 `release-evidence.json` 的 `perf.cards[]`。

**验收标准**：与 A1 一次命令产出；`full` profile 下启用。

**验收命令（v2）**：

1. `npx vitest run scripts/generate-release-evidence-bundle.test.ts`
2. `npm run report:release-evidence:full:dry-run -- --perf-json-report=<path-to-vitest-json>`

**风险**：CI 时长 —— 见第 4.3 节与第 1.2 节 `lite/full`。

---

### 6.4 工单 P1-ProgressiveDisclosure — 渐进披露专项门限

**状态（2026-04-25）**：已完成（v1）

**目标**：将“渐进披露是否有效”从描述性结论转为可追踪门限。

**指标定义（v1）**：

1. `first_pass_resolution_rate`：无需澄清即可完成的会话占比。
2. `clarify_then_success_rate`：进入 `waiting_clarify` 后一次补充即成功的占比。
3. `advanced_entry_reach_rate`：进入次层能力（如预算继续、展开摘要、高级工具确认）的会话占比。
4. `clarify_loop_rate`：连续 2 次及以上澄清仍未完成的占比（应受控）。

**数据源落点（实现强制）**：每条比率须注明**分子分母来源**（例如：来自 `writeToolDecisionAuditLog` 的某 metadata 字段、`metrics.ts` 的某 id、或 `full` profile 下采样的 requestId 列表）。若某环境无法采集，该卡片的对应字段显式 `skipped` 并附 `skipReason`，避免报告仅具空壳字段。

**验收标准**：

1. A1 报告中新增 `progressiveDisclosure.cards[]`。
2. 支持 `lite/full` 两档：`lite` 仅统计比率，`full` 增加样本 requestId。
3. 至少 1 个发布周期出具趋势图（可复用 `report:m5-trend` 输出机制）。

**验收命令（v1）**：

1. `npx vitest run scripts/generate-release-evidence-bundle.test.ts`
2. `npm run report:release-evidence:core:dry-run`

---

### 6.5 工单 P1-CostGuard — 成本阈值与输出上限治理

**状态（2026-04-25）**：进行中（v1.1，已补趋势证据）

**目标**：将现有 token 预算预警升级为成本与输出双阈值治理。

**交付**：

1. 会话级预算上限（超限时产生稳定审计 reason，并在结果层可见；与第 1.1 节命名一致）。
2. 输出 token capped default（默认封顶），超限场景触发一次升级重试。
3. 报告新增成本治理卡：预算触发次数、重试触发率、重试成功率。
4. 报告新增 `costGuard.trend`：按日输出 requestCount / budget / retry / retrySuccess / outputCap，多发布周期对照可直接复用。
5. 子代理上下文瘦身策略：按任务类型裁剪上下文（如只读研究代理默认省略高成本上下文块），并保留 kill switch。

**多厂商 / 多模型注意**：不同 provider 的 token 计数与计费口径可能不一致；成本卡元数据须记录**采用的估算规则版本**（例如统一用本地 tokenizer 估算或供应商返回值），否则跨发布对比易失真。

**验收标准**：

1. 不影响正常短会话成功率。
2. 预算超限与升级重试均可被审计回放。
3. 上下文瘦身开启后，token 消耗下降且核心成功率不劣化（至少一个发布周期对照）。

**验收命令（v1.1）**：

1. `npx vitest run scripts/generate-release-evidence-bundle.test.ts`
2. `npm run report:release-evidence:core:dry-run`
3. `npm run gate:release-evidence:cost-guard-trend`（机读校验 `costGuard.trend.compareReady/pointCount/points[]` 结构）
4. `npm run gate:release-evidence:core:require-cost-guard-trend-ready`（当进入多周期稳定阶段后启用；compareReady=false 时 enforce 失败）

**严格门禁启用规则（先 1 后 2）**：

1. 先在 CI `workflow_dispatch` 手动触发中开启 `require_cost_guard_trend_ready=true` 做影子验证（不改默认流水线）。
2. 连续两个发布周期 `costGuard.trend.compareReady=true` 且趋势点 `pointCount>=2` 后，再切换为常态启用。
3. 若任一周期回退到 `compareReady=false`，回滚到结构校验门禁（保留 `gate:release-evidence:cost-guard-trend`，关闭 strict）。

---

### 6.6 工单 P1-RagShapeTelemetry — 检索形态遥测

**状态（2026-04-25）**：已完成（v1）

**目标**：补齐检索质量的“分母信息”，避免只看最终命中。

**交付**：

1. 指标：候选规模、空选率、重复抑制率、预算抑制率、新鲜度分布。
2. 报告卡：`memoryRecallShape`（允许空选但必须记录运行过）。

**验收标准**：

1. 与 C1 两阶段检索联动，不增加默认上下文体积。
2. 至少一个发布周期可对比优化前后曲线。

**验收命令（v1）**：

1. `npx vitest run scripts/generate-release-evidence-bundle.test.ts`
2. `npm run report:release-evidence:core:dry-run`

---

## 7. 阶段 C：触发式加深（P0 / P1 / P2）

**执行前提（新增强约束）**：C 阶段详细设计仅作为候选方案，不得先于阶段 A 可运行闭环（A1a+A4 完成）进入实现。每次进入 C 阶段前，需先依据第 3.3.1 看板与最近一期 `release-evidence` 进行一次重新评估与拆卡。

### 7.1 触发条件（满足任一即启动 C 对应工单）

1. `report:m5-trend` 或发布报告中时间轴 perf 卡连续 **2 个里程碑** 退化超约定容差。
2. **指标** `blocked_write_without_explicit_target_total` 持续高位，**或** 审计/门控路径中 `**ambiguous_target` 类 reason** 持续高位（二者可分别设阈值；撰写告警规则时按第 1.1 节区分命名空间）。实现细节见 [src/pages/transcriptionAiToolRiskCheck.ts](src/pages/transcriptionAiToolRiskCheck.ts) 与 [src/hooks/useAiChat.destructiveGate.ts](src/hooks/useAiChat.destructiveGate.ts)。
3. 语音场景确认需 **2+ 引擎** 并行灰度（产品决策）。

### 7.2 工单 C1 — Memory Broker（统一检索层）

**目标**：新增 `src/ai/memory/memoryBroker.ts`（路径可调整），统一 API 形如：

```ts
// 规格说明用伪代码；实现时以实际类型为准
retrieveMemoryContext(input: {
  query: string;
  tokenBudget: number;
  session: AiSessionMemory;
  projectMemoryStore: ProjectMemoryStore | null;
}): MemoryChunk[];
interface MemoryChunk {
  text: string;
  source: 'session_summary' | 'session_preferences' | 'project_rag' | ...;
  score?: number;
  why: string; // 人类可读：命中原因
  freshnessDays?: number; // 可选：距今天数，用于陈旧提示
  suppressedBy?: 'already_surfaced' | 'recent_tools' | 'budget' | null;
}
```

**约束**：与 [useAiChat.agentLoopRunner.ts](src/hooks/useAiChat.agentLoopRunner.ts) 中 token 预算逻辑协同；broker 必须支持「截断后仍返回 why」。

**检索编排要求**：

1. 两阶段检索：`候选召回`（低成本）→ `重排选择`（限额 topN）。
2. 默认抑制重复暴露：支持 `already_surfaced` 过滤。
3. 对近期工具文档噪声抑制：支持 `recent_tools` 过滤。
4. 预算不足时允许降级，但需返回 `suppressedBy='budget'` 的可解释标记。

**快照同步协议**：

1. 增加 `snapshotTimestamp` 与 `syncedFrom` 元数据，用于判断本地记忆与项目快照是否一致。
2. 同步动作三态：`initialize`（首次同步）、`prompt_update`（提示是否更新）、`none`（无需动作）。
3. 进行 replace 同步前先清理本地孤儿记忆文件，避免旧内容残留误召回。

**验收标准**：单测覆盖预算截断与空 store；不增加默认上下文大小（默认行为与现网一致，开关启用 broker）。

---

### 7.3 工单 C2 — 扩展信任等级、配额与健康度（P1 完整版）

**前置**：B3 已稳定运行至少一个发布周期。

**交付**：信任等级（影响 capability 默认允许集）、每扩展 QPS 或每日调用配额、健康度报表（失败率、P95 延迟）。

---

### 7.4 工单 C3 — 语音 Provider Adapter Manifest（P2）

**前置**：触发条件 7.1.3。

**目标**：在 [VoiceAgentService.ts](src/services/VoiceAgentService.ts) 已有 lazy runtime 之上，增加**仅描述已存在 provider** 的 manifest（engine id、加载函数、健康探针接口），避免抽象层过厚。

**验收标准**：离线切换 `whisper-local` 路径可单测；不增加包体显著体积。

---

### 7.5 工单 C4 — 意图多候选与置信门控（P1）

**前置**：B1/B2 稳定运行至少一个发布周期。

**目标**：在现有工具意图分流基础上，补齐“多候选意图 + 置信度阈值 + 最小澄清”机制。

**交付**：

1. `intentCandidates`（最多 3 个）与 `confidence` 字段进入审计元数据。
2. 当 top1-top2 置信差低于阈值时，强制进入单问题最小澄清。
3. 澄清后一次成功率纳入第 6.4 节指标卡。

**验收标准**：

1. 不增加默认工具误调用率。
2. `clarify_loop_rate` 较基线不劣化，且 `clarify_then_success_rate` 有提升趋势。

---

### 7.6 工单 C5 — 后台记忆抽取子代理（P1）

**前置**：C1 已灰度启用并稳定一个发布周期。

**目标**：新增后台抽取流程，从会话增量中提取高价值记忆，不阻塞主链路。

**交付**：

1. 抽取触发器（默认在主响应完成后执行，且可节流）。
2. 互斥策略：主链路已写记忆时跳过后台抽取并推进游标。
3. 尾部合并：抽取中收到新上下文时合并为 trailing run。
4. 恢复一致性协议：恢复执行前过滤未闭环 `tool_use`、孤立 thinking 消息，并重建内容替换状态，避免恢复后上下文漂移。
5. 恢复运行容错：原 worktree 或上下文锚点缺失时自动回退到父会话安全路径并留审计事件；对工作区路径做**存在性校验**（对齐远端/无头产品中「工作目录丢失」类失败语义，避免静默进入错误上下文）。

**Transcript / 持久化 schema（建议）**：大版本升级时为会话 transcript 或等价存储增加 **schema 版本字段**；证据 bundle 中记录该版本，便于跨版本对比恢复行为与回归范围。

**验收标准**：

1. 抽取失败不影响主会话成功返回。
2. 抽取流程具备独立审计与耗时统计。
3. 恢复执行在“中断后继续”场景不出现重复 tool 结果与消息链断裂。

---

### 7.7 工单 C6 — 后台任务工具沙箱（P1）

**前置**：C5 设计评审通过。

**目标**：为后台任务建立独立 capability profile（不同于前台对话执行器）。

**交付**：

1. 只读工具默认放行（Read/Grep/Glob 类）。
2. Shell 仅允许只读命令集合。
3. 写入工具仅允许在授权目录内执行；**工作区根与 cwd** 须在执行前绑定校验，拒绝相对路径逃逸。
4. 命令语义级 fail-closed：变量展开、花括号扩展、process substitution、`cd + 写操作` 复合链路默认不自动放行。
5. 只读命令采用「命令 + 参数白名单」验证；对 deny/ask 输出稳定审计 reason（与第 1.1 节一致）。

**验收标准**：

1. 沙箱拒绝具备稳定 reason。
2. 非授权写入在回归测试中全部被拦截。
3. 安全绕过样例（变量注入、重定向绕过、`cd && write`）全部落入 ask/deny，不出现静默放行。

---

### 7.8 工单 C7 — 轻量协调模式与并行策略规范（P2）

**前置**：C5/C6 稳定运行。

**目标**：引入轻量协调语义层，不引入全量多智能体编排复杂度（与「全 swarm」方案刻意保持距离）。

**交付**：

1. 任务相位：`research` / `synthesis` / `implementation` / `verification`。
2. 并行策略：只读并行、写入串行、验证可并行。
3. 任务通知协议最小版（完成/失败/中止 + usage 摘要）。
4. 任务通知 schema：`taskId`、`status`、`summary` 必填；`result`、`usage` 选填；不合规通知进入隔离通道。
5. 子代理生命周期收尾清单：任务结束后清理临时上下文、挂起任务、会话级遗留状态，避免僵尸任务与状态泄漏。
6. **可观测性**：相位切换与并行决策写入审计元数据；若存在子代理身份，建议增加稳定 `actorId`/`taskId` 维度（与 C7 通知 schema 对齐），避免仅依赖自然语言描述。

**验收标准**：

1. 相位切换与并行决策可观测（审计元数据可追踪）。
2. 复杂任务平均往返轮次下降或持平。
3. 生命周期收尾完成率可统计，且残留后台任务数在会话结束后归零。

---

## 8. 与本仓其他规划、协作闸门与 Release Profile

### 8.1 与时间轴性能工单的关系

本方案与 [时间轴稳定性与性能三批次工单-2026-04-20.md](时间轴稳定性与性能三批次工单-2026-04-20.md) **正交**：后者偏组件层性能与渲染稳定；本方案偏**发布工程、AI 治理与可证明性**。同一里程碑可在发布报告中**同时引用**两份工单的证据索引行。

### 8.2 与协作里程碑链（`gate:m8` 及以后）的关系

`package.json` 中 `**gate:m8`～`gate:m14`** 与 `**gate:collaboration-cloud**` 等脚本，代表协作与云上同步能力的**独立里程碑链**，与本方案 AI 工具治理**正交**。

**规格要求**：

1. **默认不隐含**：AI 向量的 `core` profile **不得**默认断言协作链全绿；是否纳入 bundle 由发布类型与第 1.2 节 profile 显式选择。
2. **证据索引可组合**：协作发布周可在 `collab-cloud` profile 下增加索引行，引用各 `report:m*-gate` 或 `gate:m`* 的退出码与日志路径，保持与 A1「子进程输出引用」同一风格。

### 8.3 Release Profile 与 npm scripts 对照（新增，v1）

以下为落地执行的**最小映射**，后续若调整脚本，以本表与 `package.json` 同步更新为准：

| Profile | 必跑脚本（enforce） | 可选脚本（shadow/扩展） | 说明 |
| --- | --- | --- | --- |
| `lite` | `check:docs-governance`、`check:architecture-guard` | `test:timeline-cqrs-phase9`（按变更面启用） | 快速反馈，不要求全链路 |
| `core` | `report:m5-trend`、`gate:m6-release`、`gate:m7-extension` | `gate:m6-release:shadow`、`gate:m7-extension:shadow` | AI/观测主链路 |
| `collab-cloud` | `core` + `gate:collaboration-cloud` | `gate:m8-collaboration`～`gate:m14-collaboration-promotion`（按发布声明选择） | 协作发布周 |
| `full` | 在选定基线之上增加 `perf:track`、`perf:ai`、`test:timeline-cqrs-phase9` | 各阶段 shadow 脚本与额外回归套件 | 发布候选或重大版本 |

### 8.4 CI 接入速查（2026-04-25 已落地）

当前 CI 口径为“core 常驻 + full 手动触发”：

1. 常驻门禁：`release-evidence-core-gate` 在 CI 中默认执行 `npm run gate:release-evidence:core`。
2. 手动门禁：`release-evidence-full-gate` 仅在 `workflow_dispatch` 且 `run_release_evidence_full=true` 时执行 `npm run gate:release-evidence:full`。
3. 手动触发建议：发布候选周或重大版本时再开启 full，避免日常 PR 被长链路阻塞。
4. artifact 上传：core/full 两个 job 均上传
  - `docs/execution/release-gates/release-evidence/release-evidence-*.json`
  - `docs/execution/release-gates/release-evidence/logs/**/*.log`
5. npm 入口对照：
  - `gate:release-evidence:core`
  - `gate:release-evidence:full`
  - `report:release-evidence:core:dry-run`
  - `report:release-evidence:full:dry-run`

---

## 9. PR 与拆卡建议


| 建议 PR 顺序 | 标题前缀                        | 大致范围                                     |
| -------- | --------------------------- | ---------------------------------------- |
| 1        | `chore(release-evidence-a1a):` | A1a 报告骨架 + package.json 脚本入口               |
| 2        | `chore(docs-guard):`           | A4 文档修订 + 符号守卫                            |
| 3        | `chore(release-evidence-a1b):` | A1b 子进程编排 + 证据索引最小闭环                     |
| 4        | `feat(ai-audit):`              | A2 证据卡与 auditReplay 对接                    |
| 5        | `feat(ai-policy):`             | B1–B2 SSOT 与迁移                            |
| 6        | `chore(perf):`                 | A3 perf 解析与报告字段（对齐现有 npm script）          |
| 7        | `feat(cost-guard):`            | P1-CostGuard                              |
| 8        | `chore(rag-telemetry):`        | P1-RagShapeTelemetry                      |
| 9        | `chore(tools-contract):`       | P1-ToolsDoc                               |
| 10       | `chore(obs):`                  | P1-ObsSec（含信任边界可选位）                       |
| 11       | `feat(memory):`                | C1（触发后）                                   |
| 12       | `feat(memory-extractor):`      | C5 + C6                                   |
| 13       | `feat(coordination-lite):`     | C7                                        |
| 14       | `chore(runtime-hardening):`    | C5/C6/C7 恢复一致性 + 沙箱 fail-closed + 生命周期清理  |


### 9.1 5 周执行版（重排后）— 每周交付清单

> 目标：每周都能提交可验收资产，优先保证“报告可生成、证据可引用、风险可回滚”。

#### Week 1（本周）：A1a + A4

**交付**：

1. `report:release-evidence` 脚本可运行。
2. `release-evidence.json` 骨架（metadata/summary/evidenceIndex/degraded）。
3. 文档符号失配修复（A4）。

**验收**：

1. `npm run report:release-evidence -- --profile=lite`。
2. `npm run check:docs-governance`。

#### Week 2：A1b + A2

**交付**：

1. A1 子进程编排接入（docs/architecture/m5/m6）。
2. 报告 `evidenceIndex` 至少 5 行。
3. A2 至少 1 张 requestId 证据卡。

**验收**：

1. 失败场景仍产出报告。
2. 报告可定位首个失败步骤与日志路径。

#### Week 3：B1（策略 SSOT）

**交付**：

1. `src/ai/policy/aiToolPolicyMatrix.ts`（或等价 JSON + types）。
2. 与风险门控、审计 reason 最小对齐。

**验收**：

1. 策略一致性测试通过。
2. 新增工具仅需改 SSOT + 测试。

#### Week 4：A3 + P1-CostGuard v1

**交付**：

1. perf 卡写入报告。
2. 会话级成本硬阈值与可审计 reason。

**验收**：

1. 报告含 perf + cost 两类卡片。
2. 预算超限可回放，正常会话成功率不劣化。

#### Week 5：复盘与下一阶段决策

**交付**：

1. 汇总前四周证据与失败链路。
2. 形成是否启动 C 阶段的书面决议。

**验收**：

1. 决议可追溯到 `release-evidence` 产物。
2. 未达到触发条件则不启动 C 阶段实现。

**每周统一出站条件（DoD）**：

1. 当周产物已被 `report:release-evidence` 索引。
2. 新能力默认 behind-flag，且有回滚说明。
3. 当周未达 DoD 时，下一周只做收口，不加新能力。

---

## 10. 全量验收清单（发布前）

1. `npm run check:docs-governance`
2. `npm run check:architecture-guard`
3. `npm run report:release-evidence`（实现后）或当前等价 gate 组合；报告中 `**releaseProfile` 与 profile 涵盖范围**可核对
4. 工具契约 drift 检查（实现后）
5. 核心 vitest 子集（与团队现有 `validate` 约定一致）
6. `progressiveDisclosure.cards[]` 字段完整（允许 `skipped`，但不得缺失原因与数据源说明）
7. 抽样 10 条 AI 会话：确认“核心任务可首层完成，次层入口可发现且文案可预期”
8. 成本治理卡、检索形态卡、后台抽取卡三类证据均可在发布报告中检索到
9. 恢复一致性指标与生命周期清理指标在报告中可追溯到 requestId / taskId
10. 若本次发布声明协作能力变更：`collab-cloud`（或等价）profile 下协作链索引行齐全

---

## 11. 附录

### 11.1 关键源码锚点（便于评审）


| 主题                   | 文件与符号                                                                                                                         |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| 回放与 golden diff      | `loadAiToolReplayBundle`, `buildAiToolGoldenSnapshot`, `diffAiToolSnapshot` — [src/ai/auditReplay.ts](src/ai/auditReplay.ts)  |
| 审计写入                 | `writeToolDecisionAuditLog` — [src/hooks/useAiChat.toolAudit.ts](src/hooks/useAiChat.toolAudit.ts)                            |
| 指标摘要                 | `summarizeMetrics` — [src/observability/metrics.ts](src/observability/metrics.ts)                                             |
| 闸门编排                 | `resolveDestructiveGate` — [src/hooks/useAiChat.destructiveGate.ts](src/hooks/useAiChat.destructiveGate.ts)                   |
| 转写风险检查               | `createTranscriptionAiToolRiskCheck` — [src/pages/transcriptionAiToolRiskCheck.ts](src/pages/transcriptionAiToolRiskCheck.ts) |
| 扩展 deny              | `invokeCapability` — [src/extensions/extensionRuntime.ts](src/extensions/extensionRuntime.ts)                                 |
| Schema map           | `toolArgumentSchemas`, `validateToolArgumentsZod` — [src/ai/chat/toolCallSchemas.ts](src/ai/chat/toolCallSchemas.ts)          |
| 项目记忆检索               | `getRagContextVector` — [src/services/ProjectMemoryStore.ts](src/services/ProjectMemoryStore.ts)                              |
| Token 预算与 checkpoint | `pendingAgentLoopCheckpoint` — [src/hooks/useAiChat.agentLoopRunner.ts](src/hooks/useAiChat.agentLoopRunner.ts)               |
| Loop 预算阈值判定          | `shouldWarnTokenBudget` — [src/ai/chat/agentLoop.ts](src/ai/chat/agentLoop.ts)                                                |
| 意图分流与澄清              | `resolveToolIntentOutcome` — [src/hooks/useAiChat.intentResolution.ts](src/hooks/useAiChat.intentResolution.ts)               |
| 工具分级路径               | `gray/rollback/auto` 分支 — [src/hooks/useAiChat.toolDecisionPipeline.ts](src/hooks/useAiChat.toolDecisionPipeline.ts)          |
| 审计幂等与请求去重            | `hasPersistedExecutionForRequest` — [src/hooks/useAiChat.toolAudit.ts](src/hooks/useAiChat.toolAudit.ts)                      |
| 异步任务超时与清理            | `PendingWorkerRequestStore` — [src/services/PendingWorkerRequestStore.ts](src/services/PendingWorkerRequestStore.ts)          |
| 用户侧可发现性              | `agentLoopProgress`, `summaryQualityWarning`, 候选 chips — [src/components/ai/AiChatCard.tsx](src/components/ai/AiChatCard.tsx) |
| 混合意图门控               | `resolveHybridIntent`、`gate` 机制 — [src/services/HybridIntentResolver.ts](src/services/HybridIntentResolver.ts)                |
| RAG 反思门              | `shouldRunRagReflection` — [src/ai/ragReflection.ts](src/ai/ragReflection.ts)                                                 |
| 语音懒加载与网络切换           | `loadVoiceInputRuntime`, `AmbientObserver` — [src/services/VoiceAgentService.ts](src/services/VoiceAgentService.ts)           |


### 11.2 外部分析章节的索引对照（可选深读）


| 本方案集中讨论的主题         | claude-code-analysis 文档树中的典型入口（仓库内路径以该仓库为准） |
| ------------------ | ------------------------------------------- |
| Memory Broker / 检索 | `analysis/04-agent-memory.md`、上下文管理相关章      |
| 沙箱与命令安全            | `analysis/04e-sandbox-implementation.md`    |
| Tool 与权限           | `analysis/04b-tool-call-implementation.md`  |
| Session / Resume   | `analysis/04i-session-storage-resume.md`    |
| 证据化索引方法            | `analysis/07-code-evidence-index.md`        |


---

## 12. 修订记录


| 日期              | 修订内容                                                                                                                                                                                                                                                             |
| --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-04-20      | 初版：A/B/C 全量工单、验收、风险、与现有脚本对齐说明                                                                                                                                                                                                                                    |
| 2026-04-21      | 增补渐进披露治理、C1/C4、6 周节奏、7 项跨仓借鉴及机制级条款                                                                                                                                                                                                                               |
| 2026-04-21（第三轮） | **重组文档结构**（第 0–12 节）：并入术语与 Release Profile、编排器契约、外部参考映射、`package.json` 事实对齐、A3 与 `test:timeline-cqrs-phase9` 对齐、A4 推荐顺序、触发条件命名空间拆分、B3 manifest 建议、ObsSec 信任边界、渐进披露数据源硬约束、CostGuard 多厂商计量、C5/C6 工作区与 transcript 版本、C7 可观测维度、协作 gate 与 AI profile 正交说明；附录增加外部章节对照表 |
| 2026-04-25      | **执行重排与落地化重构**：新增 3.3 实证复核表、3.3.1 周进度看板、3.4 按产出密度的 5 周节奏；A1 拆分为 A1a/A1b；新增 C 阶段执行前提约束；新增 8.3 profile↔npm scripts 映射；重排第 9 节 PR 顺序与 9.1 周交付清单；A4 定版为“先修文档，再启用守卫”的组合路径；落地 `check-doc-code-symbol-parity` 并并入 `check:architecture-guard`。 |


