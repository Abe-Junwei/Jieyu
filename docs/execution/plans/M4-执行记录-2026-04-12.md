# M4 执行记录（2026-04-12）

里程碑：M4（W7-W8）  
目标：领域收口（首批绞杀迁移 + 旧入口转发 + 域服务回归）

## 1. 已完成交付物

1. 首批绞杀迁移（2 个高流量页面）
- `src/pages/useTranscriptionProjectMediaController.ts`
- `src/pages/useTranscriptionSegmentMutationController.ts`
- 变更方式：页面入口改为通过 `src/app` 调用，不再直连 `services`。

2. 旧入口转发层与废弃计划
- 新增：`src/app/transcriptionLegacyForwarding.ts`
- 废弃计划文档：`docs/execution/plans/M4-旧入口废弃计划-2026-04-12.md`

3. 领域服务回归测试集
- 新增：`src/app/TranscriptionAppService.test.ts`
- 新增脚本：
  - `test:m4-domain-app-regression`
  - `gate:m4-domain-closure`

## 2. 架构与契约变更

1. 应用服务可执行实现
- 文件：`src/app/TranscriptionAppService.ts`
- 新增能力：
  - 项目/音频操作转发（create/import/delete）
  - 语段变更转发（split/merge/delete）
  - 自动分段候选解析（cache 命中、边界门控、fallback）

2. 迁移进度更新
- 文件：`src/app/m3MigrationChecklist.ts`
- `useTranscriptionProjectMediaController.ts` 状态更新为 `migrated`
- `useTranscriptionSegmentMutationController.ts` 状态更新为 `migrated`
- 新增 `m4FirstBatchTargets` 与 `m4FirstBatchStats`

## 3. 本次验收命令

1. `npx tsc --noEmit`
2. `npm run check:transcription-legacy-forwarding-usage`
3. `npm run gate:m4-domain-closure`
4. `npm run build`

## 4. 量化结果（本次）

1. 核心页面应用服务接入率（首批目标）
- 结果：2/2（100%）

2. 旧入口调用占比（首批目标）
- 结果：0/2（0%）

3. 域服务回归通过率
- 结果：100%（`src/app/TranscriptionAppService.test.ts` + 两个高流量页面回归）

## 5. 结论

1. M4 首批绞杀迁移已落地并通过回归。
2. 页面入口已收口到 app 层，保留了有 deadline 的旧入口转发。
3. 可进入后续批次迁移（M4 remainder / M5 观测闭环）。
