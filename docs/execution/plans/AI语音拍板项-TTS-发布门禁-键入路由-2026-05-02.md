---
doc_type: execution-plan
status: active
owner: repo
last_reviewed: 2026-05-02
source_of_truth: execution-plan
depends_on:
  - ../adr/0028-assistant-multimodal-orchestration-local-first.md
  - ../adr/0029-assistant-tts-web-speech-policy.md
  - ./AI语音智能体全盘审查与架构演进方案-2026-05-01.md
---

# AI 语音相关拍板项汇总表（TTS · 发布门禁 · 键入路由）

> **用途**：把需要 **产品 / release owner / 架构 owner** 显式拍板的事项收敛为一张表；拍板后在对应 ADR、release-gates 与实现锚点文档中落勾，并更新本表「状态」列。

| 领域 | 拍板问题 | 可选方向（示例） | 当前仓库事实 / 工程默认倾向 | 建议决策人 | 状态 |
| --- | --- | --- | --- | --- | --- |
| **TTS** | 助手朗读 **默认开关**（新用户 / 新设备首次） | **A**：默认关，设置里显式开启<br>**B**：默认关 + 首次进入助手区时一次教育/ opt-in 弹层<br>**C**：默认开（需论证与 ADR-0029「不阻断主链」一致） | ADR-0029 为 **`proposed`**，正文写「默认关或关+首次明示由产品拍板」；实现上已有 `assistantTtsEnabled` 与 `speechSynthesis` 能力探测 | 产品 owner | 待拍板 |
| **TTS** | **v1 引擎范围**是否接受「仅 Web Speech `speechSynthesis`，无离线包 / 无第三方 SDK」 | **接受 v1 范围** / **不接受**（若不接受须另立项：包体、许可、宿主） | ADR-0029 草案 §决策 1：v1 以浏览器内置为唯一内置引擎 | 产品 + 桌面策略 owner | 待拍板 |
| **TTS** | 不支持或宿主不可用时：**静默跳过** vs **必出一次可关闭提示** | **静默跳过**（不阻塞发送/流式）<br>**弱提示**（toast 一次）<br>**强提示**（阻断或重复提示） | ADR-0029 倾向 **静默跳过**；若选强提示需改 UX 与测试矩阵 | 产品 owner | 待拍板 |
| **TTS** | ADR-0029 **何时改为 `accepted`** | 与上三行一起：**定稿后**改状态，并补 `docs/architecture/桌面端浏览器支持策略.md` 交叉链接 | 当前 **`proposed`**；0029 正文已列须与桌面策略互链 | 产品 owner + 工程 owner | 待拍板 |
| **发布门禁** | **Voice provider manifest / health** 何时可标 **release-stable** | **A**：满足「C 阶段方案约定」的连续 N 个周期无 P0 降级事件<br>**B**：仅人工 go/no-go，不写进 CI<br>**C**：其它量化定义（写出 N、指标、数据源） | 全盘方案 P2：C3 与 **连续周期** release evidence 验证后才宜标 stable；**CI 脚本未改**（Phase D2 叙述） | release owner + 工程 owner | 待拍板 |
| **发布门禁** | **D2 口径**：voice manifest 健康与降级路径的 **证据形态**（与 C 阶段 §4.2 一致的具体含义） | **A**：仅文档化采样步骤 + 人工归档<br>**B**：增加自动 gate / 报告脚本（须定义数据源与 flake 容忍） | 执行方案 Phase D2：**定义门禁口径**，未承诺替代代码证据 | release owner | 待拍板 |
| **发布门禁** | 语音相关 **E2E / 抽检清单** 与发版节奏：必跑 vs 选跑 | **必跑**（阻塞发版）<br>**选跑**（回归周 / 大版本） | 全盘方案 Phase A 验收提到 E2E 抽检；与 `docs/execution/release-gates/` 现有 voice rollout 对齐程度待统一 | release owner | 待拍板 |
| **键入路由战略** | 是否维持 ADR-0028：**键入不强制经过与语音相同的前置 NLU** | **维持**（当前架构事实）<br>**变更**：键入也进统一漏斗（须 **新 ADR** 显式 supersede 0028 §1 相关句） | ADR-0028 **已 accepted**：输入路径允许非对称；开放项表写明若改须新 ADR | 产品 owner（战略）+ 架构 owner（ADR 流程） | 待拍板（默认维持） |

## 使用说明

1. **拍板后**：在对应 ADR（尤其 0029）、`docs/execution/release-gates/` 下相关 rollout / checklist、以及 `docs/architecture/桌面端浏览器支持策略.md`（若涉 TTS）更新结论与本表「状态」。
2. **键入路由**若选「变更」：必须先合并 **新 ADR** 再动代码，避免与 0028 读模型冲突。
3. 本表不替代 **`npm run check:docs-governance`** 所约束的其它 governed 文档；仅作 **拍板索引**。
