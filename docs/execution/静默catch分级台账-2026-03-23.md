# 静默 catch 分级台账（2026-03-23）

## 说明
- 本台账仅统计批次A范围内的高风险路径。
- 分级标准：
  - P0：会导致用户可见功能失败，且需要 UI 提示 + 结构化日志。
  - P1：可忽略但会影响排障，至少需要结构化日志。
  - P2：合理忽略场景，可保留静默（需明确理由）。

## 已修复（P0/P1）

| 模块 | 场景 | 处理方式 | 状态 |
|---|---|---|---|
| src/pages/TranscriptionPage.tsx | 删除音频/项目失败 | 增加 try/catch + SaveState 错误提示 + logger | 已修复 |
| src/hooks/useImportExport.ts | 导入读取/archive replace-all 失败 | 统一 catch，冲突分支专用提示，结构化日志 | 已修复 |
| src/hooks/useTranscriptionUndo.ts | undo/redo/批量回退失败 | 冲突提示 + 栈回滚 + 结构化日志 | 已修复 |
| src/hooks/useTranscriptionRecoveryActions.ts | applyRecovery 失败 | 冲突检测 + 友好提示 + 结构化日志 | 已修复 |
| src/hooks/useTranscriptionUtteranceActions.ts | 批量操作失败与回滚失败 | 主失败 error 日志 + 回滚失败 warn 日志 + 统一失败文案 | 已修复 |
| src/components/ai/AiChatCard.tsx | prompt template/snapshot 解析失败 | 补 warn 日志，保留 UI 提示 | 已修复 |
| src/components/transcription/SpeakerAssignPanel.tsx | 本地持久化失败 | 补 warn 日志 | 已修复 |

## 保留并说明（P2）

| 模块 | 位置/场景 | 保留原因 | 风险 |
|---|---|---|---|
| src/hooks/useAiChat.ts | 加密设置迁移 fallback（secure write 不可用） | 兼容旧环境，失败后退回 legacy 存储 | 低 |
| src/hooks/useAiChat.ts | JSON 解析候选失败（非 JSON 文本） | 业务语义为“继续尝试其他候选”，不应打断 | 低 |

## 待后续批次改进
- 将跨模块错误提示映射进一步统一到一个 helper（当前已完成第一步：saveStateError helper）。
- 对 useAiChat 大文件中历史遗留 catch 做二次分级（建议独立批次，避免本批次范围外风险扩散）。
