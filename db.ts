/**
 * 兼容入口：统一转发到 src/db/index | Compatibility entry: forward to src/db/index
 *
 * 逐步迁移后可删除该文件并统一改用 src 路径导入。
 * This file can be removed after all imports are migrated.
 */

export * from './src/db/index';
